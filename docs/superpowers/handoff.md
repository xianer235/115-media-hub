# Agent Handoff

> 本文件只保留当前状态和最近的重要交接。
> 历史记录归档在 `docs/superpowers/handoff-archive.md`，通常无需阅读；仅在追溯旧决策、兼容性背景或历史问题时按需查阅。
> 归档中的“下一步”可能已经失效，不应视为当前任务；当前状态以代码、本文件和最新设计/计划为准。

## 当前状态

- **日期**: 2026-08-19
- **分支**: `main`
- **当前提交**: `0.8.1` 发布提交，已推送（`0.9.0` 版本元数据本次未提交未推送）
- **版本**: `0.9.0`，已同步 `version.json`、`CHANGELOG.md`、README 与本交接记录
- **最近完成**: 版本元数据更新至 `0.9.0`，收录 115 大目录读取分页重构与官方搜索（加载更多、断连自动降页重试）、刮削整理重命名批量合并与深层路径修复、刮削任务“等待执行”修复与启动恢复、预览性能优化、NFO 媒体信息分类、自动识别保留手动搜索/绑定、搜索框交互优化
- **已有验证**: 完整 580 项 unittest、项目 `compileall`、改动 JS `node --check` 与 `git diff --check` 均通过；本地无 Docker 重建与真实 115 大目录/大批量整理实测
- **下一步**: Docker daemon 启动后按项目代理配置 `docker compose up -d --build`，用真实 115 账号实测大目录读取与“加载更多”、官方搜索、1242 部合集内深层文件夹整理（含重命名位置与批量请求数）、刮削任务重启自动恢复与任务中心表现，并复核 NFO 保留与搜索框交互


## 最近重要交接

- 2026-09-18 | `main` 未提交 | 发布元数据更新至 **0.11.10**：接收夹整理支持中断（用户反馈"运行按钮没有变成黄色的方框暂停按钮"）。根因：接收夹整理跑在工作线程里，卡片按钮的中断态只看 `monitor_status.running/current_task`（那是扫描任务的状态），所以永远停在「立即整理」。改动：`quick_import` 新增 `_QUICK_IMPORT_CANCEL` + `request_quick_import_cancel()`，`run_quick_import` 在条目循环开头检查标记（已搬完的不回滚、未处理条目记「已中断，未整理」、运行记录写 `cancelled`、日志分段以「中断」收尾并在 finally 清标记）；`trigger=manual` 时整理锁改为非阻塞（此前会等满 300 秒），自动触发仍排队；`/monitor/stop` 增加 inbox 分支调用中断；状态接口新增 `cancelling`（仅运行中为真）。界面：卡片运行状态改由 `/scraper/quick-import/status.running` 驱动（按钮变琥珀色 `monitor-task-icon-btn-stop`、点击走 `stopMonitorTask`）、运行中刷新间隔 30s→5s、弹窗按钮改成 `#inbox-task-run-btn` + `toggleInboxTaskRun()`（同一开关，运行中显示「中断整理」/「正在中断…」）。测试：`tests/test_quick_import.py` 增至 55 项（空闲中断返回 False、中断后整理停住并写 cancelled）、`tests/test_quick_import_frontend.py` 增至 18 项（运行状态来源 / toggle / 5 秒刷新 / 路由调用中断）；完整 unittest 897 项、compileall、`node --check`、`git diff --check`、`npm run build:css`（新增 bg-amber-500 / hover:bg-amber-400）均通过；临时服务实测 status 的 running/cancelling 字段与空闲中断返回 idle。另：用户问到文件夹名里的 TMDB ID 括号写法，已查证主流是方括号（Jellyfin 官方文档 `Movie Name (year) [metadata provider id]` 示例 `[imdbid-tt12801262]`、*arr 社区 TRaSH Guides 示例 `[tmdbid-1520211]`；Radarr 的 `{TmdbId}` 是模板占位符语法），本项目写的 `[tmdbid-123]` 属于主流写法，未改动。下一步：用户提交推送后确认 Docker Release 钩子构建 `0.11.10`，再 `docker compose up -d --build` 实测长任务中断手感。
- 2026-09-18 | `main` 本地已提交 `1a9f85d`（合并远端 `e61f7db` 的 0.11.8 中间快照，采用本地最终状态） | 发布元数据更新至 **0.11.9**：`version.json`（版本号 + 6 条本批说明）、`CHANGELOG.md`（新增顶部 `[0.11.9]` 段，把「接收夹并入任务列表」「评审后的安全 / 停用 / 定时整理修订」两节从 0.11.8 移到 0.11.9，并把 0.11.8 保留为已推送的「整理复用现有文件夹 + 接收夹专属 webhook + 快捷导入界面重排」）、`README.md`（当前版本 + 近期更新首两条）与本记录。动机：`.github/workflows/docker-release.yml` 的触发条件是 `paths: version.json`，沿用 0.11.8 不会重新触发镜像构建，所以必须往上推一格。本批实际内容：接收夹改成内置固定任务（类型不可改 / 不能删 / 默认已存在，只配目录与分发目标）、webhook 默认关闭并要签名密钥才能开、停用=定时与全部自动触发停止、接收夹支持定时整理与执行延时、保存路径统一按 115 根目录相对路径、接收夹日志并入监控日志并成为独立分段、把接收夹从扫描链路（变更事件 / 扫描队列 / 资源导入文案）彻底摘除。验证：完整 unittest 894 项、compileall、改动 JS `node --check`、`git diff --check`、version.json 解析、`npm run build:css` 均通过；临时服务实测配置迁移、前缀 savepath 归一化、类型互换拦截、停用任务入队拦截、接收夹日志分段。下一步：用户 `git add -A && git commit -m "发布 0.11.9：…" && git push origin main`，确认 Docker Release 钩子跑出 `xianer235/115-media-hub:0.11.9` 镜像后，再 `docker compose up -d --build` 复核类型只读、webhook 密钥门控、接收夹定时整理与日志分段、资源导入到接收夹目录的文案。
- 2026-08-19 | `main` 未提交 | 发布元数据更新至 `0.9.0`：同步 version.json、CHANGELOG、README 与交接状态，收录 115 大目录分页+官方搜索、重命名批量合并、深层整理路径修复、任务“等待”修复、预览性能、NFO 媒体信息分类、自动识别保留手动搜索/绑定、搜索框交互优化。完整 unittest 580 项通过；未提交未推送。
- 2026-08-19 | `main` 未提交 | 修复 NFO 文件被误判为广告：`.nfo` 从 `SCRAPER_BATCH_AD_EXTENSIONS` 移除，新增 `SCRAPER_BATCH_INFO_EXTENSIONS` 与 `info` 分类——NFO 作为媒体信息文件始终保留原名、不参与整理、即使开启"删除广告文件"也不删除，预览提醒文案改为"媒体信息 NFO/广告/说明/标准封面等"；剧集集数解析的排除集合同步加入 `info`。更新 1 项既有断言并新增 1 项回归（开启删除广告时 NFO 仍保留、无删除动作）；完整 unittest 580 项、compileall、`git diff --check` 均通过。Docker 未重建，刷新页面即可生效。
- 2026-08-19 | `main` 未提交 | 修复批量整理自动识别完成时清空用户正在输入的手动搜索/手动绑定：`identifyBatch` 在自动搜索结束后无条件重置 `batchSearchState`/`batchBindings`/`batchIncluded`，而 `scanBatch` 发起扫描时已重置过，二次重置会抹掉用户在自动识别期间打开并输入的手动搜索框（以及已手动绑定的条目）。删除 `identifyBatch` 内的重复重置，只保留自动匹配结果写入。新增 1 项前端回归（自动识别无匹配时保留手动搜索词/绑定/勾选）；完整 unittest 578 项、改动 JS `node --check`、`git diff --check` 均通过。Docker 未重建，刷新页面后即可生效。
- 2026-08-19 | `main` 未提交 | 大目录读取 IncompleteRead 进一步加固：目录列表在断连/超时类错误重试仍失败时，自动缩小单页大小（默认 500→250→100）重试，更小响应不易被服务端掐断（仅对可重试错误缩小，405 等确定性错误直接抛出）；`resolve_115_entry_by_name` 改为复用带重试的分页读取（顺带修复二次规范化把 id 当文件 id 的隐患，测试覆盖），STRM 按名查找两个函数补上断连重试封装。新增 1 项缩小页大小回归；完整 unittest 577 项、compileall、`git diff --check` 均通过。Docker 未重建，需重启服务后在大目录实测。
- 2026-08-19 | `main` 未提交 | 修复刮削任务卡"等待执行"：根因是后台任务只有单线程事件循环，同步的 `run_scraper_job` 会占住它，前面有任务（如慢的 115 请求）时新任务只能排队；且无启动恢复，重启也不会续跑。改法：刮削任务/回滚改走独立单线程执行器 `submit_scraper_job`/`submit_scraper_rollback`（不再占用共享后台循环，也不被监控/目录树任务堵住），意外异常由 `_run_scraper_job_guarded` 标为失败避免卡"执行中"；新增 `requeue_scraper_jobs_on_startup` 在启动时把 pending 任务重新入队、把 running 任务标为"服务重启，任务中断，请重新执行"，并接入 startup。新增 1 项启动恢复回归；完整 unittest 576 项、compileall、`git diff --check` 均通过。Docker 未重建，需重启服务后验证等待任务自动续跑。
- 2026-08-19 | `main` 未提交 | 修复大目录下刮削预览"没有可执行项"的定位与健壮性：计划期/执行期的名称查找（`_target_name_exists`/`_walk_existing_folder`/`_ensure_folder_from_base`）原来会触发完整模式全量扫描（分页重构后会把大目录所有页拉完），改为有界分页查找（每页 300、找到即停、最多 80 页，与 `resolve_115_entry_by_name` 一致），并新增 `_get_scraper_entries_page`/`_scraper_page_has_more`；前端"预览没有可执行项"toast 改为直接显示前 2 条冲突/失败原因（`data.issues`），不再只给"请处理冲突后再试"。真实案例"1242 部电影合集"内深层文件夹：怀疑合集里已有同名/同标题条目被新分页查找暴露（此前 300 条截断看不到），需重试后按 toast 具体文案确认。新增 0 项（既有 575 项全过），更新 9 处测试桩 `fake_list` 接受 offset/limit；compileall、改动 JS `node --check`、`git diff --check` 均通过。Docker 未重建，需重启服务/刷新页面后重试验证。
- 2026-08-19 | `main` 未提交 | 刮削整理重命名批量合并（官方接口一次多条目）：新增 `rename_115_entries`（`webapi.115.com/files/batch_rename` 一次传多个 `files_new_name[id]=名字`，单条 `rename_115_entry` 改为其包装）；执行器重构为 `_execute_scraper_job_batch_forward`——原地改名（文件夹/同目录文件）按父目录分组合并（每 100 条一次请求）、纯移动按目标目录合并 `files/move`、移动+改名保持"临时改名→移动→改最终名"三步安全模式但每步按阶段批量提交；目标目录解析按路径去重、冲突预检按目标目录共享缓存去重；失败时尽力恢复临时名；每个动作仍独立记录状态/监控事件/回滚字段。新增 5 项回归（多条目一次提交、单条包装、原地改名合并、移动按目标分组、三阶段），更新 8 处既有测试 mock 为批量入口；完整 unittest 575 项、compileall、`git diff --check` 均通过。Docker 未重建，真实 115 大批量整理请求数/风控实测待补。
- 2026-08-19 | `main` 未提交 | 刮削页搜索框 UX 修复：点击“应用/清除”或按回车执行搜索后不再自动关闭搜索弹层，改为保持展开并聚焦输入框，可直接连续修改关键词，按 Esc 或再点搜索图标才会关闭。新增 `openSearchPopover`，更新 1 项前端回归测试断言；完整 unittest 570 项、改动 JS `node --check`、`git diff --check` 均通过。Docker 未重建，浏览器交互实测待补。
- 2026-08-19 | `main` 未提交 | 修复刮削重命名后文件被放到错误层级（致命）：根因是文件动作的目标目录锚点错位——前端在祖先目录（如根）选中库文件夹被拆分为二级条目时，`base_cid`（当前浏览目录=根）与后端兜底改写后的 `base_path`（所选文件夹的父目录=一级）不一致，执行层用 `base_cid` + 相对父路径 `新片名` 建目录，把内容建到了根目录、与一级同级。修复：文件夹模式下文件目标锚定在“所选文件夹的父目录”（新增 `_scraper_folder_organize_root`/`_scraper_file_folder_anchor`），且文件动作的 `target_parent_path` 改为完整挂载路径（`dirname(target_path)`），计划期 `_walk_existing_folder` 与执行期 `_ensure_folder_from_base` 统一从挂载根（`0`）用完整路径解析，两个锚点不再打架。新增 4 项回归（二级文件夹重命名留在父目录内、一级文件夹留在根下、深层拆分场景文件目标正确、执行层从根建完整父路径），更新 1 项既有 `target_parent_path` 断言为完整路径；完整 unittest 570 项、compileall、`git diff --check` 均通过。Docker 未重建，真实 115 拆分/深层整理实测待补。
- 2026-08-18 | `main` 未提交 | 115 大目录读取分页重构 + 官方搜索：`list_115_entries_payload` 从“单页 300、整包读取”改为按服务端 offset 逐页拉取（默认每页 500、完整模式最多 80 页），每页对 `IncompleteRead`/断连/超时做指数退避重试（HTTP 状态错误如 405 不重试）；返回真实 `count/next_offset/has_more/entries_complete`，完整模式合并后仍按“文件夹在前、名称排序”供内部按名/按 id 查找，`limit>0` 走分页模式，`folders_only` 分页会连续扫页集齐文件夹避免漏页。新增官方搜索 `search_115_entries`（`webapi.115.com/files/search`），刮削页搜索框改为服务端搜索（失败自动回退分页列表+本地过滤）。resource/scraper 接口透传 offset/limit 并返回分页元数据；刮削页新增“加载更多条目”，资源导入/订阅/监控文件夹选择器新增“加载更多文件夹”。新增 11 项后端分页/重试/搜索测试、5 项前端分页测试，并更新 1 项搜索行为测试；完整 unittest 566 项、compileall、改动 JS `node --check` 与 `git diff --check` 均通过。Docker 未重建，真实 115 大目录读取/服务端搜索实测待补。
- 2026-08-18 | `main` 已推送 | 发布 `0.8.1`：同步 version.json、CHANGELOG、README 与交接状态，收录磁力/电驴离线任务官方完成检测驱动刷新与进度展示、前端显示修复（推荐页回顶、刮削页手机端操作栏单行、日志按钮日间配色、目录树任务卡片移动端布局与图标化按钮）、订阅任务扫描链接弹窗一键粘贴按钮、文件浏览器统一组件补齐修改时间（资源导入分享目录选择器新增时间列 + 后端 compact 透传 `modified_at` 等字段，115/夸克可显示，目标目录与订阅分享目录时间列不再为 `--`）。新增 4 项 compact 回归，完整 unittest 550 项、compileall、改动 JS `node --check`、git diff --check 均通过；时间列与粘贴按钮经本地真实浏览器渲染实测（日夜主题）；Docker 未重建。下一步：Docker 重建后实测容器内离线任务/时间列/粘贴按钮表现。
- 2026-08-18 | `main` 未提交 | 目录树任务 4 个操作按钮图标化：生成并同步=单箭头环形刷新（绿）、全量重写=双箭头环形刷新（红，形状+颜色双重区分强制）、编辑=铅笔（中性灰）、删除=垃圾桶（红）；38×38 方形图标按钮（SVG 18px），复用订阅任务卡片的交互（`title` 悬浮提示 + `aria-label`，运行中禁用保持），按钮基类补齐后夜间/日间配色沿用既有 tone 类。新增 2 项前端回归（图标按钮结构与提示、图标尺寸 CSS），完整 unittest 546 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；360/1280px 与日夜主题本地真实浏览器渲染实测（4 按钮 38×38、提示正确、run 绿/full 红/edit 灰/delete 红）。Docker 未重建。
- 2026-08-18 | `main` 未提交 | 修复目录树任务卡片手机端布局：任务卡片外层原是 `flex flex-wrap items-start justify-between`，信息区 `flex-1` 但按钮区 `shrink-0`，手机端按钮不换行导致信息区被压缩成窄条、卡片被顶高；改为订阅任务同款 `flex flex-col lg:flex-row lg:items-start lg:justify-between`，手机端信息全宽、按钮独立一行，≥1024px 恢复左右并排。新增 1 项前端回归（卡片布局类），完整 unittest 544 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；360/1280px 本地真实浏览器渲染实测（360px 信息区 326px 全宽、按钮独立行、卡片 157px 高；1280px 恢复并排）。Docker 未重建。
- 2026-08-18 | `main` 未提交 | 修复监控/订阅/任务三个日志头部按钮在日间模式变成“纯文本”：根因是日间主题全局规则 `html.theme-day .bg-slate-800` 会把按钮底色拍平成卡片表面色，而这三个按钮原本只依赖 `bg-slate-800/text-slate-300` 工具类且没有边框。现统一改用 `.log-header-btn` 类（夜间深灰底、日间浅灰底 + 描边，各配 hover），模板同时去掉会被拍扁的工具类。新增 2 项前端回归（模板类名、日夜两套配色），完整 unittest 543 项、compileall 与 git diff --check 均通过；日夜主题本地真实浏览器渲染实测按钮均有可见底色；Docker 未重建。
- 2026-08-18 | `main` 未提交 | 刮削页手机端操作栏 7 个按钮一行显示：区间选择/扫描监控/批量整理 在 ≤760px 切换为短文案（区间/扫描/整理），选择区网格 6 列改 7 列，按钮内边距 0.12rem、字号 0.66rem，360/375px 本地静态渲染实测单行不折行；桌面端保持完整文案（按钮内双 span，CSS 按断点切换）。新增 3 项前端回归（7 列网格与几何参数、双标签切换、批量整理双标签结构），完整 unittest 541 项、compileall 与 git diff --check 均通过；Docker 未重建，真实页面 320px 老机型复核待补（如需覆盖可把“重命名”手机端显示为“改名”）。
- 2026-08-18 | `main` 未提交 | 修复资源推荐页翻页后停留在页面中间：翻页点击后 `renderLoadingGrid` 先把网格替换为 12 张骨架卡，页面高度骤减，浏览器把滚动位置钳制到页面中部，新一页内容渲染后仍停留中间；`goToRecommendationPage` 现于翻页分发前显式 `window.scrollTo({top:0, behavior:'smooth'})` 回顶。新增 2 项前端回归（回顶源码顺序 + vm 实测翻页触发回顶），完整 unittest 540 项、compileall、改动 JS `node --check` 与 `git diff --check` 均通过；Docker 未重建，浏览器实测待补。
- 2026-08-02 | `d8c455c` | 资源卡片、频道摘要和频道管理统一使用 18 类展示标签；光鸭保持独立展示类型，但操作仍按普通直链处理。
- 2026-08-02 | `ed43c53` | 手机竖屏资源卡片操作区改为底部四按钮单排；设置页明确磁力和电驴链接仅支持 115 网盘。
- 2026-08-02 | 未提交 | 发布元数据更新到 `0.5.2`；静态与自动化验证完成，等待 Docker 重建后的真实页面复核。
- 2026-08-02 | 未提交 | 移除资源中心频道分组标题上的链接类型汇总标签，保留单个资源卡片类型标记；容器重建后实际页面确认频道标题类型标签为 0、卡片仍显示类型标签，123 项 unittest、JS 语法和编译检查通过。
- 2026-08-02 | 未提交 | 订阅任务网盘标签接入全局配色，并在标题栏展示最近成功命中的相对日期、详情展示精确时间；定向 20 项与完整 128 项 unittest、JS 语法、Python 编译、差异检查及 Docker 日夜/手机页面复核均已通过。
- 2026-08-02 | `main` | 发布元数据升至 `0.5.3`，收录订阅任务标签与最近命中日期功能；验证版本同步、测试、编译和差异检查后提交。
- 2026-08-03 | `main` | 刮削标准命名重复识别优化：完整路径无变化条目不再加入改名计划或查询目标目录，执行层保留同名跨目录移动行为；新增 5 项回归测试，完整 133 项 unittest、项目 compileall、刮削 JS 语法和 git diff 检查通过。下一步：如需真实网盘/页面验证，按项目要求重建 Docker 后复核。
- 2026-08-04 | `main` 未提交 | 电驴外链仅识别 `telegra.ph` 页面并在后端限制同域跳转；直接 ED2K 保持导入，普通 HTTP(S) 与光鸭仅展示且禁用下载，既有五个网盘分享转存不变。新增前后端回归测试，完整 138 项 unittest、Python 编译、3 个资源 JS 语法检查和差异检查通过。下一步：启动 Docker daemon 后执行 `docker compose up -d --build`，复核资源卡与 Telegraph 导入页面。
- 2026-08-04 | `main` 未提交 | 发布元数据更新至 `0.5.5`，同步版本说明、变更日志、README 与当前交接状态。下一步：启动 Docker daemon 后执行 `docker compose up -d --build`，复核资源卡与 Telegraph 导入页面。
- 2026-08-04 | `main` 未提交 | 导入任务列表改为 20 条累计窗口与统一请求序号控制，修复加载更多、轮询缩页和过期响应覆盖；新增已完成/失败单条记录删除并在最后关联任务删除后恢复资源状态。新增 14 项前后端回归测试，完整 152 项 unittest、Python 编译、8 个改动 JS 语法检查和差异检查通过；Docker daemon 未启动，容器重建及任务中心日夜模式实测待补。
- 2026-08-04 | `main` 未提交 | 修复频道同步漏掉最新资源：`fetch_telegram_channel_post_samples` 现按发布时间/消息号倒序收集，避免第一页资源超过目标数时把最旧的一批入库；资源任务弹窗头部仅保留“任务列表”标题。新增 2 项取样回归测试，完整 154 项 unittest、Python 编译和差异检查通过；真实 TG 频道实测返回最新帖。下一步：启动 Docker daemon 后重建容器，在资源页对活跃频道复核“最近资源”时间。
- 2026-08-04 | `main` 未提交 | 发布元数据更新至 `0.5.6`：收录频道同步最新资源修复与任务弹窗标题去重，同步 version.json、CHANGELOG、README 与交接状态。完整 154 项 unittest、compileall 与差异检查通过。下一步：启动 Docker daemon 后重建容器，复核资源页活跃频道“最近资源”时间与任务弹窗标题。
- 2026-08-04 | `main` 未提交 | 修复任务中心“删除记录”按钮在浅色主题下不可见：该按钮的 `bg-red-600` 工具类未包含在旧 Tailwind 生成文件中，重新生成 `tailwind.generated.css` 后已补上；仅静态样式文件改动，git diff 检查通过。下一步：启动 Docker daemon 后重建容器，在任务中心日夜模式复核删除按钮。
- 2026-08-04 | `main` 未提交 | 发布元数据更新至 `0.5.7`：收录任务中心删除按钮可见性修复，同步 version.json、CHANGELOG、README 与交接状态。下一步：启动 Docker daemon 后重建容器，在任务中心日夜模式复核删除按钮。
- 2026-08-07 | `main` 未提交 | 修复频道同帖多条直连 ED2K 仅保存第一条：保存弹窗按频道原帖链接顺序收集、校验并去重全部 ED2K 文件，默认全选，Telegraph 解析与批量提交接口不变。新增频道多链接和前端收集回归，完整 158 项 unittest、Python compileall、相关 JS 语法检查与 git diff 检查通过。下一步：重建 Docker 后在真实频道资源中确认多文件勾选与保存。
- 2026-08-07 | `main` 未提交 | 频道同帖资源改为保存结构化 `extra.resource_links`，兼容旧 `all_links`；卡片按全部链接显示标签，复制可选全部已识别链接，下载/转存只接受已启用且可导入的候选，保持单链接、ED2K 批量和原有导入接口语义。补充混合网盘、磁力、ED2K、展示型直链、旧缓存、二级链接筛选及锚点原帖顺序回归；完整 164 项 unittest、Python compileall、3 个资源 JS 语法和 git diff 检查通过。Docker 已从当前源码重建，真实多链接卡片的复制选择、夸克单候选导入及日/夜主题流程均已复核，控制台无相关错误。下一步：待用户决定是否提交。
- 2026-08-07 | `main` 未提交 | 发布元数据更新至 `0.5.9`，同步 `version.json`、`CHANGELOG.md`、README 与当前交接状态，收录频道同帖多链接兼容修复。下一步：待用户决定是否提交。

## 记录规则

- 仅在完成重要功能、重要修复，或需要其他 agent 接手时追加记录。
- 每条写明日期、分支或提交、完成内容、验证结果、卡点和唯一明确的下一步，避免记录调试过程。
- 涉及 CLI 可操作能力（刮削整理选项、监控任务配置、接口变更等）的改动，需同步补齐 `cli.py` 命令支持与 README CLI 文档，并在记录中注明。
- 已完成、被后续决策取代或不再影响当前工作的记录移入归档。

- 2026-08-09 | `main` 未提交 | 修复资源中心频道同步完成后的首页刷新：compact 轮询发现完成状态时，在搜索框为空的情况下触发一次完整资源状态刷新；有搜索词时保留当前搜索结果。新增 3 项前端回归测试，完整 167 项 unittest、Python compileall、资源 JS 语法检查和 git diff 检查通过。Docker daemon 未启动，容器重建与真实页面复核待补。下一步：启动 Docker daemon 后按 `docker compose up -d --build` 复核空搜索同步刷新和带搜索词保留结果。
- 2026-08-09 | `main` | 发布版本更新至 `0.5.10`，同步 version.json、CHANGELOG、README 与交接记录，收录资源中心同步完成刷新修复和 3 项回归测试。完整 167 项 unittest、compileall、资源 JS 语法检查和差异检查已通过；Docker daemon 未启动。下一步：启动 Docker daemon 后重建容器复核两条同步刷新路径。
- 2026-08-09 | `main` 未提交 | 精准 STRM 同步根因修复：批量刮削的 `old_path/new_path` 统一为 115 挂载点下完整路径，旧待执行任务依据 `base_path` 确定性兼容；处理器先校验删除/新增/替换计划，路径不完整或父路径与父目录 ID 矛盾时保留旧 STRM。文件级操作不清理空目录，已知文件夹复用索引，未知文件夹只同步已知部分并标记 `manual_required`，手动监控成功覆盖后清除提示。新增真实三文件批量改名回归和矛盾父目录保护；定向 94 项通过。下一步：重新运行完整 unittest、compileall、改动 JS 语法和 diff 检查；Docker 可用时重建后再做明确的小目录真实 115 验证。
- 2026-08-10 | `main` 发布 | 精准 STRM 同步安全收口：连续 move/rename 合并不再依赖 `source_action`，跨目录链同步回溯父 CID，只有从未处理的 forward/rollback 回到原路径时才收敛为空操作；处理中断恢复的事件标记本地状态不确定并按顺序重放。所有文件夹索引逐项做旧根/新根/任务范围校验，跨任务清单按文件大小过滤范围判断完整性；共享 STRM 不允许不同远端 URL 静默覆盖，事件级 journal 在本地写入或数据库提交失败时恢复旧文件；启动恢复利用经过校验的 create/copy 目标 CID，未确认 delete 遇同 ID 改名时替换旧索引，脏文件夹改名后保留“需手动监控”。`0.5.12` 元数据已同步；定向 108 项、完整 253 项、compileall、两个改动 JS 的语法检查和 diff 检查均通过；Docker daemon 未运行，未执行容器重建或真实 115 验证。下一步：启动 Docker 后在明确的小目录执行三文件改名实测。
- 2026-08-10 | `main` 未提交 | 精准 STRM 同步日志补充实际本地删除/生成明细，批量文件按事件顺序相邻输出，文件夹以单行摘要显示；版本升至 `0.5.13`。5 项定向测试、compileall、JSON 解析和差异检查通过；未运行完整测试，Docker daemon 未运行。下一步：用户确认后再决定是否提交或推送。
- 2026-08-10 | `main` 未提交 | 修复批量刮削父目录与子文件同批改名导致“移动父目录路径与目录 ID 不一致”：执行与回退都按已成功目录变换计算当前路径，事件按父目录 ID 记录为 rename/move，文件夹清单投影同批已确认子项；增加处理器修订号，使 #184 这类旧处理器达到上限的失败事件在重建启动后自动重排队一次，并按同批已完成父目录事件恢复旧路径。89 项精准同步定向测试、完整 258 项 unittest、compileall 和 diff 检查通过；Docker daemon 未运行。下一步：启动 Docker 后按项目代理配置重建，在该电影目录确认 #184 自动完成且只保留最终 STRM。
- 2026-08-11 | `main` 未提交 | 发布元数据更新至 `0.5.14`，同步 `version.json`、`CHANGELOG.md`、README 与当前交接状态，收录批量刮削父目录/子文件同批改名修复和旧事件单次升级重试。89 项精准同步定向测试、完整 258 项 unittest、compileall、版本 JSON 与差异检查通过；Docker daemon 未运行。下一步：启动 Docker 后按项目代理配置重建，在该电影目录确认 #184 自动完成且只保留最终 STRM。
- 2026-08-11 | `main` 未提交 | 精准同步边界改为路径-only：网盘执行层继续使用 ID，刮削确认成功后传给文件夹监控的事件快照只保留文件名、旧路径和新路径；本地 rename/move 不再校验父目录 ID。刮削本地 STRM 同步失败会恢复 journal、写日志后删除事件，不累计重试；普通监控事件仍按原策略重试，启动时清理历史同类失败事件。新增/调整路径冲突、无 ID 移动、路径-only 快照、一次性失败日志与历史清理回归。精准同步 92 项、目录监控 24 项、完整 unittest 261 项、compileall、git diff --check 均通过；Docker daemon 未运行，未执行容器重建或真实 115 验证。下一步：Docker 可用时按项目代理配置重建，在明确的小目录验证刮削重命名后的 STRM 路径和失败日志。
- 2026-08-11 | `main` 发布 | 修复目录树 MD5 跳过顺序：所有源先完成下载和哈希，只有存在变化时才初始化数据库；全部未变化时不回放路径缓存、不解析目录树、不检查或写入 STRM，部分变化时仍回放未变化源缓存并只解析变化源。版本更新至 `0.5.16`，同步 version.json、CHANGELOG、README 与交接记录；目录树定向 6 项、完整 unittest 263 项、compileall 与 git diff --check 通过。未执行 Docker 重建或真实 115 目录树验证。下一步：Docker 可用时用同一目录树连续执行两次，确认第二次下载哈希后直接结束。
- 2026-08-12 | `main` 发布 | 修复刮削绑定后标准剧集文件“无法识别集数”：自动集数解析改为文件或父路径带明确季号时优先使用该季号，完全无季号时才用“未识别时默认季号”兜底，与手动集数覆盖路径一致；不再因页面默认季号为 1 而把 `S03E07` 这类文件按 season_mismatch 丢弃。版本更新至 `0.5.17`，同步 version.json、CHANGELOG、README 与交接记录；新增 5 项回归测试（含 en-dash 文件名、父目录季号、无季号兜底、多季连续编号映射），完整 unittest 268 项、compileall 与 git diff --check 均通过。下一步：Docker 可用时重建后，在刮削页绑定多季剧集并用 `S03E07` 标准文件名实测生成预览。
- 2026-08-12 | `main` 未提交 | 刮削页新增“批量整理”：先勾选条目再点“批量整理”，只对勾选的文件夹/文件识别（文件夹=一个条目并递归收集媒体文件，散文件=单条目；同批去重并剔除已含在所选文件夹内的条目），不再整目录自动扫描；批量自动识别（关键词清洗 + TMDB 多候选打分，≥80 自动匹配、≥55 建议、其余待人工搜索绑定），确认后整批生成合并预览并复用任务中心执行/回滚；批量预览内手动补集数会联动重算整批计划；合并计划带跨条目目标路径重复检测，监控目录内的整理变更继续走路径-only 精准 STRM 同步（端到端测试验证无远端列目录）。同时把“影视”等通用词加入刮削关键词噪声表，避免父目录名压过真实片名；批量整理界面补全日间模式配色（置信度徽标、类型标签等），日/夜两套主题经无头浏览器逐层背景合成对比度审计，徽标与文字对比度均 ≥6.4。新增 8 项批量整理回归测试，完整 unittest 276 项、compileall、改动 JS `node --check`、模板结构校验与 git diff --check 均通过；未执行 Docker 重建或真实 115 验证。下一步：Docker 可用时重建，在真实小目录勾选文件夹/散文件执行批量整理，确认只识别勾选项、待确认改绑、执行后监控目录 STRM 更新。
- 2026-08-12 | `main` 未提交 | 修复批量识别选错片名的两个根因：`_strip_extension` 把发布组括号尾 `[YTS.GG - YTS.BZ]` 的 `.BZ]` 误当扩展名截断，改为只裁剪纯字母数字且 1-5 位的真实扩展名；批量主查询不再用多来源加权（父目录“电影小库”曾靠 26+25+10 分压过文件夹名），改为“条目自身名称清洗标题优先，父目录名仅作噪声过滤，文件内标题兜底”，并把“电影小库/影视库/电影大全”等容器词加入通用噪声表。新增 3 项回归（YTS 尾括号不截断、英文文件夹不再被父库名抢占、通用文件夹名回退到文件标题），完整 unittest 279 项、compileall 与 git diff --check 通过。下一步：与批量整理一起在 Docker 重建后真实验证。
- 2026-08-12 | `main` 未提交 | 修复批量整理面板“改绑/接受/搜索/绑定”按钮点击无响应：`handleClick` 的 `if (!actionButton) return;` 守卫在批量按钮分发之前，导致没有 `data-scraper-action` 的批量按钮被提前吞掉；已把批量按钮处理移到守卫之前，并新增源码顺序回归测试防止回退。完整 unittest 280 项、改动 JS `node --check`、compileall 与 git diff --check 通过。
- 2026-08-12 | `main` 未提交 | 批量识别改为按发布名结构解析而非词表逐条修补：`_extract_scraper_title_candidates` 用媒体标签/年份/季集等“技术标记”做标题边界（标记前是标题，`S01E01` 开头时取标记后），先剥离站点前缀与方括号组前缀，`www.UIndex.org - Secrets to Kill For 2025 NORDiC 1080p WEB-DL DDP5.1 H.264-ADDICTION`、`Movie.Name.2024.1080p.BluRay.x264-GROUP` 等统一提取为纯标题；主查询无结果时按降级查询重试，但降级命中一律扣 25 分/级，只进“建议”不会自动选片。同时保留尾部发布组剥离与 `5 1` 声道兼容。新增 6 项结构解析/降级安全回归，完整 unittest 284 项、compileall 与 git diff --check 通过。
- 2026-08-12 | `main` 未提交 | 识别核心升级为“guessit 结构化解析 + 候选打分重做”：`_extract_scraper_title_candidates` 改为 guessit 主解析、手写解析兜底合并（requirements.txt 新增 `guessit>=3.8,<4`）。guessit 自动取路径最后一段（修复“电影小库/Governor (2026)…”父目录被并进标题）、识别 `1x03` 季集结构并参与剧集判定、剥离站点前缀；多部曲拼回（`Dune Part 2/Two`）、中英混排拆分候选（`John Wick Chapter 4`/`疾速追杀`）。打分重做：年份为硬约束（冲突重罚，且顶部候选年份冲突时禁止自动选片）、标题/别名/译名精确命中大权重（得分接近时补拉 TMDB 详情破平局）、popularity 仅做同分 tie-break，降级查询仍只建议。新增 5 项回归（路径条目取末段、`1x03` 剧集判定、多部曲/混排候选、Robocop 2014 不配 1987、别名破平局），完整 unittest 289 项、compileall 与 git diff --check 通过；Docker daemon 未运行，容器重建与真实 115 识别验证待补。
- 2026-08-12 | `main` 未提交 | 批量整理按文件类型分类处理非影视文件：视频正常改名；字幕保留语言/编码标记并自动加序号保证唯一（`Denmark.dan.srt` → `片名 (2026).dan.srt`，多条同语言自动追加 ` (2)`），TV 字幕带集数与语言后缀；海报跟随片名，广告图（`YTS.GG - Official site`、www 域名、logo 等）与标准封面（`poster.jpg`/`folder.jpg`）保留原名；广告/说明文件（txt/url/html/nfo 等）不改名不删除，仅统计并在预览提醒。新增 7 项回归（文件分类、字幕语言后缀、多字幕唯一命名、TV 字幕带集数、广告/海报保留、批量忽略统计），完整 unittest 296 项、compileall 与 git diff --check 通过；Docker 未重建、真实 115 验证待补。
- 2026-08-12 | `main` 未提交 | 手动识别与批量识别合并为单一“识别整理”入口：工具栏只保留一个按钮，点击后统一走“自动识别优先”（勾选 1..N 条都进批量面板，单条即一行）；每行保留“改绑/搜索绑定”手动入口，点击展开行内识别卡，复用大爆炸选词（`ScraperPathSelection` token 选词 → 填入关键词 → TMDB 搜索 → 绑定），选中/清空/完成选词/重新选词均可，搜索函数统一为 `fetchTmdbSearch`（手动与批量共用）。旧的 `/scraper/identify`、`/scraper/rename-plan` 与手动面板代码保留兼容、不再从菜单暴露。新增 3 项回归（工具栏单入口、handleClick 合并分发、行内选词与共享搜索），完整 unittest 299 项、改动 JS `node --check`、compileall 与 git diff --check 均通过；Docker 未重建，浏览器交互实测待补。
- 2026-08-12 | `main` 未提交 | 修复剧集集数解析两个根因：(1) `RESOURCE_EPISODE_CODE_REGEX` 的 `(?<!\d)` 会把 `Jade.2026` 结尾的字母 `e` 误当 `E 2026`（`Movie.2012` 同理），改为 `(?<![A-Za-z0-9])` 词边界，`Pursuit.of.Jade.2026.EP02...` 正确解析为 S01E02，EP02/EP03 不再撞成同目标；(2) 纯数字序号文件（`01.mkv`/`02.mkv`）此前能解析集数但 `_looks_like_tv` 判成电影不走剧集流程，新增“文件夹内 ≥2 个纯数字序号文件按剧集处理”的判定，年份（1900-2099）不参与计数避免年份电影目录误判。新增 3 项回归（`e` 结尾单词不误判集数、EP02 无年份抢占、纯数字剧集判定），完整 unittest 302 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 剧集目录实测待补。
- 2026-08-12 | `main` 未提交 | 修复合并入口后丢失命名开关与“整理后旧文件夹残留”两类问题：(1) 把“命名选项”（保留清晰度/片源/编码、标题语言、默认季号、季集识别方式、写入 TMDB ID、Season 子文件夹、同步重命名文件夹）从旧手动面板搬进批量面板，`renderBatch` 新增 `syncBatchOptionControls` 按条目的文件夹/剧集属性控制开关显隐与可用性；(2) 文件夹条目不勾选“同步重命名文件夹”时，文件改为整理在源文件夹内部（`organize_inside_source_folder`），不再创建兄弟级新文件夹、不再留下空的旧文件夹；勾选时行为不变（文件夹改名 + Season 01 + 文件移入）。端到端（文件夹改名 + Season 01 + STRM 同步）验证旧 STRM/索引全部清理。新增 4 项回归（批量面板含命名选项、renderBatch 同步开关、文件夹重命名开/关两种计划路径），完整 unittest 306 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 实测待补。
- 2026-08-13 | `main` 未提交 | 支持“数字 + 单集标题”剧集命名（`1. The Hedge Knight.mkv`、`01 - Title.mkv`、`02_Title.mkv`）：`_extract_numeric_episode_from_filename` 新增“1-3 位数字 + 分隔符（`.`/`-`/`_`/括号/全角冒号/集话）+ 标题文字”模式，纯数字与“数字+质量后缀”两条旧路径也统一排除 1900-2099 年份（`2012.1080p.mkv`、`2012.mkv` 不再误判为集数），同时保持 `12 Monkeys`（空格无分隔符）不误判。文件夹名里的 `S01` 季号识别此前已支持，`A.Knight.of.the.Seven.Kingdoms.S01.../1. The Hedge Knight.mkv` 现可正确得到 S01E01。新增 2 项回归（数字+标题集数提取、整条自动集数解析），完整 unittest 308 项、compileall 与 git diff --check 均通过；Docker 未重建，真实 115 剧集目录实测待补。
- 2026-08-13 | `main` 未提交 | 移除工具栏“退出识别”按钮，预览态只保留“退出预览”：合并入口后“退出识别”（清识别状态+清预览）与“退出预览”（清预览）在预览态同时出现且行为等价，属于重复按钮；旧手动面板头部的关闭图标与 `clear-identify` 动作保留兼容。新增 2 项回归（模板仅剩面板头部关闭图标、renderSelection 不再管理工具栏退出识别按钮），完整 unittest 309 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 内置常见中文发布站点词条并在标题清洗中过滤（不依赖具体网址）：新增 `SCRAPER_CN_AD_SITE_PHRASES`（高清剧集网/电影天堂/免费影视网等）+ 结构性规则“XX网/站/论坛/吧/组 + 发布/首发/分享/出品/压制/制作/整理”，同一短语后面跟任何 www 域名都能命中；在前缀剥离、任意位置清洗、纯广告名噪声过滤、尾部 token 剥离四个环节生效，`高清剧集网发布 剧名.S01E01.mkv` 现只识别 `剧名`，`蜘蛛侠纵横宇宙` 等正常片名不受影响。新增 2 项回归（广告短语前后缀/括号形态清洗、纯广告名噪声过滤），完整 unittest 311 项、compileall 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 修复批量行内选词与多文件夹开关两个问题：(1) 批量行内“大爆炸选词”恢复滑动/拖拽区间选择——新增 `beginBatchPathSelection`/`continueBatchPathSelection`/`syncBatchPathTokenDom`，pointerdown/move/up 手势与手动面板一致，拖动过程只更新 token 高亮、松开后刷新查询词，点击单个词块同样可用；(2) 多文件夹选择时“同步重命名文件夹/写入 TMDB ID/Season 子文件夹”开关不再被旧手动面板同步逻辑禁用——`syncIncludeTmdbIdControl`/`syncSeasonSubfolderControl`/`syncFolderRenameControl`/`syncSeasonControl`/`syncEpisodeModeControl` 在批量面板打开时跳过，改由 `syncBatchOptionControls` 按批量条目统一控制。新增 4 项回归（手势函数与 pointer 事件接线、旧同步函数批量面板保护、多文件夹 tmdbid 重命名目标、handleClick 不再点击切换词块），完整 unittest 314 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，浏览器滑动选词实测待补。
- 2026-08-13 | `main` 未提交 | 预览行配色区分文件夹：可执行（准备就绪）的文件夹行新增 `is-ready-dir` 类，底色由绿色改为淡蓝色（夜间 `rgba(14,116,144,0.16)`、日间 `#eff6ff`），文件保持绿色，批量预览多选时一眼可分辨文件夹；文件夹冲突行仍走琥珀色“需处理”。新增 2 项回归（预览行文件夹类名、日夜两套配色存在），完整 unittest 316 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 预览态收敛工具栏：生成预览后隐藏区间选择/重命名/复制/移动/删除/识别整理等文件操作按钮（`renderSelection` 按 `hasPlan` 切换 `hidden`），预览界面只保留整理相关操作；“退出预览”按钮补充 `scraper-danger-soft` 红色危险底色。新增 1 项回归（预览隐藏文件操作按钮 + 退出预览红色样式），完整 unittest 317 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 刮削管理页 URL 记忆当前位置：只编码网盘类型与文件夹路径（`#tab=scraper&provider=115&path=...`，不含 cid 长数字），切换网盘/进入目录/返回上级时用 `history.replaceState` 同步（不产生历史记录、不触发路由循环）；刷新页面时 `restoreScraperLocation` 按路径逐段走回原目录，某段失效则停留在最近可访问位置。新增 1 项回归（URL 编解码函数、导航同步、初始化恢复接线、不含 cid 参数），完整 unittest 318 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，浏览器刷新实测待补。
- 2026-08-13 | `main` 未提交 | 预览态恢复“返回绑定界面”能力：新增“修改绑定”按钮（仅预览且存在批量识别上下文时显示），点击 `reopenBatchPanel` 直接重新打开批量识别面板且保留已识别条目与绑定（不重新扫描），用户可改绑/重新选词搜索后再次生成预览；无批量上下文时回退为重新发起识别。新增 1 项回归（按钮存在、handleClick 映射、保留上下文不重置），完整 unittest 319 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 绑定界面高亮与等待动效：(1) 已绑定条目统一绿色高亮——自动匹配与手动绑定/采纳建议都显示绿色徽标与行底色（`is-auto` 徽标 + `is-included` 绿底），不再出现“手动绑定后徽标仍是灰色”的割裂感；(2) 点击识别后的等待状态加入动态加载：`scraper-busy-spinner` 旋转动画 + “正在识别中，请不要关闭页面”提示（日夜两套配色），避免用户误以为卡住。新增 1 项回归（徽标统一绿色、busy 分支含 spinner、日夜两套 spinner/绿底样式），完整 unittest 320 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 修复中英混排与字幕标签污染关键词：`男子心如钻[全30集][简繁英字幕].Strong.Will...` 这类名字现在 (1) 清洗掉 `简繁英字幕/中英字幕/特效字幕` 等字幕标签与括号内残留网址（嵌入域名正则要求“标签+分隔符+后缀”结构，不再误伤 `Treme`/`Name`/`NoTV` 这类单词）；(2) 中英混排候选拆成独立关键词（中文优先、英文次之、合并名最后兜底），搜索不再把“男子心如钻 Strong Will”当整条查；(3) 全角括号前缀 `【XX字幕组】` 在 guessit/手写解析前统一剥离。新增 2 项回归（混排拆分+字幕标签清洗、全角括号前缀剥离），完整 unittest 321 项、compileall 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 预览提醒内容可见化：生成预览的 toast 不再只报“含 N 个提醒”，会把前两条提醒原文拼进提示（超长截断）；预览列表顶部新增 `scraper-plan-warning-bar` 提醒条，逐条展示全部提醒内容（日夜两套配色），配合行内提醒更直观。新增 1 项回归（toast 含提醒原文、预览顶部提醒条、日夜两套样式），完整 unittest 322 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 扩展常见附属信息噪声词库：新增 `SCRAPER_COMMON_NOISE_PHRASES`（无字片源/无字幕/无水印/无删减/未删减/加长版/导演剪辑版/剧场版/重制版/修复版/国语版/粤语版/双音轨/多音轨/中英双语/特效中字/提取码/磁力链接/百度网盘/夸克网盘/阿里云盘等 30+ 复合短语），标题清洗任意位置去除、纯噪声名过滤，尾部 token 补充 片源/原盘/全集/完结/连载；词条采用复合短语避免误伤真实片名（`无字天书`/`独家记忆` 不受影响）。同时修复“匹配加载中无动态效果”：批量识别（扫描完成、条目已显示）阶段顶部也显示 `scraper-busy-spinner` 旋转动画与“正在匹配 TMDB 条目，请不要关闭页面”。新增 2 项回归（噪声短语清洗+真实片名保留、有条目时 busy 分支显示 spinner），完整 unittest 324 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 新增“整理时删除广告文件”内置功能（默认关闭，需勾选才生效）：批量选项新增 `scraper-delete-ad-files` 开关；`_is_scraper_ad_file` 把广告扩展名（txt/url/html/nfo/torrent 等）与广告图片（含官网/www 域名文件名）识别为广告文件；开启后 `build_scraper_rename_plan` 为广告文件生成 `delete` 动作（预览行红色“删除”徽标、目标显示“（删除广告文件）”，执行确认弹窗提示删除数量），执行走 `_delete_provider_entries` + 监控删除事件，删除进入网盘回收站、任务回退跳过删除动作；`_load_scraper_job` 把 plan_json 中的动作字段（如 delete）合并回数据库行保证持久化。关闭时行为不变（广告文件仍保留原名不删）。新增 5 项回归（广告文件分类、计划生成删除动作开/关、任务执行删除分支、删除持久化、前端开关/预览/确认文案），完整 unittest 328 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 删除与回收站恢复待测。
- 2026-08-13 | `main` 未提交 | 补充验证广告删除覆盖 `.url/.html` 等扩展名（`Polskie-Torrenty.eu – Darmowe Torrenty — kopia.url`、`wejdź po więcej — kopia.html` 复现均生成删除动作），并新增批量路径下 `delete_ad_files` 选项传递到条目计划的回归测试。完整 unittest 329 项、compileall 与 git diff --check 均通过；Docker 未重建，需重建容器后实测删除。
- 2026-08-13 | `main` 未提交 | 批量识别加载动效去重：删除列表顶部的独立转圈进度条，加载中转圈改为内联在状态栏文字前（`scraper-busy-spinner inline`），成功/失败/加载信息统一只由顶部状态栏呈现。同时扩展附属噪声词：新增 `地址发布页/发布地址/永久地址/备用地址/最新地址/官网地址/防走丢/收藏本站/最新网址/发布页` 等短语，并把独立词 `电影` 加入清洗词表（`斯特林角 电影 地址发布页` 现只识别 `斯特林角`，`电影往事` 等真实片名不受影响）。新增 2 项回归（状态栏内联转圈且无独立进度条、发布页短语与电影词清洗），完整 unittest 330 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 新增“库根拆分扫描”（`split_folders`）：批量整理选项新增“把所选文件夹按子目录拆分扫描”，选中整个影视库时可按路径在识别前拆成多个条目——直接含媒体的子文件夹=一个条目、散落媒体文件=单条目、通用分类容器（电影/电视剧/动漫/资源/合集/美剧等 `SCRAPER_LIBRARY_CONTAINER_KEYS` + 通用词）递归下探（最多 4 层）、其他只有子文件夹的文件夹按作品条目处理（含 Season 子目录）；**分层失败或没有媒体的文件夹跳过不处理**并给出“已跳过”提示。不勾选时保持“整个文件夹当一个条目”的旧行为。新增 3 项回归（库根拆分含分类下探/Season 结构/空夹跳过、不拆分保持单条目、前端开关与扫描参数），完整 unittest 333 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 库目录实测待补。
- 2026-08-13 | `main` 未提交 | 库根拆分改为自动判定并移除手动开关：`_looks_like_scraper_library_root` 自动判断选中文件夹是否像媒体库根（文件夹名是影视库/电影/剧集等容器词 → 直接拆分；否则看子项：含容器子目录、或 ≥2 个非 Season 子文件夹各自含媒体 → 拆分；直接含媒体或只有 Season 子目录 → 按单个作品条目处理），不再需要用户先识别失败再开开关重扫；后端 `split_folders` 参数保留为强制开关，前端不再传。新增/调整 4 项回归（影视库名自动拆分、自定义多作品库自动拆分、单作品/多季剧集保持单条目、前端无手动开关），完整 unittest 335 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 拆分方式改为识别弹窗顶部的两段式控件：默认“自动判断”，两个胶囊按钮“整个文件夹 / 按子目录拆分”可直接切换，切换后自动重新扫描（不再先识别失败再补救）；后端 `scan_scraper_batch_items` 新增 `split_mode`（auto/single/split），`split_folders=True` 兼容等价 split。新增 4 项回归（single 强制单条目、split 强制拆分、控件位于弹窗顶部且扫描参数传递、handleClick 分发在守卫之前），完整 unittest 338 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 新旧名称完全一致且路径无变化的项不再进入任务队列：计划层新增 `unchanged_rows`（文件夹/文件“无变化”行，包含旧名/旧路径/条目归属），预览里以灰色“无变化 / 保持原名，已跳过”行展示、复选框禁用、不可执行；批量计划汇总时带上 `item_name` 区分条目；任务执行层增加防御性快速跳过（`old_path == new_path` 时不做任何远程调用/限速等待，直接标记 skipped）。新增 4 项回归（计划无变化行不入 actions、批量汇总带条目名、预览渲染无变化行、无变化快速跳过），完整 unittest 341 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 散落单集文件按剧集合并识别：`scan_scraper_batch_items` 对选中的散落媒体文件做 `_group_scraper_loose_tv_files` 分组——同目录内 ≥2 个文件提取出相同标题且带集数特征时合并成一个“剧集条目”（识别/绑定只做一次），电影/独立文件保持单条目；合并条目携带 `entries`（全部文件），`build_scraper_batch_plan` 把 `entries` 传给重命名计划，**预览/计划仍按文件逐个生成动作，显示与原来一致**（20 个文件 = 20 行）。新增 2 项回归（20 集散文件合并为 1 条目、合并条目计划展开为每文件动作），完整 unittest 343 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 绑定成功的条目右侧显示封面图：`build_tmdb_task_binding` 补充 `tmdb_poster_url`（手动绑定路径也能拿到封面），批量识别面板已绑定行新增 `scraper-batch-match-wrap` 布局，右侧渲染 `scraper-batch-poster` 缩略封面（54×78，日夜两套配色），自动匹配与手动绑定均生效。新增 2 项回归（绑定保留封面字段、绑定行渲染右侧封面与样式），完整 unittest 345 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 绑定行封面与文本布局优化：封面加大到 66×96 并在行内垂直居中；绑定信息改为上下两行结构（徽标+类型一行，标题单独一行 `scraper-batch-title` 超出省略不换行），消除徽标/标题/类型挤在一行导致的换行撑空行问题。新增断言覆盖 `scraper-batch-title` 结构与样式，完整 unittest 345 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 批量绑定行封面改为整行右列铺满：行网格改为「勾选 / 文本 / 封面」三列，封面 `scraper-batch-poster-col` 随整行高度拉伸（`height:100%` + `aspect-ratio:2/3`，上限 168px），不再只占中间匹配段；左侧文本间距收紧（`scraper-batch-main` gap 0.32rem、`match-info` gap 0.3rem），消除上下空行与文本过散。新增断言覆盖 `scraper-batch-poster-col` 结构，完整 unittest 345 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 批量绑定弹窗（及旧识别弹窗）标题与右上角关闭按钮改为吸顶：`.scraper-batch-dialog .scraper-panel-head`（同 `.scraper-identify-dialog`）`position: sticky; top: 0`，滚动内容时头部始终可见，背景用近不透明的面板色并加底部描边（日夜两套），用户随时可关闭无需回滚顶部。新增 1 项回归（吸顶样式日夜存在），完整 unittest 346 项、compileall 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 修正批量弹窗吸顶露缝问题：弃用 sticky+负 margin 方案，改为「头部固定 + 内容区滚动」结构——`.scraper-batch-dialog` 改 flex column + `overflow:hidden`，头部 `flex:0 0 auto` 固定（带底部描边，日夜配色），新增 `.scraper-batch-dialog-body`（`overflow-y:auto` + `min-height:0`）包裹状态栏/拆分开关/列表/选项/操作，弹窗滚动只在内容区进行，顶部不再露缝。新增 1 项回归（模板 body 包裹、头部固定与内容区滚动样式），完整 unittest 346 项、compileall 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 监控任务新增“新增资源自动刮削整理”开关（`auto_scrape_on_new`，默认关，任务设置界面可勾选）：监控扫描时对比上一轮 `monitor_files` 索引，捕获新增媒体文件（含 115 fid/大小/路径），扫描完成后经 `_auto_scrape_new_media_items` 自动走“扫描→识别→计划→任务”管道，**只对置信度 ≥80 的自动匹配条目执行一次整理**，suggest/manual 跳过并在日志提示；任何失败仅记录日志不重试，不做半成品/防抖等额外处理（按用户要求保持简单）。新增 3 项回归（配置规范化、自动整理只处理高置信度且任务执行、界面开关三处接线），完整 unittest 349 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 新增资源自动整理待测。
- 2026-08-13 | `main` 未提交 | 修复自动整理“新增文件无法解析网盘路径，跳过 N 项”：此前用相对监控任务根目录的 `remote_rel` 直接调用 `_walk_existing_folder`（从挂载根 `/115` 开始找），少了一级任务根目录导致解析失败；改为先 `join_remote_path(scan_path, remote_rel)` 拼完整路径，再经 `resolve_provider_relative_path` 转成挂载根相对路径后按段解析父目录（`/115/自存电视剧/问心 (...) → 自存电视剧/问心 (...)`），条目 path 也统一用挂载根相对路径。新增断言（父目录按挂载根相对路径解析），完整 unittest 349 项、compileall 与 git diff --check 均通过；Docker 未重建，真实 115 新增资源自动整理待测。
- 2026-08-13 | `main` 未提交 | 修复自动整理“新增条目无高置信度自动匹配”：此前自动整理把新增的**单个媒体文件**作为识别条目，标准刮削文件夹（如 `云秀行 (2026) {tmdbid-239901}`）里的英文文件名搜不出中文标题；改为按父文件夹分组，把**父文件夹作为识别条目**传入（文件夹名含标准标题+年份），并解析祖父目录 ID 作为 parent_id，识别输入与手动选择文件夹一致。新增断言（扫描条目为文件夹、父/祖父目录按挂载根相对路径解析），完整 unittest 349 项、compileall 与 git diff --check 均通过；Docker 未重建，真实 115 新增资源自动整理待测。
- 2026-08-13 | `main` 未提交 | 发布元数据更新至 `0.6.0`，同步 version.json、CHANGELOG、README 与当前交接状态，收录本会话全部批量整理、识别核心升级、监控自动整理与交互优化改动（详见 CHANGELOG）。完整 unittest 349 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 端到端验证待补。
- 2026-08-13 | `main` 未提交 | “需手动监控”提示改为可查看、可操作：(1) 修复 `get_manual_required_monitor_scopes` 一直返回空列表的根因——事件表存完整网盘路径（`/115/Media/...`），原逻辑直接 normalize 当挂载相对路径匹配导致上下文解析失败，现先经 `resolve_provider_relative_path` 转成挂载相对路径，并返回 `old_path/new_path/created_at` 明细；(2) 新增 `GET /monitor/manual-required?task_name=` 接口；(3) 监控任务卡片上的“需手动监控 N”改为可点击按钮，弹出明细弹窗（逐条显示路径与事件号），提供“重新扫描”按钮触发 `startMonitorTask` 自动清除。新增 4 项回归（明细字段、接口查询、前端接线、监控同步旧断言更新），完整 unittest 351 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-13 | `main` 未提交 | 修复刮削页 URL 参数污染其它 tab：此前刮削把 `provider/path/cid` 写成全局哈希参数，tab 路由切换会保留这些参数，导致监控页链接也带上了刮削参数。改为按 tab 归属管理——`url-sync.js` 新增 `TAB_OWNED_PARAMS`（scraper 拥有 provider/path/cid），`buildHashWithTab` 切到其它 tab 时删除非本 tab 自有参数；`syncScraperLocationHash` 仅在当前 `tab=scraper` 时写入。新增 1 项行为回归（node 真实调用 buildHashWithTab 验证监控页哈希被清理、刮削页保留参数），完整 unittest 352 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；需 Docker 重建后刷新网页生效。
- 2026-08-13 | `main` 未提交 | “需手动监控”明细升级与容器验证：明细弹窗改为逐条展示事件号、操作类型（`operation` 字段）、旧路径→新路径与处理提示；修复前端任务名双重编码导致明细为空；已在运行中的 `115-media-hub-test` 容器完成重建与演示事件验证（事件 #199，任务“电视剧”）。CHANGELOG/README 已记录该项更新，版本保持 `0.6.0` 不变（未推送）。
- 2026-08-13 | `main` 未提交 | 油猴脚本兼容 iOS（Userscripts）使用：`GM_getValue/GM_setValue` 改为兼容异步 Promise 并把任务/密钥预载入内存，移除同步读取导致的“任务为空、密钥变 `[object Promise]`”；`GM_xmlhttpRequest` 缺失或同步抛错时自动走 `fetch` 兜底（torrent 二进制下载同步补兜底）；http 页面无 WebCrypto 时用纯 JS SHA-256/HMAC 签名兜底（RFC 4231 向量验证）。新增 7 项 node 回归测试，完整 unittest 359 项、compileall、node --check 与 git diff --check 均通过；Docker 未重建，iOS 真机实测待补。下一步：iOS 安装 Userscripts 后真机验证配置任务与磁力推送。
- 2026-08-13 | `main` 未提交 | 油猴脚本配置入口重构：取消全局右下角悬浮窗（避免影响所有网页），任务管理器入口全部收进“115”按钮——未配置任务时点击任意“115”按钮直接打开任务管理器（不再只弹“没有可用任务”），已配置时任务选择器底部新增“任务管理”按钮；桌面端保留油猴菜单入口。新增无任务直开管理器、选择器管理按钮、无浮窗回归测试。下一步：iOS 真机验证配置与推送。
- 2026-08-13 | `main` 未提交 | 修复单任务时“115”按钮直接推送导致无法进任务管理器的漏洞：移除 `chooseTask` 单候选快捷返回，即使只有一个任务也始终弹出任务选择器（底部保留“任务管理”入口），保证 iOS 无油猴菜单也能随时进入配置。新增单任务仍弹选择器且可选中推送的回归测试。下一步：iOS 真机验证配置与推送。
- 2026-08-13 | `main` 未提交 | 刮削标题清洗规则扩展：复合短语新增“国语配音”“中文字幕/中文配音/中文音轨/中文版”；新增 `SCRAPER_STANDALONE_NOISE_WORDS` 独立词表（中文/国语），按词边界清洗并参与纯噪声过滤，避免误伤“我的中文老师”这类含“中文”的真实片名。新增 1 项回归，完整 unittest 362 项、compileall 与 git diff --check 均通过；Docker 未重建。下一步：Docker 重建后实测真实文件名识别。
- 2026-08-13 | `main` 未提交 | 刮削页交互与保留标签升级：(1) 批量整理面板“搜索绑定”结果行复用 `renderPoster` 显示 TMDB 封面；(2) 手机端选择区网格由 5 列改 6 列并移除 360px 的 4 列覆盖，区间选择/重命名/复制/移动/删除/识别整理 6 个主按钮保持一行；(3) 命名选项“目录处理”拆为“文件夹结构”（Season 子文件夹 → 写入 TMDB ID → 同步重命名）与“文件清理”（删除广告文件）；(4) 保留标签新增“语言/字幕”两组（国语/粤语/双语/台配、中字/简中/繁中/英字/双语字幕/内封外挂中字等，整短语优先且词边界安全，不误伤“我的英语老师”类片名），并修复 `AAC.10bit` 把 10bit 误判成 1.0 声道的问题。新增 11 项回归，完整 unittest 375 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。下一步：Docker 重建后浏览器实测批量搜索封面、手机工具栏一行与新标签保留效果。
- 2026-08-13 | `main` 未提交 | 批量整理任务名改为展示 TMDB 绑定信息：`create_scraper_job_from_plan` 对批量任务按“实际执行动作涉及的条目”生成任务名并存入 `tmdb.title`——单条显示“标题 (年份)”（如 云秀行 (2026)），多条显示“首部标题 等 N 项”（N=实际执行条数），无条目信息时维持“批量整理”；刮削页任务记录与任务中心刮削 tab 均自动生效（都读 job.tmdb），旧任务不迁移。新增 5 项回归（单条带年份、多条计数、部分执行按勾选条目、非批量任务保留绑定标题、无条目兜底），完整 unittest 380 项、compileall 与 git diff --check 均通过；Docker 未重建。下一步：Docker 重建后浏览器实测任务名展示。
- 2026-08-13 | `main` | 发布元数据更新至 `0.6.1`，同步 version.json、CHANGELOG、README 与当前交接状态，收录本会话全部刮削交互优化（批量搜索封面、手机端工具栏一行、命名选项分组、语言/字幕保留标签与 10bit 声道修复、批量任务名 TMDB 展示）及 0.6.0 之后的油猴 iOS 兼容改动；完整 unittest 380 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实浏览器/115 端到端验证待补。
- 2026-08-14 | `main` | 发布元数据更新至 `0.6.2`，同步 version.json、CHANGELOG、README 与当前交接状态，收录三项改动：(1) 任务中心手机端操作按钮可收缩换行不再向左溢出，按钮文案精简，禁用配色不变；(2) 搜索/识别提示与结果副标题的长关键词改为中间省略显示，三处文案容器增加 `overflow-wrap: anywhere` 兜底；(3) 任务历史清理从页面请求拆到后台周期任务（`prune_resource_jobs_if_due`，默认每 10 分钟，`RESOURCE_JOB_PRUNE_INTERVAL_SECONDS` 可调），页面请求只做任务状态恢复。新增 2 项回归测试，完整 unittest 382 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实浏览器移动端视口与容器行为验证待补。下一步：重建容器后实测手机端任务按钮一行/换行、长磁力提示条无横向溢出，并观察日志确认后台清理按间隔执行。
- 2026-08-14 | `main` 未提交 | 修复导入任务列表“自动订阅/来源”标签日间配色过浅：任务卡片来源标签此前用 `text-violet-200`（浅紫文字），日间主题下在白色卡片上几乎看不清；改为与状态徽标一致的主题类 `bg-violet-500/15 text-violet-300 border border-violet-500/20`，日间模式自动套用既有 `#6d28d9` 深紫文字覆盖（夜间保持淡紫不变）。新增 1 项回归（标签类名不再含 text-violet-200、日夜覆盖存在），完整 unittest 383 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。
- 2026-08-16 | `main` 未提交 | 刮削页“扫描监控”功能：选择栏新增“扫描监控/刷新 STRM”按钮，勾选文件夹（扫其子树）或文件（映射到父目录）后入队手动监控任务；后端队列 payload 扩展 `savepaths`（去重、上限 50）与 `provider`，`run_monitor_task` 支持多起始目录扫描（`refresh_source_label="指定目录扫描"`，逐起始目录标记首层 dirty，清理与索引替换按范围并集限定，超限合并回退全量），新增 `queue_monitor_dir_scan` 与 `POST /monitor/scan`（跨任务自动分组、未匹配返回明确提示）。新增后端 8 项 + 前端 6 项回归，完整 unittest 451 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建。下一步：Docker 重建后浏览器实测按钮交互与子树扫描。
- 2026-08-16 | `main` 未提交 | 方案一可行性验证（115 最近操作接口）：实测 `life.115.com/api/1.0/web/1.0/life/recent_operations`（按行为分组）与 `webapi.115.com/behavior/detail`（历史流水）可用，事件带 `file_id/parent_id/parent_name/时间/大小`；用户受控操作复测完成——复制（`copy_folder`/type 18）、重命名（`file_rename`/24、`folder_rename`/20）、移动（type 6/5）出现在 `recent_operations`，删除（type=22）与上传（type=2，`SteamSetup.exe` 实测）仅出现在 `behavior/detail`，均经 `get_info` 验证（删除返回“文件不存在或已删除”、上传正常返回且 `cid`=父目录）；`get_info` 返回 `cid/pid` 可直接逐级反查完整路径；接口不支持服务端过滤/分页，`limit` 有效，偶发一次空返回需容错。结论与落地建议（双源轮询 + type 代码表 + get_info 路径反查 + 复用 savepaths 局部刷新通道）写入 `docs/superpowers/specs/2026-08-16-115-recent-operations-feasibility.md`。下一步：与用户确认是否按此方案实现轮询功能。
- 2026-08-14 | `codex/pr5-cli-merge` 已提交（未 push） | 合并外部贡献 PR #5（CLI 扩展）并适配当前主分支至 0.7.0：新增宿主机 `cli.py`（25 个子命令，登录改为 MH_USERNAME/MH_PASSWORD 或交互输入，不再静默用默认口令；修复 search --cancel、subscribe remove→/subscription/delete、monitor start/stop/remove 按名称、scrape jobs-create 三步流）；后端 scraper rename/move/copy/delete 增量支持 path/dest 入参（仅 115，复用分页解析），identify/rename-plan 支持纯路径条目；新增发现源注册表（discovery_base/discovery_registry）与 requirements-cli.txt、README 用法。审查修复共 12 项：logs --tail、resource delete --id、115 分页 id 字段文件判定、browse tree 只递归文件夹、路径解析错误透传、jobs-create 未就绪先列冲突、ID 非法中文提示，以及 subscribe add 保存路径自动推断、schedule/settings/tmdb/watchlist 输入校验、channels sync 文案、容器名自动识别、115 规范化函数统一。新增 5 个测试文件，完整 unittest 436 项、compileall 与 git diff --check 均通过。Docker 已重建（daemon 曾未运行，本次启动 Docker Desktop 后 `docker compose up -d --build`），正式容器 `115-media-hub-test` 已用当前代码重启；另用临时容器（全新默认配置、18081 端口）完成 CLI 冒烟：登录/status/version/search/providers/jobs/sources list/daemon 自动识别/scrape 路径接口错误链路均通过，Cookie 文件 0600，临时容器已清理。真实 115 刮削路径操作仍待有真实 Cookie 的环境验证；正式容器密码为 PBKDF2 哈希，CLI 登录需用户真实密码。
- 2026-08-16 | `main` 未提交 | 方案一（115 事件自动同步）决策：**不采用、不实施轮询服务**，调研转为知识储备。两份调研文档合并为 `docs/superpowers/specs/2026-08-16-115-life-events-knowledge.md`（参考项目调研、life/behavior 双接口实测、type 代码表、用户受控操作复测、若将来实施的设计备忘），删除 `plans/2026-08-16-115-life-event-sync.md` 与旧的 feasibility 文档（内容全部并入新文档）。线上兜底继续用已实现的刮削页“扫描监控/刷新 STRM”按钮。下一步：Docker 重建后浏览器实测方案二按钮交互与子树扫描。
- 2026-08-16 | `main` 未提交 | 批量整理体验优化：(1) 入口按钮统一为“批量整理”（弹窗/任务名已为此名）；(2) 命名选项重排为「文件夹（重命名文件夹/创建 Season 子文件夹/写 TMDB ID）→ 文件命名 → 文件清理（删除广告文件）」；(3) 新增“文件命名方式”三档 `file_name_mode`（standard 按识别结果重命名 / clean 仅清理广告网址与站点名保留原始命名 / keep 保持原名不重命名），keep/clean 下文件不移动（文件夹重命名照常、聚集剧集仍按 Season 子文件夹设置移入、文件名保持/仅清洗）；(4) 选项记忆存服务端并按网盘分开：新增 `scraper_batch_preferences` 表、`GET/POST /scraper/{provider}/batch/preferences`（白名单归一化、空 options 清除记忆）、前端加载/防抖 400ms 保存/“恢复默认”按钮。新增后端 11 项 + 前端 3 项回归并更新 2 项旧断言，完整 unittest 465 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 批量整理实测待补。下一步：Docker 重建后浏览器实测三档命名、选项记忆跨网盘恢复与 Season 子文件夹组合行为。
- 2026-08-16 | `main` 未提交 | 监控任务“新增资源自动刮削整理”支持按任务配置整理选项（方案 A）：弹窗勾选开关后展开“自动整理选项”三段（文件夹：重命名/Season 子文件夹/写 TMDB ID；文件命名：保持原名/仅清理广告/标准重命名 + 标题语言/默认季号/季集识别/保留细节；文件清理：删除广告文件）；后端 `normalize_task` 白名单新增 `auto_scrape_options`（非 dict 忽略），`_auto_scrape_new_media_items` 用任务选项（未配置时保持原默认 `title_language=zh, delete_ad_files=False`）替换写死选项，经 `_normalize_scraper_batch_preferences` 白名单归一化。新增后端 2 项 + 前端断言扩展，完整 unittest 466 项、compileall、改动 JS `node --check` 与 git diff --check 均通过；Docker 未重建，真实 115 自动刮削按任务选项整理待测。下一步：Docker 重建后浏览器实测监控任务选项保存/恢复与自动整理三档行为。
- 2026-08-17 | `main` 未提交 | 补齐 CLI 支持与文档（0.7.1 网页端新能力的命令行入口）：`scrape` 新增 `batch-preferences get|set|clear`（读写/清除某网盘批量整理偏好，`--provider` 指定网盘）；`scrape rename-plan` 新增 `--file-name-mode keep|clean|standard`、`--no-rename-folders`、`--no-season-subfolder`、`--include-tmdb-id`、`--delete-ad-files`、`--title-language`、`--season`、`--episode-mode`、`--preserve-file-info` 与 `--options-json`；`monitor add` 新增 `--auto-scrape-on-new` 与 `--auto-scrape-options-json`。README CLI 段落与 CHANGELOG/version.json 同步更新；handoff「记录规则」新增“涉及 CLI 能力的改动须同步补齐 cli.py 支持与 README CLI 文档”。新增 7 项 CLI 回归（batch-preferences get/set/clear、非法子动作/JSON 干净报错、rename-plan 选项透传、monitor add 自动刮削选项），完整 unittest 473 项、compileall、git diff --check 均通过；Docker 未重建。下一步：用户确认后提交（0.7.1 内容追加），Docker 重建后实测 CLI 命令与页面效果。
- 2026-08-17 | `main` 未提交 | 发布元数据更新至 `0.7.2`，同步 version.json、CHANGELOG、README 与当前交接状态，收录 CLI 补齐（batch-preferences 读写/清除偏好、rename-plan 命名选项、monitor add 自动刮削选项与文档）及交接记录规则更新。完整 unittest 473 项、compileall、git diff --check 均通过；Docker 未重建，CLI 命令与页面效果待容器重建后实测。下一步：Docker 重建后实测 CLI 命令与批量整理/自动刮削页面效果，确认后推送。
- 2026-08-17 | `main` 未提交 | 目录树任务化改造（`docs/superpowers/plans/2026-08-17-115-quick-export-dir-tree.md`）：废弃旧 `trees` 静态树源/定时/MD5 全量模式，改为“目录树任务”模型——每个任务绑定一个 115 文件夹，调用官方 `files/export_dir` 生成树文件（网盘根目录，命名 `目录树-路径段…`），固定执行“导出→删旧（回收站）→原地重命名”，随后用远端文件 sha1 与上次比对，未变化则跳过下载/解析/写 STRM；默认增量，任务卡片提供“全量重写”；清理残留按任务 scope（folder_path 子树）。真实账号校准完成（根级+二级两次导出）：响应字段、UTF-16 首行 `|——根目录/|——父级`、根目录落盘、sha1 可用均确认，自动填充公式锁定为“排除层级=1 + 前缀=父级链（根级为空）”；完整链路实测 982 个媒体文件正确生成 STRM。设置页移除目录树配置，配置项迁移到同步页（sha1 跳过/清理残留全局开关）；CLI `tree list|create|run|full|jobs` 与 README/CLI-API-AUDIT 同步。新增 tests/test_115_export_dir.py、test_tree_tasks.py、test_tree_page_frontend.py，完整 unittest 496 项、compileall、改动 JS `node --check`、git diff --check 均通过。Docker daemon 权限不足未重建。下一步：启动 Docker 后 `docker compose up -d --build` 实测页面按钮/任务流转与 CLI 命令，确认后决定是否提交。
- 2026-08-17 | `main` 未提交 | 修复目录树任务化改造导致的容器重启循环：移除 tree 定时调度时遗漏了 `startup.py` 里 `asyncio.create_task(scheduler())` 对已删除函数的引用，容器启动事件抛 NameError 后退出、Docker 反复重启。已删除该行并用真实配置本地完整跑通 startup 事件（FULL_STARTUP_OK），新增源完整性回归测试，完整 unittest 497 项、compileall、git diff --check 通过。下一步：用户重建镜像验证容器不再重启。
- 2026-08-17 | `main` 未提交 | 目录树任务页布局改为订阅任务风格：页面头部“+ 新增目录树任务 / 全部同步”按钮 + 任务列表卡片；新增/编辑统一走弹窗（文件夹、树文件名、父文件夹路径前缀、排除层级，保存后进列表）；文件选择器复用现有订阅目录浏览（`subscription/folders.js` 增加通用回调参数 `openSubscriptionFolderModal(callback, providerOverride)`，订阅原逻辑不变）；移除页面内联新增表单与自建文件夹选择弹窗。后端新增 `POST /tree/tasks/{id}` 编辑接口（校验文件夹/防同名冲突）。新增前端回归（模板标记 + 弹窗保存 POST 断言），完整 unittest 498 项、compileall、改动 JS `node --check`、git diff --check 均通过。下一步：Docker 重建后实测页面。
- 2026-08-17 | `main` 未提交 | 目录树任务弹窗参数改为纯自动推导：父文件夹路径前缀（父级链，根级为空）与排除层级（固定 1）由所选文件夹按解析规则自动填充，前端只读、后端 create/update 均忽略手动传入值；树文件名仍可编辑；CLI create 移除 --prefix/--exclude。修复任务弹窗 z-50 被目录浏览弹窗（z-56）覆盖的层级问题。新增路由级回归（create/update 忽略手动 prefix/exclude 并重新推导）与前端断言，完整 unittest 501 项、compileall、node --check、git diff --check 均通过。
- 2026-08-17 | `main` 未提交 | 修复目录树生成后“重命名未完成/文件不存在”的根因：115 重命名接口会对不带扩展名的目标名**自动补 .txt**（实测响应 `data: {fid: "目录树-x.txt"}`），原逻辑按不带后缀的名字校验与按名查找，永远失败。现统一用远程实际名 `树名 + .txt`（`_tree_file_remote_name`）做删除/重命名/校验/按名读取；重命名后校验改用 `files/get_info` 按 file_id 直查（即时、不受目录列表延迟影响），并在导出完成后先等文件就绪再加退避，减少对 115 的重复访问（对齐官方客户端“轮询状态+加载窗”行为）。真实账号复验 REPLACE_OK/sha1/下载通过。新增 get_115_file_info 封装与相关单测，完整 unittest 505 项、compileall、node --check、git diff --check 均通过。
- 2026-08-17 | `main` 未提交 | 目录树任务页 UI 整理：(1) 进度条内嵌进任务卡片头部，按任务流程阶段实时推进，任务结束后把进度重置为 0/就绪（此前结束后卡在最后百分比）；(2) 删除独立的“生成记录”卡片，日志与记录合并为单一日志框，并按新流程排版阶段日志（提交导出→等待官方生成→替换网盘树文件→sha1 对比→下载并解析/全量重写→结果汇总：解析/写入/清理/耗时/sha1）；(3) 修复日间主题配色：theme-day 的 `text-white` 覆盖漏了 `#page-task`，已补上（白字在浅色卡片上不可见）。新增进度重置与日间 CSS 回归断言，完整 unittest 506 项、compileall、node --check、git diff --check 均通过。
- 2026-08-17 | `main` 未提交 | 目录树日志改为订阅任务同款分割线风格并加入分步计时：`task-divider` 起止行（任务开始/执行成功/未变化跳过/执行失败 + 总用时），主体按【生成目录树】（提交导出/等待官方生成/替换网盘树文件/小计）与【解析目录树】（sha1 对比/下载并解析/小计）两大部分排版，末尾输出“步骤耗时：…”与“总用时：…”；全部同步同样补起止分割线与逐任务耗时。新增计时汇总单测，完整 unittest 508 项、compileall、node --check、git diff --check 均通过。
- 2026-08-17 | `main` 未提交 | 旧目录树配置字段一次性清理：启动时若 settings.json 仍含 `trees/sync_mode/check_hash/cron_hour/last_hash`，用归一化配置重写一次并在任务日志提示，用户无需手动处理（内存中早已忽略，本次是落盘清理）。设置页章节序号重新编号：移除 3/4 目录树区块后，TG 订阅源=3、PanSou=4、网络代理=5、TMDB=6、通知推送=7、后台安全=8（标题与 order 同步）。新增清理单测与模板断言，完整 unittest 510 项、compileall、git diff --check 均通过。
- 2026-08-17 | `main` 已提交 | 本轮目录树任务化改造全部改动提交（CLI 同步补齐 `tree update/defaults` 并更新 README/CLI-API-AUDIT；完整 unittest 514 项、compileall、node --check、git diff --check 均通过；Docker 重建与真实页面复核留待下一步）。
- 2026-08-17 | `main` 未提交（功能代码已提交 `b2a27d2`） | 发布元数据更新至 `0.8.0`，同步 version.json、CHANGELOG、README 与当前交接状态，收录目录树任务化改造（官方 `files/export_dir` 生成树文件 + 远端 sha1 跳过 + 任务化 UI + CLI `tree` 命令、设置页配置迁移与旧字段自动清理、容器启动重启循环与 115 重命名自动补 `.txt` 根因修复）。完整 unittest 514 项、compileall、node --check、git diff --check 与 version.json JSON 解析均通过；Docker 未重建。下一步：Docker 重建后实测目录树任务页按钮/任务流转与 CLI `tree` 命令，确认后提交推送。
- 2026-08-18 | `main` 未提交 | 磁力/电驴离线任务改为官方云下载任务完成检测驱动：新增 115 `lixian.115.com` 的 `task_lists` 查询（`list_115_offline_tasks`，复用 115 限速锁与 Cookie 健康上报），提交后记录磁力 `btih`/电驴哈希与目标目录，后台轮询器按“哈希或 URL 匹配 + `wp_path_id` 一致”等待任务完成（status=2）再触发监控扫描；失败/超时（默认 12 小时，可配置）置 failed 不自动扫描，errcode 10008 重复任务不等待并保留手动刷新；“监控触发延时”保留为首次轮询前最小等待；定时监控扫描在存在未完成离线任务时自动顺延，完成后由轮询器补发；任务中心磁力/电驴任务卡片新增 115 下载进度条（百分比 + 已下载/总大小，日/夜主题适配）。新增只读诊断接口 `GET /resource/offline/tasks` 与 CLI `offline list [--page N]`，README/CLI-API-AUDIT 同步。新增 tests/test_resource_offline_completion.py、test_resource_offline_progress_frontend.py 与 CLI 回归，完整 unittest 538 项、compileall、改动 JS `node --check`、git diff --check 均通过。下一步：Docker 重建后用真实 115 账号实测磁力/电驴哈希匹配（重点核实电驴任务 `info_hash` 与 ed2k 哈希关系）、慢速任务进度展示与 cron 顺延、秒存立即扫描、死种超时置失败。
- 2026-08-19 | `main` 已提交 | 修复目录树导出超时并调整“全部同步”按钮：官方导出等待时长从固定 600 秒提高到默认 1800 秒（`TREE_EXPORT_TIMEOUT_SECONDS` 可调），超时后任务置 failed，错误信息给出明确手动指引（到 115 根目录找到服务端命名的新树文件，删除旧标准名文件并改名，再点「下载并生成」）；原「全部同步」按钮改名为「下载并生成」，不再按 sha1 跳过，改为直接下载已存在的标准名树文件并进入生成，覆盖“导出超时后手动改名、点同步却拿旧文件/不生成”的死循环。顺带把日志「生成目录数」统一纠正为「生成目录树」。新增 4 项回归（超时指引落库、force_fetch 强制下载、未强制时仍跳过、按钮与 JS 提示改名），完整 unittest 584 项、compileall、node --check、git diff --check 均通过。下一步：Docker 重建后实测超时提示与「下载并生成」流程。
- 2026-08-19 | `main` 已提交 | 发布元数据更新至 `0.9.1`，同步 version.json、CHANGELOG、README 与当前交接状态，收录目录树导出超时处理与「下载并生成」按钮调整；完整 unittest 584 项、compileall、改动 JS `node --check`、git diff --check 均通过。下一步：Docker 重建后实测超时提示与「下载并生成」流程，确认后推送。
- 2026-08-25 | `main` 未提交 | 精准变更同步失败分类收口：索引清单/路径越界、格式损坏、监控任务不存在等确定性错误首次失败即丢弃；临时本地错误最多总计 3 次，第三次失败清理事件；STRM 回滚失败仍保留重试；监控汇总新增丢弃数量并调整终止日志；启动恢复同步清理历史确定性失败。新增/更新精准同步回归，完整 586 项 unittest、compileall、git diff --check 通过。下一步：Docker 可用时重建后在 NAS 复现一次整理失败场景，确认历史越界事件不再长期残留。
- 2026-08-25 | `main` 未提交 | 修复任务中心翻页内容不刷新：资源页动态模块统一使用 `job_page/job_page_size` 请求协议，避免后台轮询继续请求第一页；任务状态控制器拆分翻页与轮询的请求意图版本，翻页完成前旧轮询响应不会覆盖当前页；导入/刮削任务均保留每页 10 条并显示当前页附近最多 5 个页码。页码按钮补充日间/夜间主题的中性未选中色、实色当前页和焦点态。新增资源动态模块请求契约、翻页与轮询并发、页码配色回归；完整 unittest 595 项、compileall、改动 JS `node --check` 与 `git diff --check` 通过。Docker/真实浏览器验证待环境可用后补。
- 2026-08-25 | `main` 未提交 | 优化任务中心移动端分页选择器：分页控制组与总数信息分层，移动端只显示当前页附近 3 个页码并强制上一页/页码/下一页保持同一行，桌面端保持最多 5 个页码；导入任务与刮削任务模板同步调整。新增移动端分页结构与 CSS 回归。用户重建容器后已用 390x844 实际页面验证：移动端页码 3 个、桌面页码隐藏、上一页/下一页同组、总数独立显示，第 2 页点击后内容替换且无浏览器错误。完整回归待最后一轮执行。
- 2026-08-25 | `main` 未提交 | 修复任务列表竖屏分页重叠：`.resource-browser-load-more-row` 是滚动列表的最后一个 flex 子项，移动端 `min-height: 0` 且默认 `flex-shrink: 1`，列表内容超高时整行被压缩到 23px，页码按钮/上一页下一页直接压进最后一张任务卡片、总数溢出行底。改为 `flex: 0 0 auto` 固定页脚不压缩（同规则同时保护文件浏览/文件夹选择的“加载更多”页脚）。390x844 真实页面验证：修复前分页行高 23px 且与卡片重叠 7px；修复后行高恢复 85px，页码组与总数完整显示在行内，滚动到底无重叠、无浏览器错误。新增 CSS 回归，完整 unittest 598 项、compileall、git diff --check 均通过；Docker 未重建。
- 2026-08-25 | `main` 未提交 | 115 订阅扫描链接支持一次性磁力/电驴离线入库（P0）：订阅任务“扫描链接”弹窗对 115 放行 `magnet/ed2k`（前端类型校验、占位/说明文案），`POST /subscription/start_with_link` 115 分支接受磁力/电驴并给 manual_candidate 带 `link_type`；runner 仅对“手动扫描链接候选”执行离线管线（搜索自动发现的磁力继续不路由、磁力不持久化到任务配置）：提交 115 离线到 `云下载/磁力中转/<订阅任务名>/`（根目录可配 `magnet_staging_root`，默认 `云下载/磁力中转`）→ 复用哈希+目录匹配轮询完成（进度写订阅日志、可取消、默认 12 小时超时）→ 递归列中转目录（垃圾文件 sample/预告/封面/nfo 等直接删除）→ 复用订阅剧集/标题/质量筛选挑选命中文件 → 同名同大小去重后同盘移动到订阅 savepath → 精准触发监控刷新（STRM/自动刮削）→ 未命中文件保留 7 天后自动清理、命中 0 标记保留。新增 tests/test_subscription_manual_offline.py（手动候选类型/路由门禁/剧集缺失集挑选/整条管线 mock），完整 unittest 606 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建。下一步：Docker 重建后用真实 115 账号实测磁力/电驴完整链路（中转目录创建、离线完成识别、移动入库、STRM/自动刮削触发、垃圾清理与未命中保留）。
- 2026-08-25 | `main` 未提交 | 修复订阅磁力离线整包“没命中”根因：中转文件条目把 `episodes` 存成 set，而复用的 `_pick_best_tv_share_files_by_episode_bucket` 只认 list，导致 TV 分支选不出文件、回退成“按标题挑单个最佳文件”——整包 36 集只挑 1 集甚至挑不到。改为排序 list，并用真实种子目录名（`【高清剧集网发布 www.BPHDTV.com】百花杀[…]` + `Blossoms.of.Power.S01E01…mkv`）新增多集/整季挑选回归，确保走剧集缺失集桶选而非单文件兜底。完整 unittest 611 项、compileall、node --check、git diff --check 均通过；Docker 未重建，真实账号复测待补。
- 2026-08-25 | `main` 未提交 | 修复“移动/新增文件夹进了监控目录但自动刮削不触发”：原因是从项目内移动进监控目录的内容先被“刮削变更同步”处理（生成 STRM 并写入 monitor_files 索引、大目录标需手动监控），等扫描跑时文件已不是“新增”，自动刮削只对扫描时的新增文件生效。改为变更同步在“实际生成新 STRM”时收集新增媒体条目（`_event_added_media_items`：目录事件用 indexed_files、文件事件用 new_path，经 `_task_path_context` 转 remote_rel），`process_monitor_change_events` 汇总进 `new_media_items`，`run_monitor_change_task` 在任务开启 auto_scrape_on_new 时调用 `_auto_scrape_new_media_items` 自动整理（仅当 generated>0）。新增 2 项回归（开/关 auto_scrape_on_new 的文件夹移动），完整 unittest 613 项、compileall、git diff --check 均通过；Docker 未重建，真实账号复测待补。
- 2026-08-25 | `main` 未提交 | 订阅磁力中转目录定期清理（30 分钟）：新增 `app/services/subscription_offline_cleanup.py` 与启动器 watcher（`SUBSCRIPTION_OFFLINE_CLEANUP_INTERVAL_SECONDS` 默认 1800，可调）。每个任务轮询清理 `云下载/磁力中转/<任务名>/`：先读 115 离线任务列表，`wp_path_id` 命中运行中任务（status 非 2/-1）的任务目录跳过；否则递归列目录——垃圾文件（sample/预告/封面/nfo 等）直接删除、超过 7 天保留期的未命中文件删除、随后按“无文件后代”自底向上删除空目录（含移动后遗留的空种子目录与整体空任务目录）。新增 3 项回归（垃圾/过期/空目录清理、运行中任务跳过、整轮扫描），完整 unittest 616 项、compileall、git diff --check 均通过；Docker 未重建，真实账号复测待补。
- 2026-08-25 | `main` 未提交 | 变更同步“需手动监控”改自动补扫：未知清单（来源无索引/清单不可信）的文件夹移动/复制/重命名事件，`_apply_precise_event`/`_reconcile_event` 返回 `manual_required_path`，`process_monitor_change_events` 汇总进 `manual_required_paths`；`run_monitor_change_task` 处理完变更后经 `_queue_auto_rescan_for_manual_required`（复用 `queue_monitor_dir_scan` 的 savepaths 精准范围）自动排队补扫该目录，状态文案改为“变更同步待自动补扫”，用户无需再手动触发；排队失败才回退“待手动监控”。按用户决定不做大目录阈值/上限保护（用户自行控制移动规模）。新增 4 项回归（未知清单路径收集、自动排队去重计数、失败回退 0、空路径跳过），完整 unittest 620 项、compileall、git diff --check 均通过；Docker 未重建，真实账号复测待补。
- 2026-08-25 | `main` 未提交 | 文件夹监控日志可读性优化（方案：docs/superpowers/plans/2026-08-25-monitor-log-readability.md）：后端 `run_monitor_task` 新增两行——任务开始后按触发方式输出 `范围: …`（资源导入/Webhook 定位目录、指定目录、需补扫首层、全任务回退），执行成功前输出 `结论: 新增/更新 N | 跳过 N | 自动整理 N 项 | 清理 N`（复用自动整理结果摘要）；均抽成纯函数 `build_monitor_scope_line`/`build_monitor_conclusion_line`。前端 index.js 把汇总行装饰扩展到 `首层汇总:`/`变更同步汇总:`，并新增 `decorateMonitorLogText` 按前缀加图标（读取📁/生成＋/跳过⏭/清理🧹/自动整理⟳/范围➜/失败✕/结论✔），task-divider 解析与 level 色调保持不变。新增 tests/test_monitor_log_readability.py（后端 9 项纯函数 + 前端源码标记），完整 unittest 630 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，浏览器实测日志展示待补。
- 2026-08-25 | `main` 未提交 | 撤回日志逐行图标前缀（用户反馈清理等步骤加图标不好看）：移除 index.js `decorateMonitorLogText`，非汇总行恢复纯文本展示；保留汇总行彩色装饰（含首层/变更同步汇总）与 task-divider 样式。后端范围/结论行不变。更新前端源码回归为“保留汇总装饰、无图标前缀”。完整 unittest 634 项、compileall、node --check、git diff --check 均通过。
- 2026-08-25 | `main` 未提交 | 修复"变更同步汇总"整行变红：装饰函数原先只按 ` | ` 切分指标，中文逗号分隔的变更同步汇总被当成单个未知指标、颜色被行级样式覆盖。改为按 `[|，]` 切分，并为"完成/失败/生成/删除/局部读取目录/文件/需手动监控/丢弃"补齐四色映射（正值高饱和、0 值沿用 summary-zero 低饱和）。更新前端源码回归。完整 unittest 634 项、node --check、git diff --check 均通过。
- 2026-08-25 | `main` 未提交 | 变更同步汇总精简：按用户要求合并重复口径——`需手动监控` 原是"完成"的子集（事件完成即计入完成），改为"事件 N（完成 / 失败 / 丢弃）"括号分组 + 去掉冗余的"文件"指标；"需手动监控"不再常驻列表，仅在 >0 时追加"需补扫 N"警告。新格式：`变更同步汇总: 事件 34（完成 34 / 失败 0 / 丢弃 0） | 生成 STRM 34 | 删除 STRM 34 | 局部读取目录 0`。前端装饰支持括号内子指标与"事件/生成 STRM/删除 STRM/需补扫"配色。更新对应后端断言，完整 unittest 634 项、compileall、node --check、git diff --check 均通过。
- 2026-08-25 | `main` 未提交 | 变更同步三项修正：(1) 汇总统计改"净效果"——`_apply_precise_event`/`_reconcile_event` 返回 `deleted_paths`/`generated_paths`（按 STRM 本地路径），`process_monitor_change_events` 用批次内路径状态机抵消"先生成又被删除"的中间态（如文件夹改名后文件再改名：66/66 恢复为 33/33），日志逐条不变；(2) 跳过 `source_action` 以 `scraper-job:` 开头事件的自动刮削聚合（刮削任务自己的动作不再二次整理，直接刮削页移动仍保留）；(3) `run_monitor_change_task` 事件总数为 0 时空跑早退，输出"无待处理变更，本轮跳过"。新增 3 项回归（净统计抵消、scraper-job 跳过自动刮削、空跑早退），完整 unittest 637 项、compileall、git diff --check 均通过；Docker 未重建，真实账号复测待补。
- 2026-08-25 | `main` 未提交 | 修复顶部"任务"按钮数字气泡在竖屏被上方裁掉/遮挡：`.nav-task-center-btn .resource-job-trigger-badge` 原为 `top: -0.28rem` 伸出按钮上方，竖屏玻璃拟态 sticky 工具栏下会被裁。改为 `top: 50% + translateY(-50%)` 右缘垂直居中并加 `z-index: 2`，不再向上越界。新增 CSS 源码回归，完整 unittest 638 项、git diff --check 均通过。
- 2026-08-25 | `main` 未提交 | 修复手动磁力/电驴导入完成后仍全任务扫描：多文件种子会被 115 按种子名建文件夹落在 savepath 下，当 savepath 等于监控根目录时"按 savepath 刷新"退化为全扫。离线完成时新增 `resolve_offline_download_folder_hint`（列保存目录，按 115 任务返回的种子名匹配同名文件夹，仅单文件时为空），`trigger_resource_job_refresh` 支持 `offline_folder_name`：命中文件夹时把它作为 `sharetitle` + `refresh_target_type=folder` 传入，`extract_webhook_refresh_path` 精确定位到实际下载文件夹只扫该层；无文件夹（单文件）保持原 savepath 行为。新增 3 项回归（种子文件夹命中、单文件为空、刷新载荷带精确目录），完整 unittest 641 项、compileall、git diff --check 均通过；Docker 未重建，真实账号复测待补。
- 2026-08-25 | `main` 未提交 | 多网盘互传可行性调研整理成规范文档 `docs/superpowers/specs/2026-08-25-cross-cloud-transfer-feasibility.md`：收录五网盘能力现状、各盘秒传哈希机制与 Alist 跨盘秒传矩阵（仅 115↔阿里 现实可行）、流式中转架构（P1 起需补下载解析/分片上传）、订阅同盘转存现状与跨盘互传方案（P2）、磁力→115→挑选入库 P0 已实现状态。跨盘互传本身未实现，文档为设计基线。
- 2026-08-25 | `main` 已提交（待推送） | 发布元数据更新至 `0.10.0`，同步 version.json、CHANGELOG、README 与当前交接状态，收录：订阅扫描链接一次性磁力/电驴离线入库（P0）、手动磁力导入完成按实际下载文件夹精确定位刷新、变更同步自动补扫/跳过刮削任务自身重复自动刮削/净效果统计/空事件早退/汇总精简、监控日志范围行与结论行、批量整理 Season 嵌套与季目录重命名修复、顶部任务气泡竖屏裁切修复；CLI `subscribe start-with-link` 确认已适配磁力/电驴（透传链接），CLI-API-AUDIT 与 `--link` 帮助文案同步；新增跨盘互传可行性规范文档。完整 unittest 641 项、compileall、改动 JS `node --check`、git diff --check、version.json JSON 解析均通过；Docker 未重建，真实 115 磁力离线/监控链路复测待补。
- 2026-08-25 | `main` 未提交 | 修复批量整理"取消重命名文件夹仍移入新文件夹/多一层片名"三个根因：(1) 后端 `_build_scraper_target_path` 在文件已位于源目录 Season 子目录且 organize_inside_source_folder 时仍追加 `Season N`，导致 `Season 01/Season 01` 嵌套——改为源父目录已是季目录时原地重命名不再嵌套；(2) 选中的文件夹本身就是 Season 子目录时，`build_scraper_rename_plan` 仍会生成"把 Season 01 改成片名"的目录重命名动作并给文件目标多加一层片名目录——改为选中季目录时强制关闭目录重命名、文件原地整理（修复自动刮削/直接选 Season 01 的多层目录问题）；(3) 前端 `executePlan` 直接执行已生成预览，用户改勾选后不重建就提交，导致旧计划（含目录重命名）照常执行——新增 `state.planOptionsSnapshot`，执行前对比当前选项，变更则提示并自动重新生成预览。新增 4 项回归（关闭重命名+Season 内文件原地、选中 Season 01 原地、开启重命名+tmdbid 目标正确、前端选项变更重建），完整 unittest 634 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，真实 115 批量整理复测待补。
- 2026-08-27 | `main` 未提交 | 修复批量整理两个问题：(1) 剧集文件夹内多选文件批量整理预览只显示第一个——批量扫描把散落同剧集文件合并为一个条目（`entries` 含全部文件），但前端生成批量计划时只传了 `entry`（第一个文件），现把 `entries` 一并透传给 `/scraper/batch/plan`，后端按全部文件逐条生成动作；(2) 无法识别集数的文件手动输入集数点“应用”提示成功但预览不变——批量分支更新计划后未重渲染预览，现补充 `renderPlan`/`renderEntries`，失败时同时恢复 `state.batchPlanContext` 避免残留脏覆盖。新增 2 项前端回归，完整 unittest 643 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，真实 115 批量整理/手动集数预览实测待补。
- 2026-08-27 | `main` 未提交 | 修复重命名框拖选文本越界误关：`promptText` 原先在 click 事件里判断点击遮罩（`event.target === modal`）关闭，鼠标从输入框拖选越过小窗口、在遮罩上松开时 click 的公共祖先仍是遮罩，导致重命名框被误关；改为遮罩关闭只认 pointerdown（按下时就在遮罩上），click 仅处理取消/确认按钮，并加 `modal.isConnected` 防重复清理。新增 1 项前端回归，完整 unittest 644 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，真实浏览器拖选实测待补。
- 2026-08-27 | `main` 未提交 | 批量整理“文件夹重命名但文件已是标准名”时预览语义对齐：新增 `_is_scraper_folder_prefix_only_change`，计划层判断文件新旧路径唯一差异只是“所在文件夹前缀”（如 `一级/B/文件` -> `一级/新片名/文件`，含已在 `Season 01` 子目录的剧集文件），此类文件不再生成独立可执行动作，降级为 `unchanged_rows`（`note=随文件夹重命名，文件未单独移动`）；前端未变化行按 note 显示“随文件夹重命名 / 文件未单独移动，保持原名”。网盘侧仍只执行一次文件夹改名，执行层无需改动；文件名确实变化或文件要移入新 `Season 01` 等结构变化时仍保持可执行动作。新增 3 项回归（电影标准文件被动行、Season 子目录文件被动行、批量 note 透传 + 前端 note 渲染），完整 unittest 647 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，真实 115 批量整理复测待补。
- 2026-08-27 | `main` 未提交 | 标题清洗新增“国语音轨”排除关键词：加入 `SCRAPER_COMMON_NOISE_PHRASES`（与“国语配音/中文音轨/双音轨/多音轨”同组），任意位置清洗与纯噪声判定生效，`监狱星级餐厅.国语音轨.2024.1080p.mkv` 现只识别 `监狱星级餐厅`。新增 1 项回归，完整 unittest 648 项、compileall、git diff --check 均通过；Docker 未重建。
- 2026-08-27 | `main` 未提交 | 设置页新增“批量整理过滤词”（第 9 节，可选）：`scraper_noise_phrases`（复合词，任意位置删除）与 `scraper_standalone_noise_words`（独立词，按 CJK 词边界删除），内置默认词表保留、只追加自定义词；`default_config/normalize_config` 归一化（strip/去重/单项 50 字/200 条），`_get_scraper_noise_rules()` 按配置内容缓存动态合并正则并替换 `_is_scraper_generic_keyword`/`_clean_search_title` 的静态引用（配置读取失败时回退内置词表），settings.js 行解析提交、boot.js 数组配置按换行回填。新增 10 项回归，完整 unittest 658 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，设置页保存/识别实测待补。
- 2026-08-27 | `main` 未提交 | 修复 115 官方搜索结果路径被伪造：`_normalize_115_file_entry` 透传接口真实 `pid/path`；`list_scraper_entries` 官方搜索不再把结果补成“当前目录子项”，无真实路径的条目标 `search_result/path_unknown` 并清空伪父目录；前端 `enrichEntry`/`getSelectionParentPath` 不再把搜索结果拼到当前面包屑，`enterFolder` 从搜索态进入时把 trail 替换为“根目录/该文件夹（searchJump）”，URL 不再写假 path；重命名/移动/复制/删除/批量整理对未知路径搜索结果统一拦截提示“先进入该文件夹再操作”。新增 3 项回归（pid/path 透传、搜索未知路径标记、前端路径不伪造与操作拦截），完整 unittest 661 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，真实 115 搜索进入/操作实测待补。
- 2026-08-27 | `main` 未提交 | 修复 115 搜索结果进入文件夹后路径栏不显示真实路径：后端新增 `resolve_115_folder_path`（一次 `webapi.115.com/files/medialist?cid=…&limit=1&type=6&nf=1` 请求，响应自带 `path` 祖先链，真实账号实测 `狂飙` → `115自存电视剧/狂飙 (2023) [tmdbid-210757]`、深层目录也能拿到完整链），服务层新增 `resolve_scraper_folder_path` 与 `GET /scraper/{provider}/folder-path` 路由；前端 `enterFolder` 对搜索结果目录先调该接口恢复真实祖先链再显示面包屑（URL 写真实 path，可刷新恢复），查询失败才退回“根目录/该文件夹（searchJump）”并保留“不写假 path”的兜底。新增 7 项回归（medialist 解析/根目录免请求/失败报错、115 服务层透传、非 115 空路径、路由透传、前端解析器与真实路径渲染），完整 unittest 668 项、compileall、改动 JS `node --check`、git diff --check 均通过；已 `docker compose up -d --build` 重建并用真实 115 账号浏览器实测：搜索“狂飙”点击该文件夹后面包屑为 `根目录 / 115自存电视剧 / 狂飙 (2023) [tmdbid-210757]`，URL 写入真实 path，刷新后路径栏可恢复。
- 2026-08-28 | `main` 已提交并推送 | 发布元数据更新至 `0.10.1`，同步 version.json、CHANGELOG、README 与当前交接状态，收录：批量整理多文件预览/手动集数刷新/文件夹重命名被动行、重命名框拖选误关修复、国语音轨排除关键词、设置页批量整理过滤词（第 9 节）、115 官方搜索结果不伪造路径与进入后真实路径栏（medialist 祖先链 + URL 真实 path 可刷新恢复）；完整 unittest 668 项、compileall、改动 JS `node --check`、git diff --check、version.json JSON 解析均通过；已 `docker compose up -d --build` 并用真实 115 账号浏览器实测搜索进入路径栏。另收录 `docs/superpowers/specs/2026-08-27-cross-cloud-copy-design.md` 跨网盘复制流式中转设计规范（仅设计文档，未实现）。下一步：无待办，若需迭代跨盘复制按该规范排期。
- 2026-09-07 | `main` 已提交 `1f05fe8`（待推送） | 油猴脚本新增“不生效的网页（排除域名）”设置并提升脚本 `@version` 至 `2.6.0`：任务管理器中新增排除域名输入区（每行一个域名，支持带协议/端口/路径/通配符，裸域名同时匹配其子域名），用户可在菜单/任务管理器里配置，PC 与手机端（iOS Userscripts 走任务选择器“任务管理”入口）都能使用；新增存储键 `magnet_push_exclude_hosts_v1` 并接入 `loadPersistedState`/`saveExcludeHosts`，归一化函数 `normalizeExcludeHost`/`normalizeExcludeHostList`、匹配函数 `isHostExcluded` 供 test 导出；`init` 在匹配到排除域名时只保留油猴菜单/任务管理器以便改回，跳过扫描与按钮注入。新增 3 项回归（归一化与匹配、持久化回读、脚本内 UI 标记），完整 unittest、改动 JS `node --check`、git diff --check 均通过。排查确认本地 `127.0.0.1:18080` 实例已下发含排除域名功能的新脚本，但正式部署 `hub.shuxian.fun` 仍是旧 0.10.2 镜像、脚本无此功能；需推送后部署 `hub.shuxian.fun` 并重装脚本。下一步：推送后在 `hub.shuxian.fun` 拉取新代码（或新镜像）重建，再重装 `115-media-hub助手` 验证排除域名入口与跳过生效；确认后按需推送。
- 2026-09-07 | `main` 已提交并推送 | 发布元数据更新至 `0.10.2`，同步 version.json、CHANGELOG、README 与当前交接状态，收录：订阅绑定 TMDB 后标题/任务名总同步为标准片名、订阅/资源导入（电驴/磁力/telegra 页面）“新建文件夹”推荐名称一键填入 `片名 (年份)`（网盘分享类不预填）、资源导入推荐名复用刮削标题清洗规则（guessit + 噪声/站点/清晰度清洗 + 媒体标签剥离，自动去掉“剧集/电影/电视剧”类型前缀并修复年份重复）、电驴裸链接标题/大爆炸取真实文件名与旧资源回退、电驴文件夹名称行三控件布局与点击修复。完整 unittest 689 项、compileall、改动 JS `node --check`、git diff --check、version.json JSON 解析均通过；Docker 未重建，真实 115 电驴/磁力导入推荐名与订阅保存目录推荐待浏览器复核。注：`115-magnet-helper-webhook.user.js` 及其测试（排除域名功能）为独立未提交改动，未纳入本次发布。
- 2026-09-09 | `main` 未提交 | 修复资源导入三问题：(1) 链接选择——`getResourceImportCandidates` 的 ed2k 候选去重由「仅按链接类型」改为「按导入方式+链接类型」，telegra 电驴页（`ed2k-page`）与裸 ed2k 链接（`ed2k-direct`）各自独立成候选，避免 telegra 因去重覆盖其它可导入链接，≥2 个可导入候选必「选择下载或转存链接」；仍只列可导入且已配置项（普通 http 直链、未配置 cookie 的网盘分享不列出）。(2) 步骤条位置——把 `#resource-import-stepper` 从「资源文案预览卡」与「文件浏览卡」之间移到资源概览卡之后、电驴文件列表之前，所有导入模式步骤条都位于内容/文件选择区上方。(3) 电驴空文件夹名——`isResourceEd2kReady` 不再因名称为空判定未就绪；提交按「实际是否建子目录 = 勾选且名非空」传 `create_folder`/`folder_name`，名称为空即不建子目录、直接保存到所选手动目录（后端 `create_folder=false` 已有支持），原名称为空时按钮不再灰，并新增「名称为空时将直接保存到所选目录」轻提示与预览同步。新增 6 项前端/后端回归（候选按 importMode 去重、目标路径空名预览、就绪不再要求名称、提交 createsSubfolder、空名提示、步骤条位置、后端 create_folder=false 空名直存），完整 unittest 695 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，真实浏览器交互实测待补。
- 2026-09-09 | `main` 未提交 | 修复电驴同名嵌套与推荐名冒号丢失：(1) 同名嵌套——新增 `getResourceEd2kSavepathLastSegment`/`shouldCreateResourceEd2kSubfolder`（core.js 导出），`getResourceImportTargetSavepath` 预览与 `submitResourceJob` 提交统一用「所选目录末段名 != 新建文件夹名」判定 `createsSubfolder`，同名/空名时 `create_folder=false`、`folder_name=''` 直接保存到用户手选/新建的第一层目录；`syncResourceEd2kFolderControls` 检测到「新建文件夹名 == 所选目录末段」时清空名称与输入框（设为空置）并刷新预览/按钮/提示，避免 `一级/同名/同名`。(2) 推荐名保留标题内冒号——`_split_scraper_mixed_language_title`/`_extract_scraper_title_candidates` 增加 `preserve_colon=False` 参数，`recommend_media_folder_name` 以 `preserve_colon=True` 调用并用新增 `_sanitize_folder_preserving_colon`（`:`→全角`：`、去首尾孤立冒号/空格、`：+空格`归一）替代候选清洗与年份拼接处的 `sanitize_115_folder_name`，`电影：阿凡达：水之道`→`阿凡达：水之道 (2022)`，类型标签冒号仍删、内层冒号保留；刮削搜索候选默认路径不变（已验证去冒号不影响 TMDB 检索），前端兜底 `title-utils.js sanitizeFolderName` 同步 `:`→`：`。新增 5 项回归（推荐名保留内层冒号、preserve_colon 候选拆分/搜索默认不变、core 同名判定、folder-controls 同名清空、title-utils 全角冒号），完整 unittest 700 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，真实浏览器交互实测待补。
- 2026-09-09 | `main` 未提交 | 参数配置“网盘认证与签到”网盘行新增展开提示：`renderProviderAuthBlocks` 每行右侧（开关左侧）增加下拉箭头 svg（`.provider-chevron`，默认收起），点击整行展开/收起时箭头旋转 180°（`.is-open`）并同步头部 `aria-expanded`；新增 `data-provider-header`/`data-provider-chevron` 标记给 `toggleProviderBlock` 定位，开关点击仍用 `stopPropagation` 不触发展开。新增 1 项源码回归（chevron 标记、箭头旋转与 aria、CSS `.provider-chevron`/`.is-open`），完整 unittest 701 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，真实浏览器渲染实测待补。
- 2026-09-09 | `main` 未提交 | 新增 115 网盘扫码登录（客户端可手选，默认微信小程序）：后端 `app/providers/pan115_qr.py` 封装三步扫码（`qrcodeapi.115.com/…/token` 取 uid/time/sign → `qrcodeapi.115.com/get/status` 轮询 → `passportapi.115.com/app/1.0/{app}/1.0/login/qrcode` 换 cookie），`app/http_utils.py` 新增 `http_request_bytes` 透传二维码 PNG，`app/routes/settings.py` 新增 `/settings/providers/115/qrcode/*` 五个接口（apps/token/image/status/result），result 成功后写 `cookie_115` 并触发健康检查；前端 settings.js 在 115 认证块加“扫码登录 + 客户端下拉（默认微信小程序）+ 说明文案”，扫码成功后重渲染块并跑健康检查。可选客户端剔除已下架 linux/mac/windows，默认/推荐 wechatmini、alipaymini、tv（避免挤掉用户现有登录态）。新增 tests/test_pan115_qr.py（10 项 mock 断言），完整 unittest 711 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，真实 115 扫码链路（二维码图片与 qrcodeapi/passportapi 接口稳定性）待本机 `docker compose up -d --build` 后用真实 App/小程序扫码复测。
- 2026-09-09 | `main` 未提交 | 阿里云盘认证改为内置官方 OAuth + PKCE 扫码授权（方案 A，公开 client_id + `redirect_uri=oob`，无需公网回调）：后端 `app/providers/aliyun_oauth.py` 生成带 `code_challenge`(S256) 的官方授权地址并把 `code_verifier` 存一次性会话（TTL 900s），`exchange_aliyun_code` 用授权码到 `openapi.alipan.com/oauth/access_token` 换 `refresh_token`；`app/providers/aliyun.py` 的 `_ensure_access_token` 刷新改为优先官方 `openapi.alipan.com/oauth/access_token`（客户端 ID 走公开 `55091393987b4cc090b090ee17e85e0a`），保留 `auth.alipan.com`/`auth.aliyundrive.com` 回退并移除第三方中继 `api.alistgo.com`；`app/routes/settings.py` 新增 `/settings/providers/aliyun/oauth/start` 与 `/complete`（complete 写 `aliyun_refresh_token` 并直接 probe 一次）；前端 settings.js 在阿里云块加“内置扫码授权”按钮 + 授权码输入 + 绑定保存，原第三方 `aliyuntoken.vercel.app` 入口降级为备用说明。新增 tests/test_aliyun_oauth.py（8 项，含 PKCE 会话/官方刷新优先与回退），完整 unittest 719 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，真实阿里云盘授权（官方授权页二维码、oob 授权码、token 刷新）待本机 `docker compose up -d --build` 后用真实账号扫码复测。
- 2026-09-09 | `main` 未提交 | 修复阿里云内置扫码“绑定并保存”返回 HTTP 400：真实账号授权页扫码成功、能拿到 code，但 `openapi.alipan.com/oauth/access_token` 拒绝了 JSON 请求体——PDS 官方该接口 `consumes: application/x-www-form-urlencoded`。改为 `exchange_aliyun_code` 与官方 token 刷新均提交表单（保留 `auth.alipan.com`/`auth.aliyundrive.com` JSON 回退），并新增 `_read_http_error_detail`：HTTP 4xx 时读取响应体提取阿里云盘错误消息；`settings.py` 的 `/complete` 400 响应补齐 `message`，前端 `buildApiError` 因而能显示真实原因而非笼统“HTTP 400”。新增 1 项 HTTPError 详情透出用例，完整 unittest 720 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，需本机重建后重新扫码取新 code 复测（旧 code 可能已被失败请求消费）。
- 2026-09-09 | `main` 未提交 | 修复阿里云内置扫码“invalid s256 code_verifier”：表单提交已过关，但后端换取报 PKCE 校验失败——根因是 `code_verifier` 存在后端内存会话、frontend 仅回传 `state`，多点几次“获取授权码”或旧授权页遗留时，code 与 verifier 会来自不同授权请求（challenge 不匹配）。改为 PKCE 公开客户端标准写法：`/start` 返回 `code_verifier`，前端随授权码一并回传（不再依赖后端一次性会话，规避多 worker/会话漂移）；`/complete` 用回传的 `code_verifier` 换 token。前端“获取授权码”在流程结束前禁用、每次生成新授权页时清空旧授权码输入框，杜绝多授权页互串。更新 tests/test_aliyun_oauth.py 为 verifier 随前端往返（9 项），完整 unittest 720 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，需本机重建后重新“获取授权码→扫码→粘贴”复测。
- 2026-09-09 | `main` 未提交 | 【修正先前判断】阿里云内置扫码“invalid s256 code_verifier”真正根因不是“verifier 会话漂移”，而是 PKCE 方式选错：阿里云盘个人版公开客户端按官方文档走 **plain**（`code_challenge` 为 43-128 随机字符串，`code_verifier` 为其“原始值而非摘要值”，官方授权示例即 `code_challenge=11111&code_challenge_method=plain`）。此前按标准 S256 把 verifier 哈希后当 challenge，服务端拿原始 verifier 比对一个哈希值自然不匹配。已把 `app/providers/aliyun_oauth.py` 的 `_generate_pkce_pair()` 改为 plain（`code_challenge = code_verifier`，`code_challenge_method=plain`）；verifier 仍随前端往返、`/start` 返回给前端回传不变（该机制保留，仅纠正 PKCE 算法）。更新 tests/test_aliyun_oauth.py 断言为 `code_challenge_method=plain` 并校验 URL 中 `code_challenge == code_verifier`，完整 unittest 720 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，需本机 `docker compose up -d --build` 后重新“获取授权码→扫码→粘贴”复测。
- 2026-09-09 | `main` 未提交 | 阿里云内置扫码“code not found”排查与宽容化：先用假授权码实探官方两个换取端点——`openapi.alipan.com/oauth/access_token` 对无效 code 返回 `{"code":"InvalidCode","message":"code not found"}`（即正确的换取端点，且确认 code 单次/10 分钟有效）；`www.alipan.com/o/oauth/access_token` 返回 405（非换取端点）。另用 curl 确认 `openapi.alipan.com/oauth/authorize` 会 302 到 `www.alipan.com/o/oauth/authorize`，说明该公开客户端（网页版）授权/回调走 `www.alipan.com`，但 code 在 `openapi.alipan.com` 换取合法。结论：端点/表单/plain PKCE 均正确，“code not found”是**授权码值问题**（已被使用/过期/粘贴了整段 `oob?code=` 地址）。改动：`exchange_aliyun_code` 新增 `_normalize_authorization_code`（用户误贴整段 `oob?code=…` 时自动抽 code 值，纯码原样保留）、`_is_code_not_found_detail`，命中 `code not found`/`InvalidCode` 时在报错里追加可操作提示（重新获取授权码、立即扫码粘贴、10 分钟内有效且只能用一次）。新增 3 项测试（URL 抽取、纯码保留、code-not-found 提示），完整 unittest 723 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，需本机重建后重新“获取授权码→立即扫码→粘贴新码”复测。
- 2026-09-09 | `main` 未提交 | 【根因对齐】阿里云内置扫码“响应缺少 refresh_token”：公开客户端（无 AppSecret，client_id=5509…）OAuth **不返回 refresh_token**，`oauth/access_token` 只返回 30 天有效期的 access_token 且不支持刷新（官方语雀“公开客户端接入，会返回30天有效期的 AccessToken，不支持刷新”）。此前按“存 refresh_token 再刷新”设计硬等 refresh_token，故报“响应缺少 refresh_token”。修复（从根源对齐存储与运行时）：`exchange_aliyun_code` 改为返回完整 token 包（extract access_token，refresh_token 可为空）；settings `/complete` 把 access_token 写入 `aliyun_refresh_token` 键并设 `aliyun_token_is_access=True` + `aliyun_access_expires_at=now+expires_in`（保留 refresh_token 兼容分支）；`AliyunProvider._ensure_access_token` 读到该标记时**直接当 Bearer 使用**（不看签名、不刷新），无标记则走旧 refresh_token 刷新（兼容手动/旧三方）；`default_config` 增补两个新键；前端文案改为“访问凭证/access_token”。不新增/重排 `config_keys`，故手动输入框、get_cookie、敏感掩码、已配置圆点均不变。新增 7 项测试（仅 access_token 响应、provider 直用长期 access、过期报错、无标记回退刷新），完整 unittest 727 项、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，需本机重建后重新“获取授权码→扫码→粘贴”复测并确认列表/转存可用。
- 2026-09-09 | `main` 未提交 | 设置页“网盘认证与签到”布局优化：此前 115 扫码登录与阿里云内置扫码授权被放在卡片底部（手动粘贴 + 健康检查之下），主推方式反而不显眼。重构 `renderProviderAuthBlocks`：对 115/阿里云 把“扫码登录 / 扫码授权”提到卡片内**首选**（绿色“推荐”标记、独立圆角面板），手动粘贴降为“备用”子区（灰“备用”标记），底部统一“健康检查”行；阿里云授权码输入与“绑定并保存”改为同行、状态行单独放；115/阿里云两块视觉样式统一（同字号、同推荐/备用标记、同面板底纹）。夸克/123云盘等其他盘保持原有简洁结构不变。所有 DOM id 与 onclick 不变，仍复用同一套 JS 流程。用 playwright 渲染暗色/日间/桌面三档验证：扫码在首、备用在次、健康检查在底、日间文字可读。改动 JS `node --check`、`git diff --check` 通过；未改 Python，无需跑全量 unittest。Docker 未重建，需本机重建后在设置页确认最终视觉与交互。
- 2026-09-09 | `main` 未提交 | 设置页网盘认证区块日/夜适配修复：此前渲染时"推荐 扫码登录/扫码授权"面板用了 `bg-slate-900/40`，但项目日间模式只在 `index.css` 里对 `bg-slate-900/50`（及 /60、/80）做了 `html.theme-day` 翻转（未含 /40），导致日间仍是深灰块。改为 `bg-slate-900/50`（日间翻成 --surface 白），并把"备用"徽章 `bg-slate-700/50`→`bg-slate-700/40`（日间翻成 --surface-soft 近白），日/夜均与卡片、输入框、页头文字一致。用 playwright 渲染暗色/日间两档验证：日间推荐面板为白底+浅边框、推荐绿字绿底、备用浅底深字，全部可读；夜间为深色内嵌面板、整体与暗色主题一致。改动 JS `node --check`、`git diff --check` 通过。Docker 未重建，需本机重建后切换日/夜模式在设置页确认。
- 2026-09-09 | `main` 未提交 | 设置页扫码区块文案/对齐微调：说明文案过长导致 2-3 行生硬换行、层级偏平。"推荐/备用"徽章从 `text-[10px] leading-none py-1` 改为 `text-[11px] leading-4 px-1.5 py-0.5`，标签下探至与正文基线协调；区块标题加 `font-medium` 增强层级；115/阿里云说明文案精简为单行（115：用不常用设备扫码自动写入 Cookie 不挤掉现有登录；阿里云：扫码写入长期 access_token 约 30 天）。用 playwright 放大渲染日间核实：说明为单行、推荐标题更突出、徽章与正文对齐、节奏更清晰。改动 JS `node --check`、`git diff --check` 通过。Docker 未重建，需本机重建后确认。
- 2026-09-09 | `main` 未提交 | 设置页扫码区块"推荐/备用"左对齐修复：此前“推荐 扫码登录/扫码授权”被包在带 `p-3` 的内嵌面板里，比直接放在卡片体的“备用”多内缩约 12px，导致两行起始位置不齐。按 frontend-skill“面板能改为普通布局就去掉卡片包装”的原则，去掉两处扫描面板的 `bg-slate-900/50 border rounded-xl p-3` 内嵌盒（保留 `-wrap` id），让“推荐”与“备用”、说明、输入框、健康检查统一从卡片体左内边距起排；说明/徽章/标题沿用上一轮的字号字重与主题定位。用 playwright 放大渲染日间核实：阿里云与 115 两张卡的“推荐”与“备用”行左缘完全对齐、说明单行、层级清晰。改动 JS `node --check`、`git diff --check` 通过。Docker 未重建，需本机重建后确认。
- 2026-09-09 | `main` 未提交 | 新增网盘凭证回显/一键复制：设置页每张网盘卡"健康检查"行新增"复制凭证"按钮，支持把已保存的 Cookie / access_token 复制到剪贴板，便于切到其它应用。后端新增 `GET /settings/providers/{provider}/credential`（挂 settings_router，受 require_auth 保护）：按 provider 的 config_keys 取非空值，返回 `credential`（主凭证，取第一个非空 config_key）+ `credential_key` + `credentials` 字典；未知网盘 404。前端：仅对 cookie/password_cookie/refresh_token 类型网盘显示按钮（账号密码型 123云盘不显示）；已配置才可点，否则 disabled+opacity-50；点击调端点拿原始值，`navigator.clipboard.writeText` 复制，失败降级 `showAppPrompt` 手动复制，成功 `showToast` 提示。新增 tests/test_provider_credential.py（4 项：115 cookie、aliyun access_token、空值 configured=false、未知 404），完整 unittest 全绿（727+4）、compileall、改动 JS `node --check`、git diff --check 均通过；Docker 未重建，需本机重建后实测复制按钮（含剪贴板权限/降级提示）。
- 2026-09-09 | `main` 未提交（未推送） | 发布元数据更新至 `0.10.4`：同步 version.json、CHANGELOG、README 与当前交接状态，收录本批次未提交改动——115 网盘内置扫码登录（客户端可手选、默认微信小程序）、阿里云盘内置官方 OAuth + PKCE 扫码授权（修复 token 表单提交、plain PKCE、公开客户端 30 天 access_token 模式）、设置页「网盘认证与签到」布局优化（推荐扫码置顶/手动备用、对齐与日/夜适配）、网盘凭证一键复制（受登录保护的 /settings/providers/{name}/credential + 复制按钮）。完整 unittest 全绿（727+4 项，含新增 pan115_qr/aliyun_oauth/provider_credential 测试）、compileall、改动 JS `node --check`、git diff --check、version.json JSON 解析均通过；Docker 未重建。下一步：部署后使用真实账号复测 115 扫码与阿里云盘授权（含 30 天 access_token 到期重扫、凭证复制到剪贴板），确认后按用户意愿提交/推送。
- 2026-09-18 | `main` 未提交 | 刮削新增可选「AI 候选增强」（批量识别 AI 回退）：批量识别时对确定性未自动匹配（`status != auto`，即 manual/suggest）的条目调用 OpenAI 兼容大模型——先在 `app/services/ai_match.py` 的 `ai_match_generate_query` 产出「关键词 + 年份 + 媒体类型」，据此复用 `_search_batch_tmdb_candidates` 搜 TMDB，再让 `ai_match_select_candidate` 从候选里二次选择；结果以 `source=ai` 候选并入 `candidates` 并置 `status=suggest`（**不写 auto_pick、不自动改名**，仍走“计划→人工确认→提交→可回滚”），结果追加 `ai_keyword/ai_year/ai_media_type/ai_selected/ai_confidence/ai_reason/ai_error`；任一失败仅在该条写 `ai_error` 并保留原确定性候选，整批不中断；`ai_match_max_concurrency` 控制并发（默认 3，单条超时 20s），`/scraper/batch/identify` 支持 `use_ai` 覆盖（缺省跟随配置）。新增 7 个配置项（`ai_match_enabled/base_url/api_key/model/timeout_seconds/temperature/max_concurrency`，api_key 走敏感字段），接入 core 默认值/normalize、config_runtime 敏感键、设置页第 6b 节与 settings.js 收集/回填、批量结果「AI 建议」徽标 + 理由 + 置信度（含日/夜 CSS）。新增 `tests/test_scraper_ai_match.py`（29 项：JSON 围栏/嵌套解析、HTTP 400 去 response_format 重试与 500/超时、配置校验与 clamp、关键词与选择解析、回退合并/并发/接线），完整 unittest 760 项全绿、compileall、改动 JS `node --check`、`git diff --check` 均通过；Docker 未重建，真实大模型端点（DeepSeek/Qwen/Ollama）与设置页保存+批量识别实测待本机 `docker compose up -d --build` 后补。下一步：真实端点联调（含 AI 建议人工确认后改名与回滚），确认后按用户意愿提交/推送。
- 2026-09-18 | `main` 已提交并推送 `f521103` | 发布元数据更新至 `0.11.0`：同步 version.json（0.11.0 / buildDate 2026-09-18 / 4 条 AI 刮削辅助说明）、CHANGELOG（新增 [0.11.0] 段）、README（功能表刮削行 + 近期更新首条 + 当前版本）与交接状态；本版仅含「AI 候选增强」。完整 unittest 760 项、compileall、改动 JS `node --check`、`git diff --check`、version.json JSON 解析均通过；Docker 未重建，NAS 部署后按设置页 6b 配置真实模型端点复测（先关 AI 验证无回归，再开 AI 验证「AI 建议→接受→生成计划→提交→回滚」）。
- 2026-09-18 | `main` 未提交 | 发布 0.11.1：DeepSeek 适配——核实官方文档确认 `deepseek-flash`（DeepSeek-V4.1-Flash）默认开启思考模式（`reasoning_effort` 默认 `high`，思考模式忽略 `temperature`），对“文件名→关键词”结构化抽取纯属浪费；`app/services/ai_match.py` 新增 `_supports_thinking_control`（base_url 含 deepseek 或模型名以 deepseek 开头）并在命中时于请求体加 `thinking: {"type": "disabled"}`，**仅对 DeepSeek 端点下发**，OpenAI/Ollama/Qwen 等不收到该非标准字段（避免 400）；新增配置 `ai_match_disable_thinking`（默认开启）+ 设置页 6b 复选框；两个 system prompt 补小写 `json` 字样与输出样例以符合 DeepSeek JSON Output 要求。测试增至 35 项（thinking 下发条件/非 DeepSeek 不下发/开关关闭不下发/host 与 model 识别/提示词含 json/默认值），完整 unittest、compileall、改动 JS `node --check`、`git diff --check` 均通过；Docker 未重建，NAS 端真实 DeepSeek 调用待复测。
- 2026-09-18 | `main` 未提交 | 发布 0.11.2：AI 刮削辅助优化 1-6——(1) 结果缓存：`app/services/ai_match.py` 新增线程安全内存缓存（默认 24h，`ai_match_cache_ttl_hours`=0 关闭，上限 2000 条按时间淘汰），key=条目身份(query 名/父目录/样例文件)+候选签名(select)+模型配置，命中不再调用、只记 `cache_hits`；(2) 跳过二次选择：`scraper._should_skip_ai_select`，唯一候选或 top≥80 且领先≥12 时直接采用并标 `ai_skipped_select`；(3) 重试：`_ai_chat_json` 对超时/连接错误与 429/500/502/503/504 最多 3 次退避（尊重 `Retry-After`，普通 4xx 不重试），并保留「400 去 response_format」同 attempt 重试；(4) 用量：解析响应 `usage`（含 DeepSeek `prompt_cache_hit_tokens`），每条 `ai_usage` + 批量顶层 `ai_usage`，前端汇总栏显示「AI 调用 N 次（缓存命中 M）· X tokens」；(5) 最低采纳置信度 `ai_match_min_confidence`（默认 0），低于门槛只写 `ai_low_confidence` 不采纳、前端提示；(6) 思考模式改三态 `ai_match_thinking_mode`（auto/disabled/enabled，disabled 供中转/自建网关强制下发），0.11.1 布尔键 `ai_match_disable_thinking` 在 `normalize_config` 自动迁移并 pop。设置页 6b 增加「最低采纳置信度/结果缓存/思考模式(select)」并移除旧复选框。测试 `tests/test_scraper_ai_match.py` 增至 51 项（缓存命中/关闭/候选签名、429 Retry-After、500/超时重试与上限、4xx 不重试、用量解析与汇总、跳过规则、低置信度、三态与迁移），完整 unittest 782 项、compileall、改动 JS `node --check`、`git diff --check`、version.json 解析均通过；Docker 未重建，NAS 端真实 DeepSeek 调用与设置页保存/批量识别实测待补。
- 2026-09-18 | `main` 未提交 | 发布 0.11.3：AI 接口地址容错与校验——用户常直接粘贴 DeepSeek 官方文档的 base_url 一栏（`https://api.deepseek.com`，或 Anthropic 版 `.../anthropic`），据此做三处加固：(1) `_normalize_base_url` 去掉尾部 `/chat/completions`（及 `/completions`）再拼接，避免 `.../chat/completions/chat/completions` 404；(2) `validate_ai_match_runtime_config` 要求 `http(s)://` 开头，且含 `/anthropic` 时直接提示「只支持 OpenAI 兼容端点」（本项目请求体是 OpenAI 格式，Anthropic 协议不同必失败）；(3) 设置页占位符与提示文案改为官方写法 `https://api.deepseek.com`（也可 `/v1`）。另实测确认：对 `https://api.deepseek.com/chat/completions` 与 `https://api.deepseek.com/v1/chat/completions` 做无密钥 POST 均返回 `401 Authentication Fails (governor)`，两者等价可用，且都含 `deepseek`，思考模式 auto 识别不受影响。测试增至 54 项（+base_url 规范化 4 写法、请求 URL 不重复、Anthropic/非法 scheme 校验），完整 unittest、compileall、改动 JS `node --check`、`git diff --check`、version.json 解析均通过；Docker 未重建，NAS 端配置实测待补。
- 2026-09-18 | `main` 未提交 | 发布 0.11.4：AI 刮削辅助可读性 + 可用性测试 + 用量统计。起因：用户在批量整理里分不清结果是否 AI 推荐、也看不到用量——排查发现「AI 报错/低置信度」只渲染在「未匹配」分支，条目本来就处于「建议」档时 AI 被跳过会完全静默。改动：(1) `static/js/modules/scraper/core.js` 建议分支补 AI 状态显示（is-ai 徽标 + 置信度/理由；非 AI 来源时显示 `AI：<ai_error>` 或「置信度 N，低于门槛未采纳」）；汇总栏按 `batchAiUsage`/`batchAiConfigError`/`batchAiEnabled` 显示「AI 调用 N 次（缓存命中 M）· X tokens」或「AI 未运行：<原因>」。(2) `app/services/scraper.py` `_apply_ai_match_fallback` 返回值由用量字典改为状态字典 `{requested, config_error, usage}`，`identify_scraper_batch_items` 据此输出 `ai_enabled`/`ai_config_error`/`ai_usage`。(3) `app/services/ai_match.py` 新增进程级用量累计 `record_ai_match_usage`/`get_ai_match_usage`/`reset_ai_match_usage`（缓存命中单独计 `cache_hits`；两条公共函数记录 latency/ok/error），新增 `ai_match_test_connection`（固定样例真实调用，绕过缓存，不受 enabled 限制；`validate_ai_match_runtime_config` 增加 `require_enabled` 参数）。(4) `app/routes/settings.py` 新增 `POST /settings/ai_match/test`（失败响应补 `msg`，否则前端 buildApiError 读不到原因）、`GET /settings/ai_match/usage`、`POST /settings/ai_match/usage/reset`。(5) 设置页 6b 新增「测试 AI 可用性」按钮 + 状态面板（复用 .tg-proxy-status 样式，日/夜一致）与「用量统计」面板（累计调用/缓存命中/输入输出/total/缓存命中 token/最近调用与错误 + 刷新/重置）；`settings.js` 新增 render/test/load/reset 四个导出，`index.js` 新增 `getCurrentAiMatchConfig` 与三个全局 onclick 处理函数，`boot.js` 启动时拉一次用量。测试：`tests/test_scraper_ai_match.py` 63 项（+用量累计与重置、缓存命中计数、测试连接成功/HTTP 失败/不吃缓存、require_enabled）、新增 `tests/test_ai_match_frontend.py` 8 项（设置页按钮与面板、index.js 全局处理函数、建议分支 AI 状态、汇总栏 AI 状态、后端路由与响应字段）；完整 unittest 802 项、compileall、改动 JS `node --check`（index/settings/boot/scraper-core）、`git diff --check`、version.json 解析均通过；Docker 未重建，NAS 端实测「测试 AI 可用性」与用量面板待补。
- 2026-09-18 | `main` 未提交 | 发布 0.11.5：油猴脚本任务选择弹窗新增「复制磁力链接」（脚本 `@version` 2.6.0→2.7.0）。需求经过一次澄清：最初理解成在页面「115」按钮旁并列加复制按钮，用户明确要的是「点 115 后弹窗内、和推送选项同层级」，因此页面注入形态保持原样（`processTextNode`/`processAnchor` 未改动），只在 `chooseTask` 的任务列表容器内追加一个同款行按钮（`data-mh-picker-action="copy-magnet"`，紫色、右侧标「不推送」，第二行分别是「torrent 链接会先自动转成磁力，再复制到剪贴板」或「把这条磁力链接复制到剪贴板，不推送任务」）。新增 `copyMagnetFromSource(sourceLink)`：`normalizeSourceLink` → `resolveMagnetFromSource`（torrent 复用推送同款解析）→ `copyTextToClipboard`，并导出到测试 API；`overlay` 的 click 监听改为 async，`copy-magnet` 分支复制成功后按钮显示「已复制磁力链接」+ toast 且**不调用 done()（弹窗保持打开，可继续选任务）**，失败显示原因并 1400ms 后复原。测试 `tests/test_magnet_helper_userscript.py` 12→15 项（弹窗含复制入口、torrent 转换提示、点击复制写入剪贴板且弹窗仍开、未选中任务）；完整 unittest 805 项、`node --check`、`git diff --check`、version.json 解析均通过；Docker 未重建，浏览器实测（含 iOS Userscripts 剪贴板权限）待补。
- 2026-09-18 | `main` 未提交 | 发布 0.11.6：接收夹快捷导入（最小改动接入现有工作流）。需求：只要「接收 + 电影监控 + 电视剧监控」三个文件夹，推到接收夹后自动「识别（TMDB+AI）→ 整理重命名 → 按电影/剧集搬到对应监控目录」，导入时不再手选 savepath，且与监控侧整理统一流程、不能重复/冲突。实现：(1) 统一流程——把 `monitor._auto_scrape_new_media_items` 里的 scan→identify→过滤→plan 抽成 `scraper.identify_scraper_batch_entries` + `scraper.build_scraper_plan_for_batch`（`build_scraper_organize_plan` 为便捷封装），监控自动刮削与快捷导入共用；过滤口径统一为 `status == auto` 或 AI 已采纳（AI 由 `ai_match_enabled`/`ai_match_min_confidence` 把关，未开 AI 行为不变），整理选项一律取目标监控任务的 `auto_scrape_options`。(2) 防重复——`move_scraper_entries` 新增可选 `source_action`（默认 `scraper:entry:move` 不变），快捷导入传 `scraper-job:<job_id>:quick-import`；把 monitor_changes 里的守卫抽成 `_collect_event_new_media_items`（`scraper-job:` 来源不计入 `new_media_items`），目标监控任务因此只生成 STRM 不再二次整理；UI 直接移动行为不变。(3) 配置与入口——全局 `quick_import_enabled`/`quick_import_inbox_path`（监控任务页顶部区域：开关 + 接收文件夹选择 + 立即整理并分发 + 最近一次结果），监控任务新增 `quick_import_target`（不使用/电影/电视剧，卡片 intro 与弹窗都展示），校验拒绝接收夹与任何监控 scan_path 重叠。(4) 触发——`routes/resource.py` 两处导入入口用 `is_quick_import_savepath` 标记任务 extra.quick_import_inbox 并放开 auto_refresh（接收夹无监控任务时原本被强制 False），`services/resource.py` 离线 watcher 放行这类任务、status==2 分支与分享转存收尾分支改走 `quick_import.run_quick_import`（支持 refresh_delay_seconds 延迟执行）。(5) 新增 `app/services/quick_import.py`（配置/校验/编排/落库 `quick_import_runs` 表）+ `GET /scraper/quick-import/status`、`POST /scraper/quick-import/run` + CLI `monitor quick-import-status|quick-import-run`；前端 monitor 页区块、监控任务字段、文件选择器支持第二目标输入框、boot 启动拉状态。测试：新增 `tests/test_quick_import.py` 26 项（配置/校验/留守原因/防重复守卫与来源透传/共享流程/搬运成功·目标缺失·计划冲突·搬运失败/落库）与 `tests/test_quick_import_frontend.py` 9 项（页面与弹窗标记、index.js 全局函数与接口、选择器复用、settings 收集、boot 加载、后端路由与导入钩子、CLI 动作）；完整 unittest 840 项、compileall、py_compile cli.py、改动 JS `node --check`、`git diff --check`、version.json 解析均通过；Docker 未重建，NAS 端真实 115 链路（导入完成触发、搬运后只生成 STRM、无二次刮削）待实测。
- 2026-09-18 | `main` 未提交 | 发布 0.11.7：修复"散文件没有被整理成媒体文件夹"。用户实测 0.11.6 后反馈：接收夹快捷导入把 `追杀51号(2025)-2160p….mkv` 等散文件搬进影库，但既没建 `片名 (年份)/` 也没改文件名。排查确认三个根因：(1) `build_scraper_rename_plan` 里 `organize_into_media_folder` 只对"选中文件夹"生效（`folder_mode = selection_mode == "folder"`），选中散文件时 `preserve_source_parent_path=True`，只会在原地改名 → 新增 `force_media_folder` 选项，`plan_options["organize_into_media_folder"] = folder_mode or force_media_folder`，`preserve_source_parent_path = not organize_into_media_folder`；(2) keep/clean 模式（`file_name_mode in (keep, clean)`）会走"文件不移动"的捷径 `organize_root = source_relative_parent_path` → `_build_scraper_target_path` 里电影与剧集两条分支都加了 `and not force_media_folder`，强制归档时仍建媒体文件夹、文件名按原设置保留；(3) 更危险的一条：监控自动刮削把"新文件所在父目录"当条目（`_auto_scrape_new_media_items` 里 `parent_rel = dirname(mount_rel)`），当文件直接躺在监控根目录时 `parent_rel == scan_path`，会把**监控目录本身**当成待改名文件夹 → 改为这类文件走独立的 `root_file_items` 分支，构造 `is_dir=False` 的文件条目（`parent_path=监控目录`、`path=挂载相对路径`），不碰监控目录。另修：同剧集散文件被扫描合并成一个条目时 `build_scraper_plan_for_batch` 漏传 `entries`（只整理第一个文件）→ 现在透传整组。执行器侧确认无需改动：`_walk_existing_folder` 找不到目标目录返回 `("", False)`，`new_parent_id` 为空时执行器会 `_ensure_folder_from_base(..., "0", target_parent_path)` 自动建目录。接收夹快捷导入与监控自动刮削都带上 `force_media_folder=True`，两条路径行为一致。测试 `tests/test_quick_import.py` 26→30 项（散文件强制归档、keep 模式建文件夹但保留文件名、不强制时只改名的对照、监控根目录散文件按文件条目、子目录新文件仍按文件夹条目、计划透传 entries）；完整 unittest 844 项、compileall、改动 JS `node --check`、`git diff --check`、version.json 解析均通过；Docker 未重建，NAS 端需用真实散文件复测"进库后是 片名 (年份)/… 结构"。
- 2026-09-18 | `main` 未提交 | 修复"重复整理同一部片会新建文件夹"：用户反馈一部剧的多个单集文件整理后散在 `片名 (2026)`、`片名 (2026) [tmdbid-328704]`、`片名 (2026)(1)/(2)/(3)` 五个文件夹里。查真实运行库（`scraper_job_actions`）确认：快捷导入把每个单集各自整理成 `接收夹/片名 (年份)/`，再整目录搬进监控目录；目标目录里已有同名文件夹时 `115` 会把新搬来的文件夹自动改名成 `片名 (年份)(n)`，一次导入 4 集就散成 4 个文件夹（job 98/101/102/103/104 的 `new_parent_id` 互不相同即证据）。修复分三层：(1) **复用现有文件夹**——`scraper.py` 新增 `scraper_folder_name_key()` / `_match_scraper_folder_part()` / `_find_scraper_folder_part()` / `find_scraper_media_folder()`：查找目录时精确同名优先，其次忽略 `[tmdbid-xxx]` 与 `115` 自动追加的 `(n)` 装饰（`_walk_existing_folder` 计划期、`_ensure_folder_from_base` 执行期都改用它，避免同名文件夹被 `_create_provider_folder` 再建一个）；(2) **分发时合并而不是再搬一个文件夹**——`quick_import.py` 新增 `_dispatch_organized_entry()` / `_merge_organized_folder_into_existing()`：目标监控目录已有同名文件夹时，把整理结果里的文件并进那个文件夹（子目录如 `Season 01` 按同层递归合并），只在目标没有时才保持原来的整目录搬运；并进时按目标文件夹现有条目名挡重复文件（命中就留在接收夹并写入原因，不再产生 `xxx(1).mkv`），全部并完后删除接收夹里的空壳文件夹。(3) **散文件整理沿用监控任务的命名形状**——`build_scraper_rename_plan` 里 `if not folder_mode:` 分支原本无条件把 `include_tmdb_id`/`use_season_subfolder` 改回 False（那是 0.11.7 之前"散文件只原地改名"的旧行为），结果 0.11.7 的 `force_media_folder` 强制归档仍然不写 TMDB ID、不加 `Season 01`：同一部剧"选文件夹整理"得到 `片名 (年份) [tmdbid-123]/Season 01/…`，而接收夹/监控根目录的散文件却整理成 `片名 (年份)/…`（用户库里 E04/E05/E08 在 `Season 01/`、E01-E03/E06 散在片名文件夹根目录，就是这个原因）。现改为仅在未强制归档时才回落旧行为。监控同步事件仍带 `scraper-job:<job_id>:quick-import` 来源，不会触发目标监控任务二次自动刮削。测试：新增 `tests/test_scraper_folder_reuse.py` 7 项（装饰名归一化/同名候选排序/计划期与执行期复用/md 不误配）+ `tests/test_quick_import.py` 新增 4 项（单集文件并入已有文件夹、重复文件留守接收夹、目标无同名文件夹时行为不变、`Season 01` 递归合并、强制归档沿用任务命名选项且未强制时行为不变）；完整 unittest 856 项、compileall、`git diff --check` 均通过；已按项目代理配置 `docker compose up -d --build` 重建容器，并在真实 115 账号只读复核：`find_scraper_media_folder` 对 `王子与乞丐 (2026)` 命中现成文件夹、对 `(9)` 这类装饰名也能归一化命中、对 `第2季` 不误配；用真实 TMDB 绑定跑了一次“不落盘”的快捷导入计划，新目标路径已与监控任务设置一致（`王子与乞丐 (2026) [tmdbid-328704]/Season 01/王子与乞丐 (2026) - S01E01.mkv`）。下一步：用户下次真实跑一次快捷导入确认多集并入同一文件夹（并手动清理历史遗留的 `[tmdbid-…]`/`(n)` 重复文件夹），确认后按用户意愿补 `0.11.8` 版本元数据并提交/推送。
- 2026-09-18 | `main` 未提交 | 修复"同一部剧整理到多处 + 还有一个没搬过去"（承接上一条）：用户把 S01 的 5 个碎片文件夹放回接收夹再跑快捷导入，结果 `115自存电视剧/王子与乞丐 (2026) [tmdbid-328704]/` 里多出一个同名嵌套文件夹（装着 E04/E05/E08），E02/E03/E06 正确并进了 `Season 01`，而 E01 那个文件夹因为"当前目录中已有同名文件夹"留在接收夹。查 `scraper_job_actions` 定位到两个独立根因：(1) **嵌套**——监控自动刮削把"新文件的父目录"当条目（`_auto_scrape_new_media_items` 里 `parent_rel = dirname(mount_rel)`），当新文件已经躺在 `片名 (年份) [tmdbid-x]/Season 01/` 里时，条目就是 `Season 01`、锚点是 `片名 (年份) [tmdbid-x]`，`_scraper_folder_organize_root` 再加一层片名就变成 `…/片名 (年份) [tmdbid-x]/片名 (年份) [tmdbid-x]/`（job 106 即此）；(2) **留守**——快捷导入逐条"先改规范名再搬"，同批多个同名碎片文件夹会互相撞名（先处理的那条想改成 `片名 (年份) [tmdbid-x]` 时该名字已被同批另一条占用），冲突条目只能留在接收夹。修复：新增 `_scraper_existing_media_folder_root()`，`_build_scraper_target_path` 先判断"文件是否已经在最外层同名媒体文件夹里"，是的话就地锚定到那个文件夹（不再套层），剧集分支相应地在命中时不再因 `organize_inside_source_folder` 把文件从 `Season 01` 挪回片名根目录——因此"已整理好的文件"再次进入自动刮削会判定 `unchanged`（0 动作），历史嵌套结构也会被拉平回 `Season 01/`；`quick_import.run_quick_import` 改为先解析目标监控目录，若目标已有同名文件夹（含 `[tmdbid-…]`/`(n)` 归一化命中）就只对该条目关掉 `rename_selected_folders`（接收夹文件夹只是中转，直接合并即可），目标目录解析失败改为单条进 `left` 不再中断整轮。测试：`tests/test_quick_import.py` 新增 3 项（已在媒体文件夹/Season 内不再嵌套且计划 0 动作、目标已有文件夹时跳过接收夹改名、目标无文件夹时保持原流程），完整 unittest 859 项、compileall、`git diff --check` 均通过；已重建容器并用真实绑定只读复核：`…/Season 01/E04.mkv` 规划 0 动作、已嵌套的 `…/同名/E04.mkv` 规划拉平回 `…/Season 01/E04.mkv`、接收夹 `王子与乞丐 (2026)` 在目标已有 `王子与乞丐 (2026) [tmdbid-328704]` 时计划无冲突（目标 `最近接收/王子与乞丐 (2026)/Season 01/王子与乞丐 (2026) - S01E01.mkv`，分发时并入现成文件夹）。下一步：用户重跑一次快捷导入即可把 E01 并回 `Season 01` 并清掉嵌套文件夹；确认后按用户意愿补 `0.11.8` 版本元数据并提交/推送。
- 2026-09-18 | `main` 未提交 | 修复"监控自动刮削把已整理好的剧集又套一层 + 同名文件夹冲突让条目留守"（承接上一条，用户 10:28 复测又出现同一现象）：这次两份证据把两个根因钉死了——(1) 监控自动刮削（`monitor._auto_scrape_new_media_items`，条目是"新文件的父目录"= `Season 01`，`parent_path` = 剧集文件夹）进 `build_scraper_rename_plan` 时调用方没传 `base_path`，函数会用"选中文件夹的父目录"兜底 `base_path`，于是 `base_path` 被收缩成剧集文件夹本身；上一版 `_scraper_existing_media_folder_root()` 先按 `base_path` 做相对化，剧集文件夹这一层被裁掉后就再也匹配不到同名文件夹，`_scraper_folder_organize_root()` 于是又叠出一层 `…/片名 (年份) [tmdbid-x]/片名 (年份) [tmdbid-x]/`（job 110 现场复现、并在容器里 1:1 复刻成功）。改为"按完整挂载路径从最外层匹配、最后再折算回 `base_path` 相对路径"，并用 `None`（没找到）/`""`（base_path 本身就是该媒体文件夹）区分两种语义；顺带把 `_target_name_exists()` 拆出 `_find_scraper_entry_by_name()` 复用同一套分页查找。(2) 接收夹里同批/同目录存在"同一部剧的另一个名字"文件夹时（`王子与乞丐 (2026)` vs `王子与乞丐 (2026) [tmdbid-328704]`），文件夹改名必然撞车，旧逻辑直接报"当前目录中已有同名文件夹"把整个条目留在接收夹——新增 `_is_same_show_folder_conflict()`：撞名的对方是文件夹且两侧名字归一化键相同（仅 `[tmdbid-…]`/`(n)` 装饰差异）时不再改名也不再报冲突，文件就地整理进自己的 `Season 01`，交给搬运/合并阶段并进现成文件夹；查不到或不是同剧时保持原冲突行为。测试：`tests/test_quick_import.py` 新增 2 项（监控形态的 folder 条目 + 调用方不传 base_path 时判定 `unchanged` 不再嵌套；同剧装饰名撞名不再让条目留守且计划无文件夹改名动作），完整 unittest 860 项、compileall、`git diff --check` 均通过；已重建容器并用真实 115 数据只读复刻：10:29 那次产生嵌套的输入现在规划为 `ready 0 / unchanged 6 / 无 issue`。下一步：用户真实重跑一次把库里的 `王子与乞丐 (2026) [tmdbid-328704]/{Season 01(空)、王子与乞丐 (2026) [tmdbid-328704]/*.mkv(6集)、S01E01.mkv(散)}` 拉平成单层 `Season 01/`（可在刮削页勾选该剧集文件夹点"批量整理"，或由 agent 走同一套计划执行）；确认后按用户意愿补 `0.11.8` 版本元数据并提交/推送。
- 2026-09-18 | `main` 未提交 | 统一「接收夹快捷导入」区块与页面既有风格：用户反馈这块"风格不太一样"。根因是状态面板用了一组行内 tailwind 色值（`border-sky-500/30 bg-sky-500/10 text-sky-200` 运行中、`border-amber-500/30 bg-amber-500/10 text-amber-200` 配置未就绪、`border-slate-700 bg-slate-900/60` 空闲/最近一次），而项目里同类面板（设置页 `#ai-match-test-status`/`#ai-match-usage`、`#notify-test-status`、`#tg-proxy-test-status`、`#pansou-test-status`）统一用 `tg-proxy-status` + `--loading/--success/--error` + `tg-proxy-status-title/meta/note`，只有这套在 `index.css` 里有 `html.theme-day` 翻转——所以快捷导入面板夜间正常、日间发灰/对比度不足，看起来"另一个风格"。改动：模板 `#quick-import-status` 改为 `class="tg-proxy-status hidden mt-4"`，`renderQuickImportStatus` 抽出 `showQuickImportStatus(modifier, html)` 统一套 `tg-proxy-status`（运行中 `--loading`、配置未就绪/最近一次失败 `--error`、其余基础态）并按 title/meta/note 结构排内容（"最近一次：xxx"+时间/触发+已分发/留在接收夹列表）；主按钮 `立即整理并分发` 从 `py-3` 对齐同页 `+ 新增任务` 的 `py-2 … min-h-[42px]`（同页其它 emerald 按钮也都是 py-2）；状态面板与网格的外边距统一到 `mt-4`/`mt-5`。测试：`tests/test_quick_import_frontend.py` 新增 1 项（状态面板类名、按钮类名、JS 使用 tg-proxy-status 三种状态且不再夹带行内配色），完整 unittest 861 项、改动 JS `node --check`、`git diff --check` 均通过；已重建容器并用带会话 Cookie 的 curl 核对真实渲染 HTML（`#quick-import-status` = `tg-proxy-status hidden mt-4`、按钮含 `min-h-[42px]`）。注：本机沙箱内 Chromium 无法启动（mach port rendezvous 权限被拒），因此这次没做截图级实测，日/夜观感待用户在浏览器确认。下一步：用户确认是否就是这块（若指参数页 AI 区块/刮削页批量整理面板，按同一套规范再统一）；确认后按用户意愿补 `0.11.8` 版本元数据并提交/推送。
- 2026-09-18 | `main` 未提交 | 按前端设计规范重排「接收夹快捷导入」卡片（承接上一条，用户反馈"布局不好看"）：原版是"标题—长段落—启用—字段—按钮—提示—结果面板"的单列堆叠，宽屏右半边整片留白，启用开关和标题不在一条视觉线上，结果面板又被压在整卡最底部。改为 **左侧配置 / 右侧最近一次结果** 的两栏构图（`grid grid-cols-1 lg:grid-cols-2 gap-4 mt-5`，窄屏自动堆叠）：左列依次是「接收文件夹」选择器（沿用 `.resource-save-path-grid`，选择按钮不动）、「分发目标」chips（新增 `.quick-import-targets`/`.quick-import-chip`，蓝色主操作 + 绿色执行两个色仍是项目既有系统，未新增第三种强调色）、执行行（emerald 主按钮 + `text-[11px]` 单句提示）；右列固定为 `tg-proxy-status quick-import-result` 面板（限高 15rem 内部滚动）。文案按规范删减：说明从 3 句压到 1 句（`max-w-3xl`），"整理规则沿用目标监控任务"这类实现细节从副标题挪出，提示压成一句"手动拖进接收夹不会自动触发；磁力 / 分享导入完成后会自动跑一次"。交互上新增两处轻动效并尊重 `prefers-reduced-motion`：结果面板每次刷新淡入上移 3px（`.quick-import-result--enter`），chips 悬浮描边提亮；`trigger` 原样显示的 `manual` 改为「手动」。测试：`tests/test_quick_import_frontend.py` 的样式用例同步更新（两栏 grid、`quick-import-result`、chips 结构与 CSS 存在性、`prefers-reduced-motion`），完整 unittest 861 项、`node --check static/js/index.js`、`git diff --check` 均通过；按项目脚本重跑 `npm run build:css` 重新生成 `tailwind.generated.css`（新增 `ml-5` 等，同时补上此前缺失的 `bg-white`，删除确实无人引用的 `col-span-*`/`border-2` 等）。**可视化验证**：本机沙箱里 Chromium 与 playwright-cli 都起不来（mach port rendezvous 权限被拒），改走 Codex 内置浏览器（先本地起临时页写会话 Cookie，再打开 127.0.0.1:18080，用完已关标签并停掉临时服务）实测：桌面 1280 宽夜间/日间、390 窄屏三档都渲染正常——日间计算样式 `body #eef4fb / card #f8fbff / chip rgba(248,250,252,.96)`，chips 与结果面板配色正常、窄屏正确堆叠（启用开关换行到独立一行、按钮 138×42、结果面板 331 宽）。下一步：用户刷新页面确认观感（含日夜两档），若还想调密度/文案再迭代；确认后按用户意愿补 `0.11.8` 版本元数据并提交/推送。
- 2026-09-18 | `main` 发布 | 发布 `0.11.8`：同步 version.json（0.11.8 + 5 条说明）、CHANGELOG（新增 `[0.11.8]` 段：四个整理根因 + 散文件命名形状 + 快捷导入区块重排 + 验证）、README（当前版本与近期更新首条）与本交接记录。本版收录：同一部剧重复整理不再新建重复文件夹（命名装饰归一化复用 / 分发并进现成文件夹 / 已在媒体文件夹内不再嵌套且可拉平历史嵌套 / 同剧装饰名撞名不再留守）、散文件强制归档沿用监控任务命名形状（TMDB ID + Season 子目录）、监控任务页「接收夹快捷导入」两栏布局与状态面板统一。完整 unittest 861 项、compileall、改动 JS `node --check`、`git diff --check`、version.json JSON 解析、`npm run build:css` 重新生成样式均通过；容器已重建并用真实 115 数据只读复核 + 内置浏览器（桌面日/夜、390px）实测。下一步：观察下一次真实快捷导入是否直接并入现成文件夹（并清理历史遗留的嵌套/重复文件夹）；如需把遗留结构一次性拉平，可在刮削页勾选该剧集文件夹点「批量整理」。
- 2026-09-18 | `main` 未提交（并入 `0.11.8`） | 新增「接收夹专属 webhook」`POST /webhook/quick-import`：需求是"磁力链接直接推进待整理接收夹"，而原 webhook 按监控任务绑定（`/webhook/{任务名}` 要求名字必须是存在且开启 webhook 的监控任务），脚本因此只能推监控目录。按用户要求做成**独立微 hook**：新路由放在 `{task_name}` 路由之前（否则会被路径参数吞掉），并从原磁力分支抽出共享函数 `_create_userscript_magnet_job()`（两条路由共用建任务/查重/日志/响应逻辑，行为只有一份）；新端点前置校验 `validate_quick_import_config()`（未启用/未配接收夹/没有电影电视剧目标 → 400 并写明原因），鉴权沿用同一套 `webhook_secret`（token 或 ts+nonce 签名），`savepath` 留空则用配置的接收夹、填接收夹或其子目录允许、填到别处直接 400，请求体只接受磁力，创建的任务带 `extra.quick_import_inbox=1` + `webhook_target=quick-import`，去重沿用"链接 + savepath"（同条磁力重复推送 409）；油猴脚本无需改动（用户自行新增一条任务、请求地址填 `http://IP:端口/webhook/quick-import`）。测试：新增 `tests/test_monitor_webhook_quick_import.py` 10 项（未启用/无目标/路径越界/非磁力/缺签名分别 400·401、token 放行、成功建任务且标记正确、留空 savepath 回落接收夹、重复 409、原 `/webhook/{任务名}` 行为不变含任务级延时）；完整 unittest 871 项、compileall、`git diff --check`、version.json 解析通过；容器已重建，并用真实配置实测：无签名 401、带 token 非磁力 400「只接受磁力链接」、越界 savepath 400「必须落在接收夹 最近接收 内」、不存在的任务名仍 404（原推送未受影响），探针都在校验阶段失败、不产生真实离线任务。README Webhook 章节、version.json 说明与 CHANGELOG 已同步。下一步：用户在油猴脚本里新增该任务实测一条真磁力（接收夹 → 识别 → 分发），确认后按原计划提交推送 0.11.8。
- 2026-09-18 | `main` 未提交（并入 `0.11.8`） | 补上「接收夹专属 webhook」的界面入口（用户反馈"光有接口没提示，用户不知道也不会配"）：在「文件夹监控」页「接收夹快捷导入」卡片左列新增一行「接收夹专用 Webhook（磁力直推这里）」——只读输入框显示 `${location.origin}/webhook/quick-import`（自动跟随 HTTP 端口/HTTPS 反代域名，`renderQuickImportWebhookUrl()` 在每次状态刷新时写入）+ 「复制地址」按钮（`copyQuickImportWebhookUrl()`，`navigator.clipboard` 失败时降级 `showAppPrompt` 手动复制，并挂到 `window` 供 onclick 调用），下面一句说明"在油猴脚本里新增一条任务：请求地址填这个地址，保存路径填接收夹（或留空）"；参数配置页第 8 节「油猴脚本和文件夹监控的关系」补一条：该地址独立于任务、不受"保存路径必须在扫描路径内"限制。测试：`tests/test_quick_import_frontend.py` 增至 11 项（新增"入口可发现"用例：卡片输入框/复制按钮/`location.origin` 拼地址/window 挂载/配置页说明），完整 unittest 872 项、`node --check`、`npm run build:css`、`git diff --check`、version.json 解析通过；容器已重建，内置浏览器实测：卡片地址显示为 `http://127.0.0.1:18080/webhook/quick-import`、点「复制地址」出现"已复制接收夹 Webhook 地址…"成功提示、桌面与窄屏布局正常。CHANGELOG/README/version.json 已同步。下一步：用户按界面提示在油猴脚本里加这条任务并实测一条真磁力，确认后按原计划提交推送 0.11.8。
- 2026-09-18 | `main` 未提交（并入 `0.11.8`） | 「接收夹快捷导入」卡片折叠为二级弹窗（用户反馈"别全摊在页面上，收进二级弹窗/小提示"）：卡片只留标题 + 「启用」开关 + 两行摘要（`renderQuickImportSummary()`：`接收夹：<路径> · 分发目标：电影 → X / 电视剧 → Y` 与 `最近一次：<summary>（时间）`，未启用/配置未就绪/运行中分别有对应文案）+ 「立即整理并分发」「设置 / 推送地址」两个按钮 + 一句触发说明，卡片高度 460px→约 190px；新增 `templates/partials/modals/quick_import.html`（已加入 index.html include，沿用 `group-card modal-card` + `showLockedModal`/`hideLockedModal` 锁滚动机制与 `resource-source-modal-close` 关闭按钮），弹窗内左列＝接收文件夹 + 分发目标 chip、右列＝最近一次结果面板、下方整行＝接收夹专用 Webhook 地址（完整可见）+ 复制 + 用法说明，底部＝关闭 / 立即整理并分发；`runQuickImport()` 改为同时驱动卡片与弹窗两个执行按钮的忙碌态，新增 `openQuickImportSettings()`/`closeQuickImportSettings()` 并挂 window。测试：`tests/test_quick_import_frontend.py` 结构用例同步更新（卡片摘要+设置入口、弹窗内 4 个 id、index.html include、两个新函数与新按钮），完整 unittest 872 项、`node --check`、`npm run build:css`、`git diff --check`、version.json 解析通过；容器已重建，内置浏览器实测：卡片两行摘要 + 折叠后高度约 190px、点「设置 / 推送地址」弹出弹窗、地址完整显示 `http://127.0.0.1:18080/webhook/quick-import`、复制成功提示正常。CHANGELOG/README/version.json 已同步。下一步：用户按弹窗里的提示在油猴脚本加任务并实测真磁力，确认后按原计划提交推送 0.11.8。
- 2026-09-18 | `main` 未提交（并入 `0.11.8`） | 修复日间模式"浅底白字看不见"（用户反馈配色问题）+ 夜间对比度复核：日夜翻转规则 `html.theme-day .bg-slate-700/.bg-slate-700\/40/.bg-slate-600 { background: var(--surface-soft) !important; }` 只翻背景不翻文字，而全仓这类深灰底都配 `text-white`/`text-slate-200`，于是新弹窗里的「复制地址」「关闭」在日间变成浅底白字——内置浏览器实测对比度 **1.09:1**（等于看不见）。修复：全局规则补 `color: #334155 !important`，并补 `.hover\:bg-slate-600:hover` / `.hover\:bg-slate-700:hover` 的日间配色；已有作用域例外（`#page-monitor button.bg-slate-700`/`#monitor-modal`/`#resource-import-modal` 等）特异性更高故不受影响，修复后 9.54:1。夜间复核：把新弹窗三个标签从项目惯例 `text-slate-500`（深色卡片上 3.77:1）提到 `text-slate-400`（6.99:1，日间同映射 `var(--muted)` 观感不变）；其余元素实测 ≥5.35。仍低于 4.5 的只剩全站 emerald 主按钮（白字 #059669 = 3.77:1，全站一致，未动，可另议是否统一调深）。验证：`npm run build:css` 重生成样式、完整 unittest 872 项、`git diff --check`、version.json 解析通过；容器已重建，内置浏览器日夜两套逐元素（标题/摘要/按钮/标签/输入框/结果面板/chip）实测并截图确认，用后已关标签与临时服务。CHANGELOG 已补「日间浅底白字」修复段，version.json 说明同步。
- 2026-09-18 | `main` 未提交（并入 `0.11.8`） | 把「接收夹」并入任务列表（统一任务 / 路径 / webhook 口径）：用户先报了三个问题——快捷导入选文件夹的二级弹窗被父弹窗盖住、"油猴脚本保存路径是否和文件夹监控任务同一套口径"、选了文件夹仍显示未启用；随后的评审定下最终形态：**接收夹就是一个任务**。(1) 弹窗层级：模板里的 `z-[N]` 改为只当基线，`index.js` 新增 `stackModalLayer()`/`readModalLayerBase()`/`isModalOpen()`，每次 `showLockedModal` 按「当前可见弹窗基线 +1」抬升（上限 94，95 留给应用对话框），选择文件夹从快捷导入弹窗打开时会算到 59 压住父弹窗，反复开关不膨胀；用真实模板 z 值与真实函数做了离线模拟验证四种场景。(2) 任务模型：`core.normalize_task` 新增 `task_type`(scan/inbox)/`enabled`/`distribute_targets`，新增 `migrate_legacy_inbox_task()` 把旧全局配置 `quick_import_enabled`/`quick_import_inbox_path` 与扫描任务上的 `quick_import_target` 一次性迁移成接收夹任务再删除旧字段（幂等），新增 `get_inbox_task()`/`list_scan_tasks()`；`match_monitor_task_for_savepath` 跳过 inbox 任务，`startup.monitor_scheduler` 跳过 inbox 任务。(3) 入口统一：删除 `POST /webhook/quick-import`，`/webhook/{任务名}` 按任务类型分发（inbox 走 `_handle_inbox_webhook`，只接磁力、savepath 必须在接收夹内），`/monitor/start` 对 inbox 任务等价于「立即整理并分发」。(4) 路径统一：新增 `normalize_userscript_savepath()`（按挂载前缀映射把 `/115/接收` 剥成 `接收`）与 `is_relative_path_within()`；扫描任务 webhook 现在也校验 savepath 落在任务目录内（旧行为是静默下到 115 根目录下的 `115/` 文件夹）。(5) 日志合并：新增 `write_monitor_log_sync()`（`write_monitor_log` 改为它的异步包装），接收夹整理收尾写进同一份监控日志。(6) 界面：删除 `templates/partials/modals/quick_import.html` 与监控页「接收夹快捷导入」卡片、settings.js 里的两个全局字段；任务编辑弹窗新增类型选择、启用开关、接收夹分发目标下拉、webhook 地址（可复制）、「立即整理并分发」、最近接收/最近整理面板（`/scraper/quick-import/status` 新增 `recent_jobs`/`recent_job_count_24h`），任务卡片带「接收夹」标记且运行按钮语义为「立即整理」；settings.html 与 README 的 webhook 说明同步改写。测试：`tests/test_quick_import.py` 41 项（迁移幂等/类型与 enabled 归一化/分发目标映射/校验消息/重叠校验）、`tests/test_monitor_webhook_quick_import.py` 13 项（新增挂载前缀 savepath 归一化、扫描任务越界 400、扫描任务前缀归一化）、`tests/test_quick_import_frontend.py` 11 项（接收夹是任务类型、旧卡片/弹窗/字段已删除、弹窗层级、根目录相对路径文案）全部重写，完整 unittest 877 项、compileall、改动 JS `node --check`、`git diff --check`、version.json JSON 解析均通过。Docker 守护进程未启动，因此没有容器/浏览器实测：待 `docker compose up -d --build` 后在浏览器确认「任务列表出现接收夹任务卡 + 编辑弹窗字段切换 + 选择文件夹层级 + 日志混排」，并用油猴脚本真磁力跑一次接收夹 → 识别 → 分发链路。
- 2026-09-18 | `main` 未提交（并入 `0.11.8`） | 接收夹改成内置固定槽位（承接上一条，用户反馈"接收文件夹只需要一个就好了，它主要就是为了方便呀，所以默认有一个就可以了"）：`core.migrate_legacy_inbox_task()` 改名为 `ensure_inbox_task()`，语义从"有旧配置才迁移"变成"保证始终有且只有一个接收夹任务"——全新配置直接补一个默认的「接收」（`enabled=True` + `webhook_enabled=True` + 空路径，界面会直接提示"配置未就绪：请先给接收夹任务「接收」选择文件夹"），已存在多个则只保留第一个，用户手动关掉的 webhook 不会被归一化重新打开；空路径保持 `""` 而不是归一化成 `/`。配套收口唯一性：`/monitor/delete` 对 inbox 任务返回 400「接收夹任务是内置的，不能删除；不想用时把「启用本任务」关掉即可」（否则删掉会被配置归一化立刻补回并丢失已配路径），任务卡片不再渲染删除按钮；`syncMonitorTaskTypeOptions()` 在有内置接收夹时禁用「新增任务」里的接收夹类型选项（文案改为"已内置，直接编辑任务列表里那个"）。验证：`tests/test_quick_import.py` 增至 43 项（默认即内置、多个只留一个、已配置的接收夹归一化后原样保留且不重开 webhook）、`tests/test_quick_import_frontend.py` 增至 12 项（无删除按钮 / 类型选项互斥 / 服务端拒绝删除），完整 unittest 880 项、compileall、改动 JS `node --check`、`git diff --check` 均通过；另用临时配置起真实服务复核：全新配置下 `/monitor/status` 返回单个 `接收` inbox 任务（enabled + webhook 均 true、path 空），`/scraper/quick-import/status` 返回 `config_error="请先给接收夹任务「接收」选择文件夹"`，删除请求被 400 挡下。待 `docker compose up -d --build` 后在浏览器确认任务列表与编辑弹窗观感。
- 2026-09-18 | `main` 未提交（并入 `0.11.8`） | 评审后的修订（用户逐条拍板）：(1) **webhook 默认关闭 + 密钥门控**——`ensure_inbox_task` 的内置接收夹 `webhook_enabled=False`，迁移旧配置时只有设置了 `webhook_secret` 才开；`webhook_secret` 加入 `config_runtime._STATIC_SENSITIVE_KEYS`（`/get_settings` 不再明文回传、`sensitive_configured` 带出是否已配置），`index.js` 的 `getSensitiveSettingFields` 补 `webhook_secret`，编辑弹窗新增 `#webhook-secret-hint` 且 `syncWebhookToggleState()` 在没密钥时禁用「开启 webhook」。(2) **停用语义收紧**——`queue_monitor_job` 对非 manual 触发解析任务、`enabled=False` 返回 `disabled` 不入队；`/webhook/{任务名}` 对停用的扫描任务返回 400「该任务已停用…」（inbox 仍走 `validate_quick_import_config` 自己的未启用提示）；编辑弹窗「启用本任务」info-dot 说明运行方式与停用影响。(3) **接收夹定时整理**——`startup.monitor_scheduler` 不再跳过 inbox，`cron_minutes>0` 且启用时到点 `asyncio.to_thread(run_quick_import, "cron")`；`_handle_inbox_webhook` 把任务 `delay_seconds` 作为 `task_delay_seconds` 传入。(4) **日志分段修复**——`run_quick_import` 在锁内写 `━━━━【任务开始 | 接收 | 触发】━━━━`/`任务结束` 两条 `task-divider`（`write_inbox_divider` 捕获写盘异常），让接收夹整理在监控日志里成为独立任务分段（实测 `build_monitor_log_segments_from_entries` 得到 `('task','接收')`）。(5) **保存兜底**——`save_monitor_tasks` 漏传接收夹时沿用当前 `get_inbox_task(cfg)`，不再被归一化补成空路径。(6) **界面收口**——模板把「重试次数/列出延时/大小过滤」移进 `#monitor-scan-fields`，`delay_seconds`/`cron_minutes` 留在外面（接收夹也生效），`refreshWebhookHint` 追加"改名会改地址"提示。小清理：删死代码 `list_scan_tasks()`，`write_monitor_log_sync` 的内存日志写加 `_monitor_log_lock`。验证：`tests/test_quick_import.py` 增至 46 项、`tests/test_quick_import_frontend.py` 增至 15 项，完整 unittest 885 项、compileall、改动 JS `node --check`、`git diff --check`、version.json 解析均通过；另用脚本实测：空接收夹整理日志形成 `('task','接收')` 分段、停用任务 resource 触发返回 `disabled` 而 manual 返回 `started`。待 `docker compose up -d --build` 后浏览器复核「webhook 密钥门控 / 接收夹定时整理 / 日志分段」。
- 2026-09-18 | `main` 未提交（并入 `0.11.8`） | 评审收尾（用户选 7A + 9A）：(7A) savepath 越界仍保持 400，但按场景补了修改提示——`/webhook/{任务名}` 里磁力提示「磁力必须指定保存路径，请改成任务目录内的路径」，非磁力（分享转存/刷新）提示「只想触发刷新、不需要指定下载目录，可以不传 savepath」；接收夹越界提示补「留空则默认落到接收夹」。(9A) 接收夹任务卡片上的「最近接收 24 小时 N 个 · 最近整理：…」跟随页面既有状态轮询按 30 秒节流自动刷新：`index.js` 新增 `inboxStatusFetchedAt` / `INBOX_STATUS_REFRESH_INTERVAL_MS` / `maybeRefreshInboxTaskStatus()`，并在 `applyMonitorState` 的两条落地路径（tab 模块 `afterApply` 回调 + 兜底分支）各调一次；`refreshInboxTaskStatus()` 显式调用仍立即拉取。测试：`tests/test_monitor_webhook_quick_import.py` 增至 15 项（新增非磁力越界提示、磁力/接收夹提示断言），`tests/test_quick_import_frontend.py` 增至 16 项（新增自动刷新节流断言）。完整 unittest 886 项、`compileall`、`node --check`、`git diff --check`、version.json 解析通过；待 `docker compose up -d --build` 后浏览器复核「越界报错文案 / 卡片自动刷新」。
- 2026-09-18 | `main` 未提交（并入 `0.11.8`） | 任务类型锁死（用户反馈"接收文件夹不应该能选择并更改为另外的类型，另外类型的文件夹也不应该能改成接收文件夹；新增的也只是普通的监控文件夹"）：(1) 服务端新增 `core.apply_task_type_constraints(existing, posted)`——按任务名强制沿用原类型（扫描不会变 inbox、内置 inbox 不会变 scan），提交里出现的新 inbox 类型一律降级成 scan，并清掉非 inbox 任务的 `distribute_targets`；唯一放行场景是"当前接收夹名字从提交列表消失且提交里恰好一个 inbox"（即接收夹改名，改名的 webhook 地址变化已有提示）；`/monitor/save` 在归一化后、补默认接收夹前调用它。(2) 界面把「任务类型」下拉改成只读展示（`select.disabled = true`），并新增 `#monitor-task-type-hint`：编辑接收夹时提示"类型不能改，只有接收目录、分发目标和开关可以调整"，新建/编辑扫描任务时提示"新增的都是普通监控任务；接收夹是内置固定任务，直接在任务列表里编辑它"。(3) 配套：漏传接收夹时沿用当前配置（上一条已做），所以"类型互换 + 漏传"这类绕过方式不会丢接收夹路径。测试：`tests/test_quick_import.py` 增至 50 项（新增 `MonitorTaskTypeConstraintTest` 4 项：扫描不能变接收夹、接收夹不能变扫描、新任务强制 scan、接收夹改名放行），`tests/test_quick_import_frontend.py` 更新类型锁断言。并用临时服务实测三种绕过：类型互换保存后类型原样保留（接收夹 path 仍是 `/115/接收`）、新增 inbox 类型任务被存成 scan、漏传接收夹时接收夹与分发目标保持。完整 unittest 891 项、`compileall`、`node --check`、`git diff --check`、version.json 解析通过；待 `docker compose up -d --build` 后浏览器确认类型下拉是灰的。
- 2026-09-18 | `main` 未提交（并入 `0.11.8`） | 提交前结构复查（用户要求"再检查设计结构以及代码上有没有漏洞，没有的话准备更新提交"）——查出三处真实漏洞并修掉：(1) **换接口可绕过类型锁**：`/save_settings` 也接受 `monitor_tasks` 但没走类型约束，把约束抽成 `core.finalize_monitor_tasks_for_save(existing, posted)`（类型不可改 + 漏传接收夹沿用当前配置），`/monitor/save` 与 `/save_settings` 共用。(2) **接收夹混进扫描链路**（最严重）：`monitor_changes.match_monitor_tasks_for_paths()` 会把接收夹目录的刮削变更匹配成"文件夹监控任务"，进而生成变更事件、`configured_names` 又把它当可执行任务入队（虽然会因 `target_path` 为空在 `validate_monitor_runtime_config` 处报错，但会刷错误日志、并有生成错误变更计划的风险）。修复：`match_monitor_tasks_for_paths` 新增 `_scan_task_names()` 过滤 inbox；`queue_monitor_job()` 命中 inbox 任务一律返回 `inbox` 不入队（含 manual）；`run_monitor_task` 与变更同步执行器各加兜底拒绝（写一条 warn 后结束）。(3) **资源导入文案误导**：接收夹出现在 `monitor_tasks` 后，`resolveResourceMonitorTaskMatch`（`resource/core.js`）会把它当监控任务，弹窗显示"命中文件夹监控任务「接收」，保存完成后会自动触发生成 strm"——实际走的是接收夹整理。修复：匹配时跳过 inbox 并返回 `isInbox/inboxTaskName`，命中时显示"命中接收夹「接收」，保存完成后会自动识别、整理并按类型分发"、`hiddenInput` 不再填任务名；`import-modal.js` 两处成功提示改用响应里的 `data.quick_import_inbox` 显示"保存完成后会由接收夹自动识别整理并分发"，"还没有配置文件夹监控任务"的数量统计也排除内置接收夹。测试：`tests/test_quick_import.py` 增至 52 项（新增 `match_monitor_tasks_for_paths` 跳过 inbox、`queue_monitor_job` 拒绝 inbox），`tests/test_quick_import_frontend.py` 增至 18 项（新增资源导入识别接收夹断言）。完整 unittest 893 项、`compileall`、改动 JS `node --check`、`git diff --check`、version.json 解析均通过；待 `docker compose up -d --build` 后浏览器复核资源导入弹窗文案。
- 2026-09-23 | `main` 未提交 | 完成文件夹监控“运行记录”重构：新增 SQLite `monitor_runs` / `monitor_run_events` / `monitor_run_links`，普通扫描、精准变更同步和接收夹整理均写入结构化生命周期；普通局部刷新展示实际目录，接收夹识别后展示影视名称，网盘分发与后续 STRM 同步按运行 ID 关联。队列合并保留所有来源和局部路径并集，任务范围不会被局部请求收窄；离线接收夹仅在下载确认完成后整理；自动整理和接收夹整理读取刮削真实完成状态。监控页面底部改为可筛选的运行列表与居中详情弹窗（过程/网盘/STRM/问题），提供记录保留、预览后手动清理和历史文本日志弹窗；日夜、键盘关闭、移动端均已复核。新增 `docs/superpowers/plans/2026-09-23-monitor-run-records.md` 工作流图，版本升至 `0.11.11` 并更新 CHANGELOG/README。验证：定向 unittest 242 项、`compileall`、三个改动 JS 的 `node --check`、`git diff --check` 通过；完整 unittest discovery 已运行（其中 CLI 非法命令回归会输出预期 argparse 报错），Docker 重建后浏览器确认运行记录页面及版本 `V0.11.11`。下一步：真实 115 小目录可在部署环境执行一次普通局部刷新和接收夹分发，确认真实网盘操作与 STRM 明细。
- 2026-09-23 | `main` 未提交 | 补齐文件夹监控运行记录的操作闭环：新增 `POST /monitor/runs/{id}/retry` 与 `POST /monitor/runs/{id}/cancel`。失败/部分完成的普通扫描按持久化的全任务或指定目录范围创建独立重试运行，并以 `retry_of` 关联原记录；精准变更同步因未持久化事件范围明确拒绝伪重试。取消只移除仍在队列、尚未派发的同一运行 ID，绝不停止已运行扫描。运行详情仅在状态适用时展示两个按钮，并补充重试来源筛选。新增 3 项运行操作回归；相关 249 项 unittest、`compileall`、`node --check`、`git diff --check` 通过。Docker 已重建；浏览器桌面和 390x844 移动端已复核页面与筛选布局，未对真实网盘任务执行重试或取消。
- 2026-09-23 | `main` 未提交 | 按 `2026-09-23-folder-monitor-log-redesign.md` 完成运行记录整改：`monitor_runs` 兼容历史接收夹计数并将来源、摘要和关联查询统一为中文语义；详情接口合并运行与变更事件，支持分类计数、稳定分页与问题视图；前端新增 `monitor/run-view.js` 作为安全中文显示模型，列表保留异常结论与关键数量，详情按概览、网盘、本地播放文件和问题分类展示，并处理迟到请求、活动刷新、键盘标签和焦点返回。运行记录改为紧凑列表与时间线，日夜主题、窄屏换行和独立滚动正文均已实现；执行前取消显示“已取消”，执行后才显示“已中断”。验证：`tests.test_monitor_runs` 与 `tests.test_monitor_run_frontend` 共 30 项通过，`node --check` 和 `git diff --check` 通过。Docker 构建和本地 FastAPI 端口绑定均被自动审批服务的 404 拒绝，故浏览器验收待可访问 Docker 或本机端口时补做；未对真实网盘任务执行重试或取消。
- 2026-09-24 | `main` 未提交 | 修复运行详情弹窗在切换标签时外框高度变化、宽屏仍固定窄条的问题。根因是专用 `.monitor-run-modal-shell` 规则出现在通用 `.app-dialog-shell` 之前且选择器优先级相同，通用 `30rem` 宽和仅最大高度规则覆盖了专用设置。现改为 `#monitor-run-modal .monitor-run-modal-shell`，宽屏使用 `min(94vw, 76rem)`，外框固定为 `min(82dvh, 50rem)`；正文 `flex: 1 1 auto` 独立滚动，窄屏保留稳定的近全屏高度。新增 CSS 回归，`tests.test_monitor_run_frontend` 5 项与 `git diff --check` 通过。当前浏览器确认运行容器仍提供旧缓存资源（实测 480px 宽）；Docker 重建因自动审批服务的模型配置 404 被拒绝，待可重建后在同一运行详情中切换四个标签复核外框高度与宽屏宽度。
- 2026-09-24 | `main` 未提交 | 修复运行详情标签栏在“网盘操作”和“本地播放文件”等内容切换时被压缩：浏览器实测标签栏为 37.8px，但内部按钮为 34px 且带上下内边距，已经溢出；根因是固定高度弹窗内正文仍采用内容尺寸的弹性基准，导致标题栏和标签栏参与收缩。`#monitor-run-modal .app-dialog-header` 与 `.monitor-run-tabs` 现设为 `flex: 0 0 auto`，正文设为 `flex: 1 1 0`，使正文成为唯一可收缩并滚动的区域。新增 CSS 回归，`tests.test_monitor_run_frontend` 6 项通过；待容器重建后在真实页面切换四个标签检查标签栏完整高度。Docker 重建再次被自动审批服务因模型配置 404 拒绝。
- 2026-09-24 | `main` 未提交 | 修复运行记录“记录保留”弹窗的日间可读性和“立即清理”无反馈问题。浏览器计算样式证实单选项继承夜间 `#cbd5e1`，在白色日间弹窗上对比度不足；现正文改用 `--run-text`，并为日间主题的选项和天数提示设定深色文本，禁用天数行仍保留低强调。清理接口的筛选条件正常；前端在预览为 0 时原先直接返回、接口异常也未显示，现统一显示“无需清理”、预览/保存/清理失败和列表刷新失败状态，清理成功仍刷新记录列表。新增前端回归覆盖日间主题和空结果/异常反馈。`tests.test_monitor_run_frontend tests.test_monitor_runs` 共 34 项、`compileall`、改动 JS `node --check`、`git diff --check` 通过。完整 `unittest discover` 运行 931 项但有 3 项既有失败：`test_log_header_buttons_frontend` 期待模板中仍有“清空日志/加载更早”按钮，`test_resource_sync_frontend` 的 VM 上下文未注入新增的 `window.MonitorRunView`；均与本次变更无关，待单独维护测试基线。当前浏览器仍由旧容器提供缓存静态资源，重新加载后的日间标签计算样式仍为旧 `rgb(203, 213, 225)`，需重建/部署当前代码后做页面复核；未点击真实“立即清理”，避免删除用户运行记录。
- 2026-09-24 | `main` 未提交 | 重构运行记录清理语义，去除 `days=0` 同时表示“长期保留”和“清除全部”的歧义。`cleanup_runs` 现强制使用显式范围：`expired` 必须带正数天数，用于每天自动清理和“清理过期记录”；`all_finished` 只由手动危险操作调用，清除成功、失败、部分完成、无变化和已取消等终态记录。两个范围都会保护排队、运行、等待后续同步的记录，以及仍被活动下游记录引用的已结束父记录。记录保留弹窗分为“保留策略”和“手动清理”：长期保留明确为“不自动清理”，“保留最近 N 天”才可预览/清理过期记录；另有红色“清除全部已结束记录”按钮，使用独立预览与确认文案。新增服务层范围/依赖保护回归与前端结构、请求契约回归。`tests.test_monitor_run_frontend tests.test_monitor_runs` 39 项、`compileall`、`node --check static/js/index.js`、`git diff --check` 通过；未在用户现有记录上执行清理。下一步：部署当前静态资源后复核保留弹窗的两组按钮及危险操作确认框。
- 2026-09-24 | `main` 未提交 | 修复手动清理运行记录在确认后不执行：根因是清理入口调用同步 `window.confirm()`，而项目已将其重写为只打开异步自定义确认框并固定返回 `false`，导致删除请求永远不会发送。两种手动清理现统一等待 `showAppConfirm()` 的真实选择结果；新增行为回归，模拟预览、确认、执行删除和刷新列表的完整链路，覆盖“清理过期记录”与“清除全部已结束记录”。`tests.test_monitor_run_frontend tests.test_monitor_runs` 共 41 项、`compileall`、`node --check static/js/index.js`、`git diff --check` 通过；未删除用户现有记录。Docker 重建因自动审批服务的模型配置返回 404 被拒绝，当前 `18080` 仍提供旧静态脚本，页面实测需在容器成功重建后补做。
- 2026-09-24 | `main` 未提交 | 修复接收夹混合结果长期停在“等待后续同步”：此前只要任一条目分发后产生监控同步事件，收尾逻辑就覆盖整条运行状态为 `waiting`；同批未识别、冲突或搬运失败的条目即使不会有后续分发，也会使记录无法手动清理。现在仅“全部条目已分发”的运行进入等待 STRM 同步；存在留在接收夹条目时立即结束为“部分完成”。若已分发条目的下游同步仍在执行，已有清理保护继续保留父记录。新增混合结果行为回归，验证状态为 `partial` 且没有活跃下游运行时可被“清除全部已结束记录”清理。`tests.test_quick_import tests.test_monitor_runs tests.test_monitor_run_frontend` 共 95 项、`compileall`、`git diff --check` 通过；未操作用户现有接收夹文件或运行记录。
- 2026-09-24 | `main` 未提交 | 补齐接收夹卡住记录的历史修复：新增 `settle_waiting_inbox_runs()`，启动后只结算 `run_kind=inbox`、状态为 `waiting` 且结果含留在接收夹条目的旧运行，将其改为“部分完成”、写入结束时间与原因事件；普通扫描、已全部分发的等待 STRM 记录不受影响。存在活跃下游运行时，运行记录清理的现有依赖保护仍会保留父记录。新增服务层回归覆盖历史 `left_items` 兼容、状态结算与清理。`tests.test_quick_import tests.test_monitor_runs tests.test_monitor_run_frontend` 共 97 项、`compileall`、`git diff --check` 通过；未操作用户现有接收夹文件或运行记录。
- 2026-09-24 | `main` 未提交 | 从根源重构接收夹与下游监控的两阶段状态机：混合识别结果只要产生同步事件就进入 `waiting`，统一归纳器同时检查子运行和持久化变更事件，支持多下游、子任务先完成竞态及最终 `completed/partial` 收尾；未识别/计划冲突/目标不可用/分发失败统一记录 `reason_code`，条目事件改为终态 `skipped`，界面显示“未处理”。变更事件 ID 统一扁平化，变更任务异常先写终态且日志失败不再遗留假“执行中”；启动恢复会结束旧进程遗留的排队/运行记录，再恢复事件并归纳等待父记录。定向 207 项 unittest、`compileall`、改动 JS `node --check`、`git diff --check` 通过；完整 951 项 unittest 有 3 项已知既有失败（旧日志按钮断言 1 项、资源同步沙箱未加载 `MonitorRunView` 2 项），与本次改动无关。未执行真实接收夹分发或删除运行记录。
- 2026-09-24 | `main` 未提交 | 完成接收夹两阶段状态机的最终收尾复核：变更同步先持久化业务终态，再以尽力方式更新成功页脚和界面摘要，避免展示层异常把已成功运行覆盖成失败；新增对应回归。同步维护旧前端测试夹具：日志按钮断言适配当前运行记录按钮，资源同步沙箱加载 `MonitorRunView`。定向 208 项、完整 952 项 unittest、`compileall`、改动 JS `node --check`、`git diff --check` 全部通过。未执行会修改真实网盘内容的接收夹分发，也未清理现有运行记录。
- 2026-09-24 | `main` 未提交 | 修复接收夹运行详情把一次分发显示为两次“网盘移动”：父运行已有“接收夹分发”结构化记录，而关联 `monitor_change_events` 只是后续 STRM 同步输入，详情查询此前把两者都归到网盘操作。现对 `run_kind=inbox` 的父记录隐藏原始变更事件，保留接收夹分发；下游同步运行和数据库事件不受影响。新增详情回归。完整 954 项 unittest、`compileall`、`git diff --check` 通过；未执行真实网盘操作。
- 2026-09-24 | `main` 未提交 | 修复资源离线任务保存到接收文件夹时被当作“未绑定监控”并过早结束的问题。任务携带 `quick_import_inbox` 标记时，离线提交后保持 `submitted` 并启动离线完成观察；下载完成后运行接收夹整理与分发，已分发内容再由目标监控任务同步本地播放文件。资源任务卡片按该标记显示“接收文件夹 / 下载完成后整理与分发 / 接收夹整理”，不再错误显示“当前目录未纳入文件夹监控”。新增离线提交、完成提示和前端文案回归；未执行真实网盘操作。下一步：重启或重建当前服务后，在资源页提交一个保存到接收夹的离线任务，确认卡片文案与下载完成后的整理链路。
- 2026-09-24 | `main` 未提交 | 修复批量整理窄屏条目中 TMDB 封面挤压“大爆炸”完整路径选词的问题。根因是条目网格始终保留“复选框 / 主内容 / 封面”三列，封面在手机宽度下吞掉主内容宽度。现仅在 `max-width: 760px` 改为两列，封面移到主内容列下方并缩为 `72x108`，选词组件恢复整行主列宽度；桌面端右侧封面不变。新增响应式 CSS 回归；相关 21 项 unittest、`compileall`、`git diff --check` 通过。下一步：服务加载当前静态资源后，在手机宽度的批量整理弹窗确认选词区域宽度与封面位置。
- 2026-09-24 | `main` 未提交 | 按批量整理条目三段式重排：第一段“原始文件夹”显示来源名称和媒体数量；第二段“当前影视信息”展示识别状态、影视标题和右侧封面；第三段“改绑”承载改绑按钮与完整路径选词/搜索内容。封面从条目外层移入第二段内部的标题右侧，不再干扰第三段的选词宽度；手机端封面保持右侧但缩至 `72x108`。新增结构及响应式回归，`tests.test_scraper_batch_organize tests.test_scraper_path_selection_frontend` 共 139 项、`node --check`、`compileall`、`git diff --check` 通过。Playwright 浏览器检查因 npm registry DNS 不可用未执行；未操作真实网盘任务。
- 2026-09-24 | `main` 未提交 | 完成运行记录分类整改：列表筛选明确拆为“配置任务 / 运行流程 / 启动方式 / 运行结果”，配置任务显示“目录监控”或“接收夹整理”类型；新增 `run_kind` 后端筛选，选择“增量变更同步”时才纳入下游子运行，并补“来自接收夹整理：任务 · 条目”上下文。默认列表同时排除直接父子和仅通过关联表绑定的下游运行，避免父子重复混排。订阅批次收口、历史待刷新和订阅离线导入触发的监控运行统一标记为“订阅任务”；变更来源文案改为“检测到网盘变更”，与“增量变更同步”流程区分。新增流程筛选、关联子运行上下文和订阅来源回归；定向 74 项与完整 `unittest discover -s tests` 零退出，`compileall`、两个改动 JS 的 `node --check`、`git diff --check` 通过。真实页面复核待服务加载当前静态资源后进行。
- 2026-09-24 | `main` 未提交 | 历史文本日志弹窗补“清空日志”（用户反馈：新运行记录列表上线后，旧日志只能看、没有清除入口）。根因有两处：(1) 弹窗里只有加载逻辑，旧的“清空日志”按钮随底部日志区块一起被移除，`clearMonitorLogs()` 成了没有入口的死代码；(2) `clear_log_file()` 只截断主文件，而 `_read_monitor_log_entries_from_files()` 会把 `monitor.log.1`/`.2` 轮转备份一起读出来展示，所以即便调接口，清完弹窗里仍会显示旧内容。修复：后端 `clear_log_file()` 统一为“清空主文件 + 删除轮转备份”，监控/系统/订阅三处清空接口一致生效；模板在弹窗页头新增“清空日志”按钮、用途说明和结果状态行；前端把弹窗逻辑拆成 `loadLegacyMonitorLogs()` / `clearLegacyMonitorLogs()`，经 `showAppConfirm()` 确认后清空并重新加载，失败会显示原因（`clearMonitorLogs()` 不再吞掉请求异常）。新增回归：`tests/test_log_file_clearing.py` 3 项（备份被删、重复清空、清空后分页不再含旧内容）、`tests.test_monitor_run_frontend` 4 项（按钮与链路、确认后执行、取消不执行、失败提示）。定向 80 项、完整 `unittest discover -s tests` 974 项零失败，`compileall`、`node --check static/js/index.js` 通过。下一步：容器重建后在页面点一次“清空日志”，确认日夜主题下的按钮与状态行、并确认“运行记录”不受影响（未在用户现有日志上执行清空）。
- 2026-09-24 | `main` 未提交 | 修复运行详情弹窗“刷新”按钮随窗口宽度漂移（用户浏览器标记：`div#monitor-run-modal .app-dialog-header > button.monitor-run-refresh` 定位不稳）。根因是 `.app-dialog-header` 用 `justify-content: space-between` 排三个平级子元素（标题块 / 刷新 / 关闭），中间子元素被居中，位置随标题换行和容器宽度变化。现把共享页头约定为“标题块 + 右侧动作组”两种子元素：新增 `.app-dialog-header > :first-child { flex: 1 1 auto; min-width: 0; }` 让标题吃掉剩余宽度，`.app-dialog-header-actions { flex: 0 0 auto; margin-left: auto; }` 承载动作按钮；运行详情页头的“刷新 + 关闭”和旧日志弹窗的“清空日志 + 关闭”都收进动作组，标题过长时只换行不再推挤按钮。新增结构回归 `test_dialog_header_pins_actions_to_the_right`；浏览器实测 1600/1280/1024/845/768/430 六档宽度下动作组与关闭按钮间距恒为 8px、距弹窗外框恒为 17.8px，页头无横向溢出，“记录保留”“历史文本日志”弹窗样式未受影响。`tests.test_monitor_run_frontend` 20 项、完整 `unittest discover -s tests` 975 项零失败，`git diff --check` 通过。
- 2026-09-24 | `main` 未提交 | 按复查结论修复 0.11.11 运行记录的功能缺口。(1) **变更同步看不到网盘改动**（P1）：详情用 `monitor_change_events.monitor_run_id = run_id` 关联事件，但该字段只有接收夹分发会写，实测全库 445 条变更事件 440 条没有归属，48 事件完成的变更运行「网盘操作 0」。现 `process_monitor_change_events(monitor_run_id=...)` 在处理成功时把运行 ID 写回事件（已有归属不覆盖，保住接收夹两阶段状态机），`services/monitor.py` 变更流程传入 `run_id`；新增 `monitor_runs.repair_change_event_owners()` 在启动时一次性补齐历史事件，只在“任务相同 + 完成时间落在唯一一条变更同步运行区间内”时归属，歧义事件不猜。用真实库副本实测：未归属事件 440 → 392，05:54 变更运行「网盘操作」0 → 48 条（网盘移动/重命名）。(2) **部分完成/失败没有解释**（P1）：变更失败现写入 `problem` 事件（失败条数 + 原始诊断）；`run-view.js` 为早期只存数量的记录按 `failed_dirs/manual_required/left/failed` 生成“未完成内容”说明块与概览入口。(3) **查询随历史劣化**（P1）：`monitor_run_links.related_run_id` 补索引（实测单条按 `related_run_id` 查询 426µs → 7µs），`list_runs` 的子运行统计与父运行上下文改为批量查询（2.2 万条记录 137ms → 1.2ms）；运行记录列表加渲染键，不再随每次状态推送整块重绘。(4) **补回丢失入口**：历史文本日志弹窗恢复“加载更早的日志”（使用接口既有的 `has_more/next_offset`），筛选区新增“重置筛选”（有筛选时才出现），结论文案改为中文说明句（`build_monitor_run_summary` / `build_monitor_change_run_summary`，文本日志行保持不变）。(5) 参数容错：`/monitor/runs` 与 `/monitor/runs/{id}` 的 `limit/offset` 非法值退回默认值不再抛 500；列表标题按文档限制两行。定向回归：`tests.test_monitor_runs` 50 项、`tests.test_monitor_run_frontend` 25 项、`tests.test_scraper_monitor_sync` 105 项、`tests.test_monitor_log_readability` 16 项；完整 `unittest discover -s tests` 991 项零失败，`compileall`、三个改动 JS 的 `node --check`、`git diff --check`、version.json 解析通过。
- 2026-09-24 | `main` 未提交 | 上一轮修复的页面复核（容器已重建到最终代码）：变更同步运行「网盘操作」由 0 变为 48 条并列出“网盘移动/重命名 + 文件路径 + 时间”；启动时的历史归属补齐在真实库生效（未归属事件 440 → 392，`idx_monitor_run_links_related` 已建）；“部分完成”运行的问题标签显示 1 条并给出“需要手动监控 1 处 + 建议下一步”，概览出现“1 项未完成内容，查看原因与影响”入口；筛选“接收夹整理”后列表 2 条且“重置筛选”出现，点击后回到 8 条并自动隐藏；旧文本日志弹窗请求 `offset=0`，按钮组为“清空日志 / 关闭 / 加载更早的日志”（当前文本日志被清空过只有 1 段，故“加载更早”按预期隐藏）。复核期间发现重置筛选在固定 1.2 秒等待下偶尔读到旧列表，改为等待接口响应后连续两次稳定；同时给 `resetMonitorRunFilters()` 加了立即收起按钮 + 作废渲染键的兜底。完整 `unittest discover -s tests` 993 项零失败。
- 2026-09-24 | `main` 未提交 | 按用户澄清的语义补齐“按当前所选任务清空”：旧文本日志继续只在“历史文本日志”弹窗里用独立的“清空日志”，运行记录页新增“清空该任务记录”。实现：`cleanup_runs(scope, days, preview, task_name="")` 支持把清理范围收窄到单个任务（沿用原有保护：运行中/排队中/等待后续同步及其依赖记录保留），`POST /monitor/runs/cleanup` 接收 `task_name`；前端按钮只在“任务筛选”选中具体任务时出现（未选中时隐藏并提示“请先在上方选择一个监控任务”），点击后先预览条数、再应用确认框（明确写清保留规则与“不影响网盘文件与本地播放文件”）、成功后刷新列表。页面实测：未选任务按钮隐藏；选中「电影」后按钮出现且 title 为“清空「电影」的已结束运行记录”；点击后预览 4 条并弹出确认框（仅取消、未删除真实记录）；切回“全部配置任务”按钮隐藏。回归：`tests.test_monitor_runs` 新增按任务清理范围用例、`tests.test_monitor_run_frontend` 新增按钮出现条件与确认/取消链路用例；完整 `unittest discover -s tests` 996 项零失败，`compileall`、`node --check`、`git diff --check` 通过。
- 2026-09-24 | `main` 未提交 | 发布准备：版本号 `0.11.11` → `0.11.12`（`version.json` 的 `buildDate` 改为 `2026-09-24`，静态资源缓存串随之失效）。`CHANGELOG.md` 拆成两段：`[0.11.11] - 2026-09-23` 只保留运行记录重构（结构化运行记录、接收夹流程、范围/来源修正、记录保留、运行操作闭环），本轮复查修复与补齐（变更同步网盘明细 + 历史归属补齐、部分完成/失败原因、中文结论句、索引与批量查询、历史文本日志可清空/可翻更早、重置筛选、清空该任务记录、页头按钮贴右、参数容错）迁到新的 `[0.11.12] - 2026-09-24` 段落并附验证清单。`README.md`「近期更新」的当前版本改为 `0.11.12` 并补一条本轮变更摘要。验证：`tests.test_monitor_runs` 51 项、`tests.test_monitor_run_frontend` 28 项、`tests.test_scraper_monitor_sync` 105 项、`tests.test_monitor_log_readability` 16 项、完整 `unittest discover -s tests` 996 项零失败，`compileall`、`node --check`、`git diff --check`、`version.json` 解析通过。下一步（等用户确认）：提交 + 推送后打 tag / 构建并推送镜像，使 GitHub 上的 `version.json` 与本地一致（关于页的“远端最新版本”以远端 `version.json` 为准）。
- 2026-09-24 | `main` | 发布准备 + 提交：版本号 `0.11.12` → `0.11.13`（`version.json` 的 `notes` 换成本轮四条修复条目，`buildDate` 保持 `2026-09-24`，静态资源缓存串随之失效）；`CHANGELOG.md` 的 `[未发布]` 段并入 `[0.11.13] - 2026-09-24`；`README.md`「近期更新」当前版本改为 `0.11.13`。验证：完整 `unittest discover -s tests` 1018 项零失败、`compileall`、两个改动 JS 的 `node --check`、`git diff --check`、`version.json` 解析通过。本轮只提交、未推送、未打 tag、未构建镜像。另记一个已知小概率问题（用户明确暂不处理）：外部来源的媒体文件夹若用 `{tmdb-N}` 之类写法且译名不同，本项目按“文件夹名”判定同一部作品（`scraper_folder_name_key` 只忽略 `[tmdbid-…]`/`(n)` 装饰、全项目没有按 TMDB ID 找文件夹的逻辑），会出现同一 TMDB ID 的两个文件夹各自保存一份；根治方案是按 TMDB ID 做身份判定（含 `{tmdb-N}` 等装饰写法），待用户需要时再实施。
- 2026-09-24 | `main` | 用户澄清后修正 0.11.13 的设计误解（该提交已推到 origin/main，故不重写历史，改为 0.11.14）：运行记录**不做工作单元分组**，改回“一条任务一行”——接收夹整理是一条任务，它分发出去触发的每条扫描/自动补扫也是独立任务。具体：`list_runs` 恢复平铺输出（去掉 `group_runs/group_total/group_more` 与前端 `listGroup/listHtml`、折叠按钮、缩进样式、`monitorRunCollapsedGroups`），默认可见性改回只排除“触发记录的直接子运行”（`parent_run_id != ''`），二次分发出来的运行只挂 `downstream` 关联（`queue_monitor_dir_scan` 不再写 `parent_run_id`）因而照常成行，也让“按配置任务筛选”能筛到它们；`_add_parent_contexts` 只在父运行是 `inbox` 时补“来自接收夹整理”上下文，避免扫描行显示成“来自接收夹整理：电影 · 文件变更”。时间顺序修正保留并加固：`_touch_ancestors` 给祖先活动时间加毫秒后缀（`…:06.999999`），所有运行自身写入改为 `updated_at = MAX(updated_at, ?)` 单向递增，保证同秒内触发记录也排在下游之前；`_descendant_entries` 为只有关联表父级的运行补 `link_parent_id`，详情链路仍按前序展示（变更同步 → 自动补扫）。验证：`tests.test_monitor_runs` 67 项、`tests.test_monitor_dir_rescan` 28 项、`tests.test_monitor_run_frontend` 33 项、完整 `unittest discover -s tests` 1017 项零失败，`compileall`、`node --check`、`git diff --check`、`version.json` 解析通过；本地 /tmp 预览复核本页 7 条 = 1 条接收夹整理（置顶、后续 6 项）+ 6 条「系统补扫」各行一条、无分组节点。下一步：用户重建容器后确认列表顺序与状态。
- 2026-09-25 | `main` | 用户反馈“接收夹推送到监控文件夹后没有对应的单条生成记录”，定位为：只有清单未知（`manual_required`）的目录才会触发补扫并留下单条记录，清单已知的条目由变更同步内部处理，列表里只有一条「最近接收」。修复：`monitor_changes.process_monitor_change_events` 新增 `dispatched_item_paths`——逐个事件判断归属运行（`monitor_change_events.monitor_run_id`）是否为 `run_kind='inbox'`，是则把该条目在监控目录里的位置（文件夹条目取新路径，文件条目取其媒体文件夹）收集起来（`_dispatch_item_scope` / `_inbox_dispatched_paths`）；`run_monitor_change_task` 用新增的 `_queue_dispatch_item_scans` 对每个条目**逐条**排一次目录扫描（`queue_monitor_dir_scan(..., run_source="inbox_dispatch", force_new=True)`，`queue_monitor_dir_scan` 因此新增 `force_new` 参数），它们只挂 `downstream` 关联所以是列表里的独立任务；同样被覆盖的目录不再重复走“系统补扫”（`_path_covered_by_scopes`）。前端运行记录的“启动方式”新增「接收夹分发」（`SOURCE_LABELS` / `run-view.js` / `monitor_about.html` 筛选）。版本号 `0.11.14` → `0.11.15`，CHANGELOG/README 同步。验证：`tests.test_monitor_dir_rescan` 29 项（新增“分发条目各自成行 + 链路等待 + 完成后一起收尾”）、`tests.test_scraper_monitor_sync` 107 项（新增“只有归属接收夹整理的事件才算分发出的条目”“文件条目映射到媒体文件夹”）、`tests.test_monitor_runs` 67 项、`tests.test_monitor_run_frontend` 33 项；完整 `unittest discover -s tests` 1020 项零失败，`compileall`、`node --check`、`git diff --check`、`version.json` 解析通过；本地 /tmp 预览复核 7 条 = 1 条「最近接收」+ 6 条「启动：接收夹分发」。下一步：用户重建容器后确认分发后每个影视都有单条记录。
- 2026-09-25 | `main` | 用 `docker compose up -d --build` 重建本地容器后发现（关键诊断）：用户运行的实例一直是 0.11.12 的旧镜像（容器内 `/app/version.json` = 0.11.12，代码里没有 `dispatched_item_paths`），所以此前两轮修复“看起来没生效”是部署问题，不是代码问题；重建后容器内版本为 0.11.15（启动日志正常，登录页 200）。随后按用户要求把运行详情改成默认展开：`run-view.js` 的 `eventCard` 步骤明细固定 `open`（原逻辑只在执行过程 / 问题 / 失败时展开），`valueHtml` 的「原始诊断信息」也 `open`；新增前端用例 `test_run_detail_expands_record_details_by_default`。版本号 `0.11.15` → `0.11.16`，CHANGELOG/README/version.json 同步；`tests.test_monitor_run_frontend` 33 项、完整 `unittest discover -s tests` 1021 项零失败。
- 2026-09-25 | `main` 未提交 | 需求（用户）：接收夹识别分发直接采用 AI 结果、看不到「把原来的影视识别成了什么」，且规则+AI 仍识别不准（例：`[朱弦玉磐2024]...Musica.2024...` → 误配 `为乐而生(2023)`；`七勇破星阵.Star.Pilot.1966` → 文件夹被命名成 TMDB 中文名「星际飞行员」）。定位根因三条：(1) 文件名开头方括号里的中文片名被当噪声整体丢弃，只拿 `Musica` 去搜；(2) `_ai_match_one_item` 里 `year = ai_year or result["year"]` 让 AI 年份压过文件名确定性年份，且 AI 选片不校验年份；(3) `ai_match_min_confidence` 默认 0，AI 自报多低都直接采用。实现：**可视化**——`quick_import.run_quick_import` 的 move/merge 事件 detail 新增 `original_name`（整理前原文件名）、`match_source`（规则匹配 / AI 识别）、`confidence`、`match_reason`、`tmdb_id`、`identified_year`，`run-view.js` 的 `fields` 表补对应中文标签；**规则准确率**——`scraper` 新增 `_extract_scraper_leading_cjk_titles`（提取开头括号中文片名，去掉年份、过滤字幕/字幕组/简繁/双语/国语/配音等噪声括号，并用 `_SCRAPER_SUBTITLE_MARKER_RE` + `common_re` 双重过滤），`_extract_scraper_title_candidates` 把它作为最高优先级候选；**AI 准确率**——`_ai_match_one_item` 改为 `year = 确定性年份 or ai_year`，并在 AI 选中候选年份与条目年份冲突时标 `ai_year_conflict` 直接不采纳；`ai_match_min_confidence` 默认 0→60（`core.py` 默认值/迁移兜底 + `ai_match.py` 运行时兜底 + `normalize_config` 把旧默认 0 升级为 60，显式非 0 值保留），设置页提示同步；**命名**——`choose_scraper_title` 对电影条目优先用文件名里的中文片名作为文件夹标题（新增 `_extract_scraper_filename_cjk_title`，含结构性占位词黑名单 `_SCRAPER_STRUCTURAL_CJK_TITLES`），剧集仍以 TMDB 命名为准（避免单集文件名的占位词干扰）。验证：`tests.test_scraper_batch_organize`（新增“保留括号内中文片名”“电影文件夹优先用文件名中文名”）、`tests.test_scraper_ai_match`（新增“确定性年份优先”“拒绝年份冲突候选”“旧默认 0 升级为 60”）、`tests.test_quick_import`（新增“移动事件写入识别映射”）、`tests.test_monitor_run_frontend`（新增“移动明细展示识别映射字段”）；完整 `unittest discover -s tests` 1028 项零失败、`compileall app main.py`、改动 JS 的 `node --check`、`git diff --check`、`version.json` 解析通过；mock TMDB 搜索复核 `[朱弦玉磐2024]...Musica.2024` 直接 `status=auto` 命中「朱弦玉磐」2024。版本号 `0.11.16` → `0.11.17`，CHANGELOG/README 同步。下一步（需用户重建容器验证）：真实 115 接收夹推一条中文片名在方括号里的电影，确认运行记录能看出「原名→识别为」且不再错配。
- 2026-09-25 | `main` 未提交 | 需求（用户）：把「接收文件夹」的文档口径对齐到真实目标——它是**分类前的中转文件夹**（电影 / 电视剧各有一个监控目录，以前保存 / 推送前要先挑分类，现在统一丢进接收夹即可），收到内容后按识别结果和分发目标的刮削预设整理并移动过去；同时明确「接收文件夹同样支持油猴脚本推离线链接，鉴权与普通监控任务共用同一个密钥，只是接收后的处理流程不同」。核对结论：鉴权在代码里**本来就是全站唯一**的 `webhook_secret`（`app/routes/monitor.py` 的 `_verify_webhook_auth` 对 `/webhook/{任务名}` 的所有任务类型共用一套 token / ts+nonce+sign 校验，`normalize_task` 里没有任务级密钥字段），所以本次不需要新增按任务密钥，只把口径写清并修掉会误导的描述。改动：`README.md`「直接推送到接收夹」改为「统一落到接收夹：分类前的中转文件夹」（补目的说明 + 「和普通监控任务共用同一个 Webhook 签名密钥」条目 + 油猴关系章节的三条口径）；`templates/partials/pages/settings.html` 第 8 节移除已失效的「新增任务 → 任务类型选接收夹任务」说法（接收夹是内置固定任务），改为说明中转目的、内置任务位置与密钥共用；`templates/partials/modals/monitor.html` 的 webhook info-dot / 默认提示块 / 接收夹路径 info-dot / 分发目标说明 / 推送地址说明同步改成「扫描任务 savepath 在扫描路径内、接收夹任务填接收夹或子目录，全站唯一密钥两者共用」；`static/js/index.js` 的 `refreshWebhookHint()` 两个分支（扫描 / 接收夹）都写明密钥共用与接收夹目的；`115-magnet-helper-webhook.user.js` 任务管理器说明改写（接收夹＝分类前中转 + 密钥只有一把 + 保存路径可留空）、编辑器 savepath 占位符改为两种任务都可读，`@version` 2.7.0 → 2.7.1；顺带修两处不一致：脚本 `validateManagerTask` 不再要求 savepath 非空（面板本来就写「留空则默认落到接收夹」，服务端收 400 时改成「普通监控任务必须填该任务目录内的保存路径；接收夹任务可以留空」），`quick_import.validate_quick_import_config` 的「还没有接收夹任务，请先新增一个」改为提示内置任务缺失需重启 / 查配置。测试：`tests/test_quick_import_frontend.py` 新增 `test_receive_folder_docs_state_staging_purpose_and_shared_secret`（README / 设置页 / 弹窗 / index.js / 油猴脚本的口径锁定，并断言旧「新增接收夹任务」说法已删除）、`tests/test_magnet_helper_userscript.py` 新增「保存路径可留空、名称与请求地址仍必填」、`tests/test_monitor_webhook_quick_import.py` 新增「同一个 token 既能推接收夹也能推普通监控任务」与「扫描任务漏填 savepath 的提示同时说明两种任务」。验证：上述三个测试文件 51 项通过、完整 `unittest discover -s tests` 1032 项零失败、`compileall app main.py`、`node --check static/js/index.js 115-magnet-helper-webhook.user.js`、`git diff --check` 通过。版本号 / `CHANGELOG.md` 未改（本次为文案与口径对齐，未做发布）；下一步（需用户确认）：重建容器复核设置页第 8 节、任务弹窗提示与油猴脚本任务管理器文案，若确认要作为版本发布再补 `version.json` / `CHANGELOG` / README 版本条目。
- 2026-09-25 | `main` 未提交 | 承接上一条（同一批改动）：`docs/superpowers/specs/2026-09-23-folder-monitor-workflow-design.md` §3.4「接收夹」补一段定位说明——接收夹是分类前的中转文件夹、是整理链路的正式一环而不是可选旁路，且与普通监控任务共用同一个全局 `webhook_secret`（不存在按任务拆分的密钥）。纯文档补充，无代码改动。
- 2026-09-25 | `main` 未提交 | 用户澄清并纠正上一批的口径（重要，别再写反）：**接收夹是新增的功能与便捷入口，不是强制流程**——用户完全可以按旧用法继续手动推送 / 保存到电影、电视剧各自的监控目录，两种用法并存。上一批我写成"不是备选路径、而是默认统一接收入口 / 整理链路正式一环"，属于把定位写重了。本次改正：`README.md` 接收夹章节补第二段（明确"不是强制流程：原来的用法完全保留，直接指定分类目录行为不变，接收夹只是多一个选择"）；`templates/partials/pages/settings.html` 第 8 节的接收夹说明改为「内置接收夹任务（可选）」并补"不用它也没关系，照旧把保存路径填到分类监控任务目录，两种用法可并存"；`templates/partials/modals/monitor.html` 的接收夹路径 info-dot 改为"新增的便捷入口（可选，不是强制流程）…不想用它时照旧填分类监控任务目录"；`static/js/index.js` 的接收夹 webhook 提示补"不用它也能照旧把 savepath 填到分类监控任务的目录"；`115-magnet-helper-webhook.user.js` 任务管理器说明同上（`@version` 保持 2.7.1）；`docs/superpowers/specs/2026-09-23-folder-monitor-workflow-design.md` §3.4 改为"属于新增的便捷入口，不是强制流程，原用法继续有效"。回归同步：`tests/test_quick_import_frontend.py` 的口径锁定用例增加「便捷入口」「不是强制流程」「可选的便捷入口」断言，防止再次写重。验证：`tests.test_quick_import_frontend tests.test_magnet_helper_userscript tests.test_monitor_webhook_quick_import` 51 项、`node --check`、`git diff --check`、`_read_template('index.html')` 实渲染通过；代码逻辑未动（密钥本来就是全站共用的一把 `webhook_secret`）。
- 2026-09-25 | `main` 未提交 | 用户反馈：手机端油猴脚本推送到**接收夹**报 `401 {"ok":false,"msg":"Webhook 签名已被使用"}`，推到**监控文件夹**却成功。定位：该 401 来自 `app/routes/monitor.py::_verify_webhook_auth` 的防重放——`webhook_used_nonce_cache` 以 `ts:nonce` 为键，且**唯一写入点在签名校验通过之后**，所以"已被使用"必然意味着同一份签名提交了两次、且第一份已受理。脚本侧能造成这件事的只有 `postJson()`：GM_xmlhttpRequest 返回 status 0（18s 超时 / iOS 网络抖动 / 扩展未就绪）时会拿**同一份 headers** 走 fetch 兜底重发，第二次被判重放，而 toast 显示的正是兜底那次的结果。所以这和接收夹无关——鉴权发生在任务类型判断之前，扫描任务与接收夹共用同一段代码；"接收夹失败、监控任务成功"最可能是页面打开后的第一次点击撞上了 GM 通道失败，随后那次点击其实已经建了任务。本地复核：同一份签名连续调用两次 `_verify_webhook_auth` → 第一次通过、第二次返回"已被使用"；另核对本地数据卷 `.../115strmxianer` 的 `monitor.log` 与 `resource_jobs`（最新 09-25 00:18，全部是 manual_import / subscription，无 userscript_webhook 记录），确认手机推的是另一个部署、取不到那次服务端日志。修复（改根因而不是提示层）：`115-magnet-helper-webhook.user.js` 的 `postJson` 支持"请求头工厂"（新增 `resolveRequestHeaders`），`pushMagnet` 每次尝试（GM / fetch 兜底）都重新签名，绝不重发同一份签名；同一条磁力 + 同一保存路径命中去重（409）改判为"已提交过：这条磁力正在后台处理（上一次点击可能已经成功），请到任务中心确认"，用新增的琥珀色 `warn` toast 而不是报错；401 且含"签名已被使用"时提示"到任务中心确认是否已提交，确认没有收到再重推"；`@version` 2.7.1 → 2.7.2。服务端把该错误文案补成"Webhook 签名已被使用：同一份签名被重复提交（客户端重试或重复点击）"，便于其他客户端自查。`README.md` 油猴章节新增「常见报错」两条（401 已被使用 / 409 已提交过）并更新脚本版本说明与 2.7.2 变更点。测试：`tests/test_magnet_helper_userscript.py` 16 → 18 项（新增「GM 失败后 fetch 兜底必须换一份新签名（两次请求的 nonce / sign 不同）」「409 提示已提交过且不再回退 fetch」）；完整 `unittest discover -s tests -p 'test_*.py'` 1034 项零失败，`node --check`（脚本 + index.js）、`git diff --check` 通过。下一步（需用户操作）：手机端重新安装 / 更新油猴脚本（面板 `/userscript/magnet-helper.user.js`，`@version` 应为 2.7.2）后重试，并先在「任务中心」确认那条磁力是否其实已经提交成功；若仍复现，把手机时间与推送时间点发我，再按 `X-Webhook-Ts/X-Webhook-Nonce` 查服务端日志。
- 2026-09-25 | `main` 未提交 | 承接上一条（用户追问"确定是这个问题吗"）：补两条**服务端复现用例**把结论钉死——`tests/test_monitor_webhook_quick_import.py` 新增 `test_identical_signature_is_accepted_once_and_rejected_twice`（同一份 ts+nonce+sign 提交两次：第一次 200 建任务、第二次 401「签名已被使用」，正是手机截图的现象）与 `test_fresh_signature_retry_falls_into_dedupe_not_replay`（换成新签名重试：200 后是 409 去重、不再撞防重放），并加 `_signed_headers()` 辅助函数按脚本算法造签名。用途：向用户证明"401 必然等于同一份签名提交了两次、且第一次已受理"，不必只靠推理。另核实：接收夹 webhook 服务端路径（`build_quick_import_config` / `validate_quick_import_config` / `resolve_provider_relative_path`）全是配置计算、无网络 IO，不会天然拖过脚本 18s 超时；用户 NAS 跑的是**已发布镜像**，面板下发给手机的脚本仍是旧版（≤2.7.0），所以本次修复（每次重试重新签名）不会自动生效——要生效需出新镜像后重装脚本，或手动更新手机上的脚本内容。验证：三个相关测试文件 55 项、完整 `unittest discover -s tests -p 'test_*.py'` 1036 项零失败，`node --check`、`git diff --check` 通过。
- 2026-09-25 | `main` 已提交并推送 | 发布 **0.11.18**（`version.json` 0.11.17 → 0.11.18，`buildDate` 保持 `2026-09-25`，`notes` 换成 5 条本批说明），`CHANGELOG.md` 新增 `[0.11.18] - 2026-09-25` 段（三节：手机端 `401 Webhook 签名已被使用` 的定位与修复 / 接收夹口径对齐为"可选便捷入口 + 共用同一个签名密钥" / 其它提示容错 + 验证清单），`README.md`「近期更新」当前版本改为 `0.11.18` 并新增首条摘要，本记录同步。本批实际内容（详情见上面两条交接）：(1) 油猴脚本 `@version` 2.7.0 → **2.7.2**：`postJson` 支持请求头工厂、每次尝试（GM / fetch 兜底）重新签名，修复手机端偶发 401「签名已被使用」；409 去重提示改为「已提交过」（新增 warn 配色）；401 且含"签名已被使用"时给出引导；保存路径允许留空。(2) 服务端：防重放错误文案补成"同一份签名被重复提交（客户端重试或重复点击）"，空 savepath 提示写明普通任务必填 / 接收夹可留空，内置接收夹缺失提示改为重启 / 查配置。(3) 文档与界面口径：README / 设置页第 8 节 / 任务弹窗 / `index.js` webhook 提示 / 脚本管理器说明 / 设计草案 §3.4 统一为"接收夹＝分类前的中转文件夹、可选的便捷入口、不是强制流程、与普通监控任务共用同一个全站签名密钥"。(4) 测试：`tests.test_monitor_webhook_quick_import` 新增"同一签名两次 → 200/401"与"新签名重试 → 200/409"两条服务端复现用例，`tests.test_magnet_helper_userscript` 16 → 18 项，`tests.test_quick_import_frontend` 新增口径锁定用例。验证：完整 `unittest discover -s tests -p 'test_*.py'` **1036 项零失败**，`compileall app main.py`、`node --check`（脚本 + index.js）、`git diff --check`、`version.json` 解析通过。下一步（用户）：等 GitHub Action 构建出 `xianer235/115-media-hub:0.11.18` 后，在 NAS 拉取新镜像并重启容器；手机端重新安装 / 更新油猴脚本（`@version` 应显示 2.7.2）；再推一条磁力到接收夹验证，若仍报错先按面板「监控日志」核对是否成对出现「Webhook 磁力任务已创建」与「Webhook 校验失败: … 签名已被使用」。
